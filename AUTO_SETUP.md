# AUTO SETUP ? MC Skill MCP Server

> **? AI Agent ?????????**  
> ????????? Cursor???? AI IDE???AI ??????????????????????

---

## ??

? `mcp-server` ?????**??** Cursor MCP ?????????????**??????**??? `mcp.json`?**??????** `mklink`???????????????? stdio MCP?? **31** ??????

---

## ????

### Step 1 ? ???????

?????????????? `MC_skill/`?

```bash
pwd
```

????? `cd` ? `MC_skill` ????

---

### Step 2 ? ?? Node????????

```bash
node -v
# ?? Node.js >= 22.5?package.json engines?Yarn ? node:sqlite?
node -e "const [maj,min]=process.versions.node.split('.').map(Number); if(maj<22||(maj===22&&min<5)){console.error('Need Node >=22.5');process.exit(1)} else console.log('OK',process.versions.node)"

cd mcp-server
npm ci
npm run build
npm run build:yarn-sqlite
```

???

```bash
ls dist/index.js
```

- ?? ? ?? `engines`??? **Node.js 22.5+**
- Yarn ???? `data/fabric_*/mappings/yarn-mappings.sqlite`?? `build:yarn-sqlite` ???
- **???????**? `node_modules` ??? git / Release

---

### Step 3 ? ?? MCP ???????????

???????

```bash
node -e "console.log(require('path').resolve('mcp-server/dist/index.js'))"
node -e "console.log(require('path').resolve('data'))"
```

??????????????????????**????????** `%APPDATA%\Cursor\mcp.json` ? `~/.cursor/mcp.json`?

```json
{
  "mcpServers": {
    "MC-AI-Coding-Assistant-Tool": {
      "command": "node",
      "args": ["<dist/index.js ????>"],
      "env": {
        "MC_SKILL_DATA": "<data ??????>"
      }
    }
  }
}
```

???`MC_SKILL_DATA` ???? **`data/` ????**??? `H:/MC_skill/data`????????

???????? `port_project` ???????

```json
"MC_SKILL_ALLOW_WRITE": "1",
"MC_SKILL_PROJECT_ROOT": "<????????>"
```

? C: ??????????????**????????**? `mklink /J`????????? + `MC_SKILL_DATA`?

---

### Step 4 ? ????? Cursor

??????????**?????????** Cursor???????? `MC-AI-Coding-Assistant-Tool`?? 31 ?????

---

### Step 5 ? ??????

```bash
cd mcp-server
set MC_SKILL_DATA=<data ????>
npm test
```

---

## ????

| ?? | ?? |
|------|------|
| Node ???? | ?? Node **22.5+**??? 18+? |
| ?????? / ???? | ?? `MC_SKILL_DATA` ???? `data/` |
| Yarn ???? | ?? `npm run build:yarn-sqlite` |
| ????? | ?? `MC_SKILL_ALLOW_WRITE=1` ????? `MC_SKILL_PROJECT_ROOT` |

---

## AI ????

- ?????????? `mcp.json`???????
- ??????????? junction / ?????
- ????????? `yarn-mappings.json`
