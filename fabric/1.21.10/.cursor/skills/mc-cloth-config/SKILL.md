---
name: mc-cloth-config
description: Fabric 1.21.10 mc-cloth-config。本档在 versions.json 无 slot、无可核坐标；核不到则 search_fabric_docs version=1.21.10，禁止输出。
platform: fabric
version: "1.21.10"
dependencies: []
mappings: yarn
---

# mc-cloth-config（Fabric 1.21.10）

本 Skill **尚未用本版文档核到可执行步骤**。本档在 `knowledge/libs/fabric-only/mc-cloth-config/versions.json` 里**无 slot**，故**不得给出任何坐标**（依赖串 / 版本号 / 类名 / 方法名）——核不到坐标就别写，禁止输出方法名与示例代码。

改口：`search_fabric_docs`（`version=1.21.10`）。核不到就停。

要签名：先 `query_loader_api`；无摘要则用户自备 jar 走 `ingest_loader_api`。
