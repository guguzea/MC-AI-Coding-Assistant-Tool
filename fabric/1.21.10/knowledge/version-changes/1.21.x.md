# Minecraft 1.21.8 → 1.21.10 版本变化（Fabric 1.21.10 档）

> 本文件 2026-09-11 新建（用户裁定「补 1.21.x 纪要」）。本档原 `gaps.knowledge` 句「本档语料无可核实主题页可支撑更多短文」措辞过强，已改：语料**有** `develop_porting_index.md`，只是正文 = 改构建脚本三步 + 站外链接。本文件**只用仓内语料与小映射表**，未取任何站外页。
>
> 证据面：`data/fabric_1.21.10/fabric-docs/1.21.10/`（processed 86 页 / raw 79 页）、基线 `data/fabric_1.21.8/`（raw 67 页）、两版 `mappings/yarn-*-tiny.gz`（`yarn-1.21.10+build.3` / `yarn-1.21.8+build.1`）。
>
> ⚠️ 与 1.21.8 档同为**跨空档**区间：官方口径写 1.21.8 → 1.21.10，中间 **1.21.9 本仓无档**（无规则树、无语料，见根 `AGENTS.md` fabric 空洞档清单），故本区间不写 API 名级断言。

## 1. 官方迁移口径

- 基线声明：CORPUS `data/fabric_1.21.10/fabric-docs/1.21.10/processed/develop_porting_index.md:13`（1.21.8 → 1.21.10）。
- 「Updating the Code」（`:31`）仍只给外链，NeoForge primer 两段在 `:43`（1.21.8→1.21.9）与 `:44`（1.21.9→1.21.10）。⇒ 中段那版的变化只能开外链看，本仓未取。

## 2. 构建脚本

CORPUS 同页 `:27-29`，与 1.21.4 / 1.21.8 档逐字相同的三条（`./gradlew wrapper --gradle-version latest` → 抬四件套版本 → 刷新或 `--refresh-dependencies`）。⇒ 「迁移官方指引在 1.21.4→1.21.10 区间没变」这件事本身就是结论：别假设新版给了新的构建步骤。

> 本仓不代跑 Gradle（人在环）。本档 `scaffold/` 无 wrapper 三件套 ⇒ 第 1 条在此模板上跑不了，见 `pack.meta.json` `scaffold.gaps`。本档 `scaffold/build.gradle` 的 `java.toolchain` = `JavaLanguageVersion.of(21)`。

## 3. 语料覆盖差：1.21.10 新出的专题入口（本区间最主要的可用信息）

raw 79 页 vs 1.21.8 的 67 页 = **仅 1.21.10 有 14 页**、仅 1.21.8 有 1 页（`develop_getting-started_index`）：

- **新文档族 `develop_porting_mappings_*`（3 页：`index` / `loom` / `ravel`）** —— 上游把「映射与 remap 工作流」升成一等专题，是本档最值得先读的入口；问 1.21.10 的 loom / mappings / ravel 一律 `search_fabric_docs version=1.21.10`，不要引用邻版。
- 其余新页：`develop_data-attachments`（本档 `pack.meta.json` 的 `coverage.attachments` 已登记其落点与「自称 experimental、未写 FQCN / 模块 id」的限制）· `develop_saved-data` · `develop_mixins_bytecode` · `develop_entities_attributes` · `develop_items_item-appearance` · `develop_items_spawn-egg` · `develop_blocks_transparency-and-tinting` · `develop_data-generation_item-models` · `develop_loom_classpath-groups` · `develop_loom_tasks`。
- 1.21.8 的 getting-started 分 IDE 页结构在本版继续存在（未回退）。

## 4. Yarn 名字实测（本区间的形状）

- CLASS 总数 9469 → **9953**（+484），但**没有新增任何已命名的顶层包**（`net/minecraft/<seg>/` 首段集合 1.21.10 与 1.21.8 相同，只差一个未命名条目 `net/minecraft/class_11541` 在 1.21.10 消失）。⇒ 与 1.21.4→1.21.8（新出 `dialog/` `storage/` `gametest/` 三包）不同，本区间的变化是**包内**的。
- 按前三段包名的计数差（只报数，不作解释）：`client +238` · `server +108` · `world +36` · `unused +27` · `entity +19` · `block +18` · `network −16` · `util +12` · `datafixer +9` · `text +8` · `resource +8` · `loot +5` · `data +4` · `component +3`。
- 点名核过、**没有**变化的：`net/minecraft/registry/`（128 vs 128，成员零差）、`net/minecraft/data/recipe/`（18 vs 18）、`net/minecraft/dialog/`（37 vs 37）、`net/minecraft/gametest/`（1 vs 1）、`net/minecraft/attachment/`（两版都 0 —— 附加数据不是 vanilla Yarn 包，见本档 `coverage.attachments`）。

复现（本仓根执行）：

```bash
u() { zcat data/fabric_$1/mappings/yarn-$2-tiny.gz | awk -F'\t' '$1=="CLASS"{print $NF}' | sort -u; }
u 1.21.10+build.3 | wc -l                                        # 9953
u 1.21.10+build.3 | cut -d/ -f-3 | sort -u > /tmp/a; u 1.21.8+build.1 | cut -d/ -f-3 | sort -u > /tmp/b; comm -13 /tmp/b /tmp/a   # 新增顶层包：无
u 1.21.10+build.3 | grep -c '^net/minecraft/registry/'           # 128
```

## 5. 本档仍未覆盖的

- 1.21.9 的中段变化本仓无档可查（见开头警告）。
- 本档 `knowledge/` 只有 `common/verified-api-1.21.10.md` + 本文件；`code-patterns/` 与 antipatterns 仍缺（薄档形态，见 `pack.meta.json` `gaps.code-patterns`）。
- 上面 §4 的包名计数差**没有**逐名展开（要把它们变成 API 结论，需要点名对照 primer，那是外网腿）。
