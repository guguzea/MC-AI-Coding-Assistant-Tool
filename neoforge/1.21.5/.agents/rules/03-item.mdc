# 03 — 物品（NeoForge 1.21.5）

来源：https://docs.neoforged.net/docs/1.21.5/items/

`DeferredRegister.Items` + `registerItem` / `registerSimpleItem` / `registerSimpleBlockItem`。文档示例返回 **`DeferredItem<Item>`**。`Item.Properties#setId` **必须设置**（`registerItem` 内部会 `ResourceKey.create(Registries.ITEM, registryName)`）。第三参仍是 `new Item.Properties()`，不是 unary operator。

官方列出的常用 Properties：`setId`、`overrideDescription`、`useBlockDescriptionPrefix`（BlockItem 应调用）、`stacksTo`、`durability`、`fireResistant`、`rarity`、`setNoCombineRepair`（不是更早档的 `setNoRepair`）、`jukeboxPlayable`、`food`。余量与冷却：`craftRemainder`、`usingConvertsTo`、`useCooldown`。工具/盔甲向：`enchantable`、`repairable`、`equippable` / `equippableUnswappable`。

创造栏：加入原版页用 `BuildCreativeModeTabContentsEvent`（模组总线、逻辑客户端），`event.getTabKey() == CreativeModeTabs.INGREDIENTS` 后 `event.accept(...)`。自建页注册 `CreativeModeTab.builder()` 的 `displayItems`。

1.21+ 物品数据优先 Data Components，不要把 1.12 NBT 当主存储。

禁止：`RegistryObject<Item>` 当 NeoForge 1.21.5 持有类型；禁止 Fabric `Registry.register` 冒充。
