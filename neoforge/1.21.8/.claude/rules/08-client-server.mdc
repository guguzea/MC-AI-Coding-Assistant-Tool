# 08 — 物理端（NeoForge 1.21.8）

来源：https://docs.neoforged.net/docs/1.21.8/concepts/sides/
> ⚠️ 离线数据缺口：本档 data/neoforge_1.21.8/ 未入库 concepts/sides 页，search_neoforge_docs 查不到；上述来源以线上版为准（禁止从邻版复制补索引）。

- 物理客户端 / 物理服务端 / 逻辑客户端 / 逻辑服务端 分开。
- 物理端：`FMLEnvironment.dist`。文档推荐 `@Mod(value = MODID, dist = Dist.CLIENT)` 客户端入口类。
- 逻辑端：`level.isClientSide()`。
- 禁止在服务端线程碰 `Minecraft.getInstance()`。
- 网络处理线程：1.21+ payload 默认主线程；重计算用 `HandlerThread.NETWORK` + `enqueueWork`。
