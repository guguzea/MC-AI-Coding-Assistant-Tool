---
name: mc-mixin
description: NeoForge 1.21.8 mc-mixin。类名只来自本档核实表与 search_neoforge_docs。
platform: neoforge
version: "1.21.8"
dependencies: []
mappings: mojmap
---

# mc-mixin（NeoForge 1.21.8）

Java 21。资源 id 类型：`ResourceLocation`。禁止从扁平 neoforge/.agents/skills 或邻档复制旧 API。

先 mixin_analyze；deep 需缓存 jar。

网络不要用 SimpleChannel。payload 事件：RegisterPayloadHandlersEvent。核不到则 search_neoforge_docs version=1.21.8，禁止输出。
