---
description: 04 — 实体（NeoForge 1.21.1）
---

# 04 — 实体（NeoForge 1.21.1）

来源：本档 `search_neoforge_docs` 的 `entities` 页**可查**（A-43 活体复核 2026-09-19：`get_neoforge_doc_full --id=entities --version=1.21.1` = `ok:true`；旧稿「没有独立页 / `DOC_NOT_FOUND`」是过期前提，已作废）。

实体 API 未核实。禁止输出 `DeferredRegister.Entities` 等具体签名。用 `search_neoforge_docs query=entities version=1.21.1`；若仍无页则停。不要用 1.21.8 实体页回填。

禁止：Forge 1.12 `EntityRegistry`；把邻档 entities 页改版本号当本档已核。
