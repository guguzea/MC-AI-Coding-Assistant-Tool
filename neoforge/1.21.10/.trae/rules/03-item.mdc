# 03 — 物品（NeoForge 1.21.10）

来源：https://docs.neoforged.net/docs/1.21.10/items/

`DeferredRegister.Items` + `registerItem` / `registerSimpleItem` / `registerSimpleBlockItem`。文档示例返回 **`DeferredItem<Item>`**。`Item.Properties#setId` **必须设置**（`registerItem` 内部会写入）。第三参：本档 **docs** 示例给 eager `new Item.Properties()`，同档 **scaffold / 官方 MDK** 用 unary（`scaffold/src/main/java/com/example/examplemod/ExampleMod.java:49/:54` 的 `p -> p.mapColor(...)` / `p -> p.food(...)`）⇒ 两套官方口径本身不一致，本档两者都接受并各自注明出处；unary 重载的 **docs** 支撑出现在 1.21.11 与 26.1 两档、本档 docs 未出现 ⇒ 不得写成「unary 是 26.1 的」，也不得说本档 docs 给了 unary。

官方列出的常用 Properties：`setId`、`overrideDescription`、`useBlockDescriptionPrefix`（BlockItem 应调用）、`stacksTo`、`durability`、`fireResistant`、`rarity`、`setNoCombineRepair`（不是 `setNoRepair`）、`jukeboxPlayable`、`food`。余量与冷却：`craftRemainder`、`usingConvertsTo`、`useCooldown`。工具/盔甲向：`enchantable`、`repairable`、`equippable` / `equippableUnswappable`。

创造栏：加入原版页用 `BuildCreativeModeTabContentsEvent`（模组总线、逻辑客户端），`event.getTabKey() == CreativeModeTabs.INGREDIENTS` 后 `event.accept(...)`。自建页注册 `CreativeModeTab.builder()` 的 `displayItems`。

1.21+ 物品数据优先 Data Components，不要把 1.12 NBT 当主存储。

禁止：`RegistryObject<Item>` 当 NeoForge 1.21.10 持有类型；禁止 Fabric `Registry.register` 冒充。
