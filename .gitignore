# Gradle
.gradle/
build/
!gradle/wrapper/gradle-wrapper.jar
gradle-*.zip

# IDE
.idea/
*.iml
*.ipr
*.iws
.vscode/
.omo/
*.swp
*.swo
.DS_Store
Thumbs.db
.zcode/
# 只忽略仓库根 Cursor IDE 目录（prompts / 本地草稿）。
# 禁止写成 `.cursor/`：那会排除 fabric/<ver>/.cursor 等平台包。
# session 只从各档 `.cursor/rules` 注入 00–10，漏提交则 clone 后 rules[] 为空。
/.cursor/
# MCP server
mcp-server/node_modules/
mcp-server/dist/
release/
*.tsbuildinfo
*.log
npm-debug.log*

# Local-only audit / scratch artefacts
mcp-server/data/loader-api-summaries/*-last.json
mcp-server/data/loader-api-summaries/extracted-classes.json
mcp-server/audit-*.txt
mcp-server/scripts/_temp/
mcp-server/_temp_localize_test/
agent-tools/
check-sources.txt
临时文件*.md

# Local jar scratch (do not commit)
temp/
temp/mod-jars/

# Mapping build leftovers / large Mojang downloads (re-fetched by extractor)
data/**/yarn-mappings.sqlite.tmp*
data/**/mappings/test-build*.sqlite
data/**/mappings/client.txt
data/**/mappings/mojang-client.txt
data/**/mappings/client-mappings-*.txt

# Java / Minecraft
*.class
# Keep mapping zips / yarn jars under data/ tracked; ignore elsewhere
*.jar
!data/**/*.jar
*.zip
!data/**/*.zip
run/
run-data/
src/generated/
eclipse/
out/

# Environment
.env
.env.local

mcp-server/_temp_tool_probe/
mcp-server/_sweep-findings.json
mcp-server/_audit-findings.json
openspec/
ralphy-spec/
# SQLite journals (never commit)
data/**/*.sqlite-journal
data/**/*-journal

# 测试残留（MCP 解包产物，勿入库；已跟踪的需 git rm -r --cached data/test_mcp_check）
data/test_mcp_check/

# 会话缓存 / 提交信息草稿（本地工作文件）
_tmp_session.json
.gitcommitmsg.txt

# 本地代码索引
.codegraph/

# 一次性扫描/草稿文件（曾入库的已随 sweep31 删除，防复发）
scripts/_scan-*.mjs
scripts/_temp/*.log.err
临时文件-*.md
HTTP

