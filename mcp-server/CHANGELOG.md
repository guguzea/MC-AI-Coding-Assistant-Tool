# mcp-server changelog

## Unreleased（2026-09 全仓库审查修复轮；工作树，未发版）

- **未做③ 落地：Forge 1.17+ 的 SRG 成员层入库（2026-09-26）** —— 上面 09-25 那条普查的结论**当天即被本轮推翻**，见该条尾部的更正。三件事：
  1. **新来源**：`mcp-server/scripts/ingest-forge-srg.mjs`（新）从本机 ForgeGradle 缓存 `~/.gradle/caches/forge_gradle/minecraft_user_repo/de/oceanlabs/mcp/mcp_config/<v>-<ts>/srg_to_*.tsrg` 取 MCPConfig 的 `tsrg2 left right`，削减（丢参数层 `\t\t…` / 修饰行 `static|final` / `<init>`·`<clinit>` / 一切非 `m_/f_` 形状的成员行）后写 `data/forge_<v>/mappings/srg_to_official-<v>.tsrg` + 同档 `mcp_config-<v>-<ts>.provenance.json`（上游坐标、`.input` 里的 `mapping=`/`mcp=` commit SHA、源与派生件 sha256/bytes、削减计数、`consumedBy`、许可姿态）。默认 **DRYRUN**（`wantWrite()`），任一档取不到源 ⇒ 非零退出。**实测 6/6 档**（1.16.5 / 1.17.1 / 1.18.2 / 1.19.4 / 1.20.1 / 1.20.4）：源 46,866,384 B → 派生 32,273,362 B（削减率 31.1%）· 类 40,513 · 方法 265,564 · 字段 171,715；单档 1.20.1 与上游逐桶对上（`m_` 一级行 48,575 = 入库方法数；字段 32,079 = 2 段 31,003 + 3 段 1,076；`<init>`+`<clinit>` 9,637 与 `toString`/`equals` 等 6,321 全部未采纳）。**1.16.5 是同一条路径的第二种年代形状**：它的 `srg_to_official` 里 SRG 位置用的是老形 `func_\d+_x` / `field_\d+_x`（实测 35,526 + 19,841 行，且该档此前**连一个成员库都没有**）⇒ 两个解析器的成员正则都从「只认 `[mf]_\d+_`」放宽到四种形状（`access-lines.ts` 的 `SRG_SHAPE_RE` 本来就两套都认，所以消费面零改动）。口径：方法 35,526 比一条按 `\w+` 数的普查多 1 —— 多的那个是含 `$` 的合成成员 `func_210297_lambda$static$0`（现算复核，非推断）。
  2. **新档面**：`_lib/build-yarn-sqlite.mjs` 加候选 `srg_to_(official|parchment|snapshot)*.tsrg(.gz)` 与 `kind === "srg-to-official"` 分支（新 `_lib/import-srg-to-official.mjs`：**named 列 = SRG 名、official 列 = mojmap 可读名、intermediary 留空**，另灌 `searge_methods`/`searge_fields`），`mappingEra` 新值 **`mcp-config-srg`**。实测建出 6 份库 = 272.4 MB（33.6 / 40.8 / 42.8 / 50.2 / 51.0 / 54.0 MB；仓内已跟踪的 22 份同类库合计 540.2 MB，同一量级）。
  3. **顺带修掉一个既有真缺陷**（本轮 E2E 才暴露）：`convert_mapping` 在「同一 MC 版本 Fabric 与 Forge 两侧都有库」时**从来不看加载器**，谁先被扫到就用谁 —— 新库一进来就会让 Forge 查询拿到 Yarn 层、或让 Fabric 查询拿到 SRG 层。修法：`MappingQuery.mappingDbPreference` → `resolveMappingDbPath/getMappingEra/getSchemaVersion/getCsvDb/resolveOwnerClassNamed/lookupYarnClass(*, prefer)` 全链 threading，Forge 查询由 `preferMappingDb(loader)` 在 `convertMappingEx` 里**归一化一次**（`access-lines` 与批量腿共用）；归属判据按 **era** 落，不按「有没有平台」—— `mcp-config-srg` 只在显式要 Forge 时才算数（`resolveCsvMappingDbPath`），因为 1.14.4/1.15.2 的 `mcp-csv` 并存回答 MCP 是**既有契约**（`test-core:1280`），而 1.20.1 fabric 侧 `to=mcp` 必须继续拒答（`test-core:653`）。我第一版收紧成「prefer 必须是 forge 才启用该库」矫枉过正，把 :1280 砍红了才换成按 era 判。
  **口径变化**：`convert_mapping(accessLines=true, platform=forge)` 在 1.16.5 / 1.17.1 / 1.18.2 / 1.19.4 / 1.20.1 / 1.20.4 六档从此出**可粘贴的完整 AT 行**（`public net.minecraft.Util m_137477_(Ljava/lang/String;)Ljava/util/concurrent/ExecutorService; #makeExecutor`，`complete:true` + `selfCheckOk:true`），不再只能 `<TODO:SRG名>`。
  **门与投毒**：新门 `mcp-server/scripts/assert-forge-srg-ingest.mjs`（挂 `test-scripts.mjs` 的 REAL_RUN + SELFTEST 两条数组，实测真跑 121 ms / selftest 43 ms，零联网、只写 OS tmpdir）：夹具 4 类 / 3 方法行 / 4 字段行（含「同名跨 owner」与「同 owner 双形状」两种重复），判「**上游件与入库件逐行同答**」（reducer 与 importer 是两个解析器，比字节会放过形状漂移）+ era/形状/searge/intermediary + 消费面成行 + 在盘三方对账（派生件 sha256 ↔ provenance ↔ 库 meta）。**首跑就自己红了一次**：我把「入库件也必须丢 1 行参数」当等式钉 ⇒ `[COUNT] 入库件 丢参数行: 实得 0，期望 1` 四条全红 —— 削减件本就没有该丢的行了，改成「上游件按 EXPECT、入库件四项必须为 0」（非零即 reducer 漏行）。selftest **12 记投毒**（11 记改观测值 + **1 记真输入投毒**：抽掉上游行的可读名 ⇒ 真解析器必须不采纳）全红在该当判据上，正对照绿。`test-core` 侧：`s2Todo`（原「1.20.1 无 SRG 库 ⇒ 必须留 TODO」）**翻成正证** `s2Srg1201`（等值断言那行 AT 文本）+ 两条反证（真查无此名 / forge 1.21.1 无库档仍留 TODO）+ 归属反证 `srgLeak201`（fabric 侧 1.20.1 `to=parchment` 必须仍 `YARN_TINY_NO_MCP_LAYER`）+ `resolveCsvMappingDbPath(v,null)==null` 与 `("v","forge")` 为字符串那对。**生产侧投毒 2/2 红**（只动 `dist/mappings/yarn-sqlite.js`，跑完逐字节还原并回读 `true`）：M1 删整条 era 判据 ⇒ 红在「无平台查询不得选中」+ fabric 拒答失守；M2 把 `prefer !== "forge"` 反向成 `!== "fabric"` ⇒ 红在「显式 forge 必须选中该档库」。**第三记生产侧投毒（两种年代形状的宽口径）**：把 `_lib/import-srg-to-official.mjs` 与 `ingest-forge-srg.mjs` 两份成员正则**同时**退回窄口径（只认 `[mf]_\d+_`）⇒ 夹具门 rc=1 红 20 条（含 `[COUNT] 上游件 方法: 实得 0，期望 4`、`[MEMBERS] 成员集合不符 缺 9`、`[AT-LINE] 完整行` 三条），且 `ingest --versions=1.16.5` 当场印 `方法 0 字段 0 · 其它 59,840`（= 整档静默入库空库，正是这条投毒要证的后果）；跑完两个源文件按快照逐字节还原并回读 `true`。**第 8 步链又替我抓出第二个真缺陷（meta 虚报）**：G4 `assert-index-consistency` 先报 `实扫出现未登记 yarn 库 forge_1.17.1…1.20.4`，摊开行数发现 `searge_methods` 只有 33,222 行而 `methods` 有 48,575 行 —— MCPConfig 把同一个 `m_<id>_` 在多个 owner 下各列一行（例：`m_5574_ acquire` 同属 `Library$1` / `Library$ChannelPool` / `Library$CountingChannelPool`，可读名与描述符逐字相同 ⇒ 3,059 个重复键多出 15,353 行；字段侧 1,076 个 `f_<id>_` 是「裸形 + 带 `()` 描述符形」两行且**同 owner**），而 `searge_*` 以 `searge` 为主键 ⇒ 按设计塌行（**同键不同可读名 = 0 例** ⇒ 塌行不丢信息）。但我的 build 分支把源行数照抄进 `meta.seargeMethodCount` ⇒ 正是本仓 `DEBT_MAPPING_COUNT` 那条债的形状（「读侧直接信 meta ⇒ 覆盖数虚报」）。修：该分支改为**数表内实数**写 meta + 一行 `srgNameCollisionNote`，六档库随之重建（复核：六档 `meta.searge*Count == 表内行数` 全 OK，塌后值 26,870/28,190/32,738/33,222/34,990）；门侧夹具扩成 **4 类 / 3 方法行 / 4 字段行 / searge 2+3 行**（故意含上述两种重复），新增两腿 `meta.*Count == 表内行数` + 第 12 记投毒「照抄源行数必红」；`G4` 的 `LEDGER_YARN` 按 `MC_SKILL_INDEX_RELEDGER=1` 重算后登记六档（逐字段机械回比 12 个比对字段全等；**手抄错过一次**：`forge_1.20.4` 的 `officialFields` 被我抄成 searge 值 32,991，G4 立刻红在该字段 ⇒ 抄完必须回比，别信眼睛）。⚠️ 该台账因此成为**又一个消费者**：库不入库（`CONTRIBUTING.md` `L67` 选 ②）时这五行必须一并撤下。另记一次**探针假阴性**：我先用 `split("\t")` 去数上游原件的 `m_` 行 ⇒ 得 0 行（上游是空格分隔），差点据此写下「上游无重复」；重复性结论一律改由**入库削减件**（制表符形）实测，上游侧只沿用先前按 `/\s+/` 的形状普查数。
  **待拍板**：那 6 份库（272.4 MB）+ 6 份派生 `.tsrg`（32,273,362 B）+ 6 份 provenance（14,007 B）共 **18 件 / 303.2 MB 现在都在盘但全部未跟踪**；`test-core` 的 `s2Srg1201` / `srgLeak201` / 归属对是**硬腿**（不入库 ⇒ 门红，不会静默 skip）。要么 `git add`（与既有 540.2 MB 同类库同量级、且 `data/forge_1.16.5/mappings/obf_to_srg.tsrg` 早有 MCPConfig 派生件 tracked 的先例），要么本轮把这三条腿挪进上面那道夹具门 —— 请拍一个。

- **A3 残面① + B7 + A6 尾项改判（2026-09-25）**：**A3** `search_docs(platform=quilt)` 进 GENRE 门判据面 —— 该面是**只收窄面**（`QUILT_SEARCH_DEFAULT_LIMIT == QUILT_SEARCH_LIMIT_MAX == 20`），照搬「放宽必须变多」必假红 ⇒ 新增反向腿 `checkNarrowOnlyFaceWindow`（默认无 limitWindow / limit=1 必真收窄 / 上界窗口与默认同序 / 超面界必被 clamp+披露 / 行数不超池），selftest +7 失守 +2 对照（含「照搬 widened 腿恒假红」的对照）；**真跑探出真形状**：共用 `searchDocsSchema` 的 limit 上界是 30，面界 20 靠运行时 clamp 兜底 ⇒ schema 腿按 `schemaMaxKey=SEARCH_DOCS_LIMIT_MAX` 分流（拿面界判 schema 也是假红）。**其余 9 面（community/loader_api/related×5/query_registry/query_upstream_releases）登记在门头注记**：逐面语义待核（query_upstream_releases 是 total 恒全量的 top-N 截断族，照搬 min(请求,池) 必假红），未管 ≠ 已覆盖。**B7** `FLUIDTYPES` 形收编进 `assert-rule-ban-vs-example.mjs` `PAIRS`（`forge-1182-fluidtypes-ban`，禁令锚 = mc-fluid SKILL:22，判据源 = L6 已闭环裁定；表 4→5 对 / 被扫 16→19 件，真跑 + selftest 全绿）。**A6** 改判**假阴性闭环**：REPORT.md 四条事实实际全部在盘（`:455-458`，C1 写作「C1 **三态**判定链已落地」⇒ 逐字扫「C1 判定链」0 命中是扫描口径问题），且其声称的两条探针实测在 `test-cli.mjs:851/:861`。**A10/A11 未动**（覆盖面欠账大头，需排期分批）。残面登记从门头文案提升为 `CONTRIBUTING.md` **L56**（8 面两桶：甲桶 community/loader_api 挂前先拍板内联 `.max(50)` 提常量；乙桶 related×5 + 两个 query_* 的 limit 是截断/分页语义，禁止硬挂「超池必披露」）；`docs/mcmap-linkie-absorption.md` 两处陈旧更正（① 行「7 个源 · 7 条端点」→ **10 个源** + 计数唯一真值源指向 ② 行；③ 行行号锚 `:1070` 改内容锚 + 现位 `:1146`，并行会话当天两连漂）。另开 `CONTRIBUTING.md` **L57**：leg2 依赖方向文字化（对照产物三层落点 `--pairs-dir` → `$MC_SKILL_CACHE` → `os.tmpdir()` 一次性目录 ⇒ 新终端等价 skipped 而 rc=0；生产者 `build-yarn-mojmap-pairs.mjs` 要联网逐档现拉 client.txt、产物从不写 `data/**`；⚠️ 生产者本体仍未纳管 ⇒ 干净 clone 连重跑入口都不存在，与 L10 互引）。**同日活体演示**：tmpdir pairs 被清后 `assert-cross-layer-names` 退化运行（A/B 130→0 假绿 + C 33→58 假红 +25，红点文件全部 mtime ≤ 建门时点、forge 树 13:35 后零写入 ⇒ 排除「有人改树」），重跑生产者后 C=33 恢复、A/B 基线重钉 **135**（门注释带三条依据），运行环境须设 `MC_SKILL_CACHE=D:\mc-skill-temp\cache`。**同日第三遍（C 腿清零 + B 腿真欠账）**：C 腿 33 行处置——真欠账 24 行文本修复（KeyboardListener→KeyboardHandler×5、BiomeAmbience→BiomeSpecialEffects×1（1.16 Forge 教程原样搬进 1.17.1 档）、EntityDataManager 并列行改只留本档正名×3、IRecipe→Recipe×3、SetCount→SetItemCountFunction×2、RandomValueRange→NumberProvider×5、LootEntry→LootPoolEntryContainer×5；含糊注「以本版 mappings 为准」撤下换实名）+ Forge 跨代同名类 9 行（ItemModelProvider×5/BlockTagsProvider×4）进 ALLOW（tsrg 快照含 forge 类行所致门误报）；B 腿真欠账 10 行（KeyBinding→KeyMapping×8、Structure→StructureFeature×2，1.18.2 的 Structure.StructureSettings 嵌套类经 client.txt 证不存在 ⇒ 改 StructureFeature<Config>+Codec）；全部替换名经 5 版 client.txt 现验（StructureFeature@1.19.4+=N 印证 1.19+ 重构，04-world 版本注同步改 1.19+）；BASELINE_C 33→**0**、A/B 135→**125**（两钉依据在门注释）；5 档镜像 sync。另：leg2 补 `mcpClassNameProtect` 的 **1.14.4/1.15.2 → 1.16.5 tsrg 回退**（pairs 重拉 13/13 档把 1.14.4 补进判据面后，yarn≈MCP 同形名 10 件假红现形）⇒ leg2 判 483/冲突 0/自证 37。⚠️ 链现红于并行会话半成品门 `assert-lib-coord-snapshot.mjs`（SNAPSHOT_MISSING + selftest 7/16 不符）——按 L34/L55 不抢笔，待并行车道收口。**同日 00:3x 交接单执行（temp/audit-20260921/HANDOFF-fixer-20260926.md）**：② `--require-pairs` / `MC_SKILL_REQUIRE_PAIRS=1` 落地（照抄 cross-layer 模板：默认关、产物缺席 ⇒ `PAIRS_MISSING` 响亮红并念再生命令；selftest 37→**39** 加两记端到端「产物在不误伤 / 缺席响亮红」；三态实测 rc=0/0/1——**pairs 入库后 CI 已能判，开关定位改为维护者复核基线的显式断言**，交接单的「CI 永不可开」前提被 pairs 入库推翻已注记）；① 散文面 KeyBinding **131→0**（桶 B 批修：rules/09×4 + AGENTS.md×5 + CLAUDE.md×5 + knowledge/antipatterns/events.md×5 + 05-events 标题×4 + 08-client-server 标题×1，全部改「按键绑定（KeyMapping）」；桶 D 04-world 1.17.1 的 `GenerationStub`/`GenerationContext`/`NoFeatureConfiguration` 样例经 client.txt 证 4 档零命中 ⇒ **撤块留 TODO(未核实)**；桶 A `registerKeyBinding` 出处句已在册保留）；5 档镜像 sync ×2；scanner 验收对账 131→**0**。⚠️ 基线三钉完成（A/B 130→135→125→**110**、C 33→**0**、D 109→**97**、E 2→**7**——E 含并行车道 5 条 provenance 待补件，补齐后必须降回）；期间并行会话交替编辑同门（D=97 与 Phase 豁免系他们所钉，互补无冲突）。**全链 rc=0**（G4 一次并发写窗口瞬红复跑即绿，L22 先例）；并行会话同窗口收口了 `assert-lib-coord-snapshot`（快照落盘 + 自证修好 + 接线，真跑 39/selftest 30）。

- **第二批 · S 批（2026-09-25，接「S1 完成后继续 S2–S6」）** —— 以下 8 条与上面的 09:4x–17:0x 改动不同批，故分隔（上面那条「B7 被扫 16→19 件」经本轮 #21 门自报核对 = **19 件正确**，与 `assert-rule-java-shapes` 的 `FLOOR_SCANNED` 519 是两个不同分母，别混读）。
- **S2 + S3（2026-09-25，接「S1 完成后继续 S2–S6」）**：把 Linkie 的两个强项接进 `convert_mapping`，**工具面 82 不变**。新文件 `src/mappings/access-lines.ts`（条目行生成器）与 `src/mappings/convert-extras.ts`（`convertMappingEx` = 唯一入口；handler 只调用 + 序列化，所以 `test-core` 测的是同一条路径不是副本）。新参数：`memberName` 支持逗号/分号/换行批量（`BATCH_LIMIT=50`，超限 `action.code:INVALID_INPUT` 且不跑 51 次；单个名字时输出逐键不变）、`accessLines`、`platform`（8 值枚举，只有 `accessLines` 消费）、`access`、`finalOp`。**名字层分叉全部落到可核对出处**：Forge AT 成员行必须 SRG 名（`data/forge_1.20.1/forge-docs/1.20.1/processed/advanced_accesstransformers.md:55`，示例 :114/:118）；NeoForge AT 成员行用可读名 + 粘连描述符（`data/neoforge_1.21.1/…/advanced_accesstransformers.md:127,:137,:191,:197`）—— 同一页同一节，两套**不可混用**，故 `platform` 缺省时两条都给并各自标注；Fabric/Quilt AW 条目「名须与工程当前映射层一致」（`data/fabric_1.21.11/fabric-docs/1.21.11/processed/develop_class-tweakers_access-widening.md:82`，格式 :93/:105/:117），头的 namespace 随 `to` 层（named / intermediary / official），转换未命中时回落 named 并披露「层未确认」。取不到 SRG 名或缺描述符 ⇒ `complete:false` + 行内 `<TODO…>` + `selfCheckOk:null`（**含 TODO 的行不自检**，否则自检给坏行背书）；`complete:true` 的行回灌 `validate_at`/`validate_aw` 用的同一解析器。实测输出：AW 类行 `accessible    class    net/minecraft/class_1309`（1.21.1 yarn→intermediary，头 `accessWidener v2 intermediary`）；Forge AT 成员行 `public net.minecraft.advancements.Advancement func_192067_g()Lnet/minecraft/util/ResourceLocation;`（1.12.2 forge-srg 库真名 + 库内描述符）；NeoForge 行 `public net.minecraft.Util makeExecutor(Ljava/lang/String;)Ljava/util/concurrent/ExecutorService;`；1.20.1 无 SRG 库 ⇒ `… <TODO:SRG名>(Ljava/lang/String;)…ExecutorService;` + 「不拿可读名顶替」说明。
- **顺带修真缺陷（S2 前置探测发现，validate_aw 实测）**：`parseAccessWidener` 只认 `accessWidener v1|v2` 头，把**官方 1.21.11 参考工程的第一行** `classTweaker v1 named`（`data/fabric_1.21.11/reference/1.21.11/src/main/resources/example-mod.classtweaker:1`；可用性条件见 `develop_class-tweakers_index.md:16,26` = Fabric Loader 0.18.0+ / Loom 1.12+）判成红：修前同一条输入 5 条 errors + `valid:false` + `checkedMembers:0`，修后 `valid:true` + `checkedMembers:2`。解析器另加 `spec` 返回键；`transitive` 的「仅 v2 支持」警告收窄为**只对 `accessWidener v1` 念**（classTweaker v1 的 `transitive-*` 是语料列出的合法形式）。
- **本轮映射库覆盖普查（`node --no-warnings` 直查各档 sqlite，非引用旧台账）**：有库 = fabric **13 档**（`26.1.2` NO FILE；era 全 `yarn-tiny`，methods 21 982–49 730 行）+ forge **7 档**（1.7.10/1.8.9/1.9.4/1.10.2/1.11.2/1.12.2 = `forge-srg`，1.13.2 = `tsrg`）+ forge 1.14.4/1.15.2 = `mcp-csv` 且 **methods/fields 三表 0 行**（与 AGENTS 那条一致）；**NO FILE** = forge `1.16.5/1.17.1/1.18.2/1.19.4/1.20.1/1.20.4`、全部 `neoforge_*`（9 档）、全部 `quilt_*`（6 档）。⇒ 1.17+ 的 `m_/f_` 形 SRG 名本仓**任何一档库里都没有**，Forge AT 成员行在 1.16.5–1.20.x 只能出 `<TODO:SRG名>`；已落新残差（见未做清单）。**⚠️ 2026-09-26 更正：该结论已被本轮推翻** —— 六档（1.16.5/1.17.1/1.18.2/1.19.4/1.20.1/1.20.4）现各有 `mappingEra=mcp-config-srg` 的库，Forge AT 成员行出实名；那条普查本身数的是**当时**的盘（`1.16.5` 仍无库，`neoforge_*`/`quilt_*` 仍无），别整句删。
- **钉子与投毒**：`test-core.mjs` 新增 32 条断言行、11 个场景（本轮「自审补丁」条又 +5 ⇒ 该块现测 **37**，见下方该条）（AW 类行 / Forge 完整 AT 行 / `<TODO>` 三件套 / NeoForge 分叉 / bedrock 拒绝 / 不传参时形状不变 / 批量命中数与 missing 点名 / 上限拒绝 / classTweaker 头 + `spec` + transitive 收窄 / 两条反向对照 `accessWidener v1` 警告仍在、`v3` 头仍红）。**五腿逐条投毒各红在该当原因**（P1 摘 `<TODO:SRG名>` 兜底 → 红在「缺 SRG 名必须留 TODO」；P2 头不跟层 → 红在「namespace 必须跟着 to 层走」；P3 解析器退回只认 accessWidener → 红在「classTweaker v1 头必须能解析」；P4 `if (false)` 废掉上限 → 红在「超上限必须报错」；P5 摘掉可读名判断 → 复现真 bug：行变成 `… ResourceLocation; #h`，红在 AT 行等值断言）；**P6（自审补丁）**把 `SRG_SHAPE_RE` 放回旧松版（尾段允许 `[mf]_\d+_[A-Za-z0-9_$]*`）→ 红在「`m_5_foo` 不是语料里的 SRG 形状，不得当成合法名写进行里」。投毒只动 `dist/`，跑完按备份还原并 `npm run build` 重编，三处锚点回读 = 1/1/8。`test-core.mjs` rc=0。
- **S4′（2026-09-25，同批：接「S1 完成后继续 S2–S6」）**：`query_upstream_releases` 的分档 TTL 磁盘缓存 —— 吸收 mcmap 的 tiered-TTL 磁盘缓存思路，不引入它的库。新文件 `src/upstream/cache.ts`（`UPSTREAM_TTL_MS` 档位表 + `upstreamCacheKey` + `read/writeUpstreamCache` + `ageUpstreamCacheEntry` 供门造到期态）；`src/upstream/releases.ts` 的 `queryUpstreamReleases` 变成 wrapper（原逻辑改名 `queryUpstreamLive`，一行未动），`UpstreamQueryResult` 加 `cache` 位，工具 schema 加 `refresh`，**该工具的 `outputSchema` 同步声明 `cache`**（它是全仓唯一带 outputSchema 的工具，漏声明就是合同过期）。**三条判据写进代码注释与文档**：① 只缓存 `ok:true`（`ok:false` 是「没查到」= TLS/代理/HTML 壳/非 2xx，固化下去会让一次抖动冒充几小时事实）；② 「有」6 h / 「没有」2 h 分档（证否会被新版本推翻，**禁止同档**）；③ 缓存根只许 `$MC_SKILL_CACHE/upstream-cache/`，`isResolvedInside(repoRoot, dir)` 为真 ⇒ 拒写（`data/**` 是上游逐字语料面）。键覆盖 `source`+`minecraftVersion`+`slug`+`limit`。端到端实测（真打上游，缓存根指临时目录，`node --no-warnings` + CLI dist）：第 1 发 `cache{hit:false,tier:available,ttlMs:21600000,wrote:true}` + `total:253`（fabric-loader@1.21.1）；第 2 发 `hit:true, ageMs:985`，`releases` 逐条相同；`refresh=true` ⇒ 回源并重写；不存在的版本（fabric-yarn@9.9.9）与 modrinth 无此 slug 都落 `tier:absent, ttlMs:7200000`。门不联网：`test-core.mjs` 只测缓存层 24 条断言行（**本轮「自审补丁」+11 ⇒ 现测 35**：原子写 5 条 / rename 失败清 tmp 3 条 / 半截文件读法 3 条）（键差异 / 档位 / 读写往返 / 到期必 miss 且念原因 / `ok:false` 拒写且不留半截文件 / 仓库内根拒写 / 开关关档）+ **一条差分对照**（仓库外合法根必须 `rejected:null`，否则「守卫」其实是恒拒、上面那条 `ok()` 毫无意义）。投毒 **4/4 各红在该当原因**（R1 「没查到」当可缓存 → 红在档位判定；R2 `absent` 改 12 h → 红在「必须比有短」；R3 `limit` 摘出键 → 红在「limit 必须进键」；R4 TTL 乘 1000 → 红在「超 TTL 还命中」），**自审补丁再加三腿投毒 ⇒ 现 7/7**（B 撤掉 tmp+rename 改直写 → 红在「birthtimeMs 未前移」；C 让 `readUpstreamCache` 的 catch 返回载荷 → 红在「半截 JSON 被当命中」；D 让 rename 失败不清 tmp → 红在「必须清掉自己的 tmp」），跑完按备份还原（四锚点回读 1/1/1/1，另 `dist/upstream/cache.js` 三处 `writeFileSync` 回读 = 行 100 写 tmp / 行 126 只服务 `ageUpstreamCacheEntry`）。R5（仓库内守卫）**没用 dist 投毒**：那条腿判的是 `process.env.MC_SKILL_CACHE` 与 `resolveRepoRoot()` 的关系，注入法证不到它 ⇒ 改成上面那条差分对照，别把「没投毒」写成「已投毒」。

- **S6-②（2026-09-25，同批）**：CLI 的 schema 层拒绝补机读位 —— `src/cli.ts` 新增导出 `zodIssueToFieldError()` + `ValidationCliError` 带 `fieldErrors`，`errorKind:"validation"` 的信封多一个 `fieldErrors[]`（`{field,code,message,expected?,received?,enumValues?}`）；**缺必填在 zod 里是 `invalid_type` + `received:"undefined"`，必须折成 `MISSING_REQUIRED`**（否则消费方还得解析中文串），枚举违规带 `enumValues`。散文 `error` 串一字未改（人和旧消费者照旧）。实测 `generate_datagen --platform=forge --version=1.20.1 --providerType=nope` → 3 条 `{providerType:INVALID_ENUM_VALUE(9 值域), modId:MISSING_REQUIRED, targetName:MISSING_REQUIRED}`。钉子 = `test-cli.mjs` 新增 s6-field-errors 腿（含「enumValues 必须念出来」与「散文串不得被挤掉」两条），**两腿投毒各红在该当处**（Q1 摘 `envelope.fieldErrors` 赋值 → 红在「validation 信封必须带 fieldErrors[]」；Q2 把 `received === "undefined"` 改成 `"never"` → 红在「缺必填必须折成 MISSING_REQUIRED」），跑完按备份还原（回读 1/1）。根 `AGENTS.md` §工具边界 加一条「CLI 的 schema 拒绝已机读」，并注明它与 A1 的 `action.code`/`resultKind` 是两套面。
- **自审补丁（2026-09-25，62/62 收口之后）：把我自己交给外部审稿地图的两个 P0 先修掉**。① `src/upstream/cache.ts` 的写盘**原来没有原子性**（直 `writeFileSync(file,…)`）：并发会话同打一个上游、或 OneDrive `-4094` 抖动打断写入，会留下半截 JSON，而读侧 `catch` 只能保证「不拿坏数据当事实」、不能保证缓存还在 ⇒ 改 `writeFileSync(tmp)` + `renameSync(tmp, file)`，失败路径 `rmSync` 清自己的 tmp（`ageUpstreamCacheEntry` 仍直写：那是门造到期态的助手，不是缓存路径）。② `src/mappings/access-lines.ts` 的 `SRG_SHAPE_RE` 尾段**过松**（`[mf]_\d+_[A-Za-z0-9_$]*` 会把 `m_5_foo` 这类近失名判成合法 SRG 写进 AT 行）⇒ 收成 `^(?:func_\d+_[A-Za-z0-9_$]*|field_\d+_[A-Za-z0-9_$]*|[mf]_\d+_)$`，注释点名出处形状只有一种（语料里 `m_<id>_` 恒以 `_` 结尾、无任何带后缀的实例）。**两条新判据不是装饰**：原子写这条找到本机可观测的 discriminator（探针 `D:/mc-skill-temp/s2/probe-birthtime.mjs`：原地覆盖 `birthtimeMs` 不变、rename 进来的新文件前移），腿断言「同键第二次写入 birthtime 必须前移 + 内容必须真换新 + 键路径同一」，并补一条**失败路径夹具**（把最终名占成目录 ⇒ `renameSync` 必败 ⇒ 须 `wrote:false` + 念「写盘失败」+ 不留 `.tmp-`）——没有这个夹具，先前那条「目录里没有 `.tmp-`」只在进程被杀时才可能红，属装饰。投毒 **4/4 各红在该当原因**（A=松回 SRG 形状 → 红在近失名；B=撤原子写 → 红在 birthtime；C=坏文件当命中 → 红在「坏数据卖成事实」；D=失败不清 tmp → 红在残留检查），脚本 `D:/mc-skill-temp/s2/poison-selffix.mjs`，跑完从 `poisonbak/*.selffix.bak` 还原。**顺带更正本文件一条自相矛盾**：「未做（本轮）」⑤ 把 S4′ 写成「本轮未开工」，而它上面就是 S4′ 完成条 —— 该行现在只留 S5′。
- **#55（2026-09-25，收口后第二批）：`validate_aw` / 解析器补齐 class tweaker 五族指令 + `classTweaker v2` 头**（任务书原话只点 `inject-interface`，实测同族共 **3 处**）。判据源全部是**上游逐字**，不是本工具自述：`data/fabric_26.1.2/reference/26.1.2/src/main/resources/example-mod.classtweaker:1` 头就是 `classTweaker v2 official`（修前整份文件 6 处判红），该文件 28/32/39/43/47 行给出 `inject-interface`（含带泛型 `…GenericInterface<+Ljava/lang/String;[Ljava/lang/Boolean;>`）与三条 `extend-enum` 实例；语法行 = `develop_class-tweakers_interface-injection.md:71`（内部名 :74）、`develop_class-tweakers_enum-extension.md:92`（内部名 :95，**且 :87 明写「把头设成 v2」+ Loom 1.16+**）；`transitive-` 五族 = `index.md:48,53,56`；「元素任意空白分隔含 tab」= `index.md:39-40`。改动：`src/mixin/access-widener.ts` 头正则 `classTweaker v(\d+)`（认不出的号只披露不判红，因 `index.md:34` 明说上游会长版本）、新增两操作数指令分支（arity / 泛型尖括号形状 / `extend-enum` 需 v2 才放行 / 内部名含点号只 warn）、`AccessWidenerEntry` 加 `operand` 并把 `type` 联合拓宽到五族、**冲突分组键补 `operand`**（同目标注入两个接口是合法的，修前会被念「重复声明」）、`splitWidenerFiles` 的头继承改成两种头都认（修前 classTweaker 多段文件从第二段起「缺 header」——同族第二处）。**未做的检查**：不校验「被注入接口的方法必须全是 default」（那是用户自己的类，不在客户端 jar 里，出处 `interface-injection.md:38`），也不泛型签名内部结构（只校尖括号配对，签名格式表在 :82-95，本工具不重造第二真值）。钉子 = `test-core.mjs` #55 块 **25 条断言行**（含「上游参考工程整份文件必须 0 error、9 条目、按族计数 4/2/3」+ 三条反向对照：`frobnicate` 仍红、`accessWidener v1` 的 transitive 警告仍在、`accessWidener v3` 头仍红）+ `test-deep-mixin.mjs` **8 条**（走真 jar 索引：目标类不存在必红 = owner 那腿真跑、两接口同目标不报重复、同接口两遍仍报重复、`extend-enum` 的常量名不被当成员去 jar 里查、`checkedMembers` 不虚增）。**投毒 7/7 各红在该当原因**（E1 摘掉两族指令 / E2 头退回只认 v1 / E3 撤 `extend-enum` 的 v2 门槛 / E4 撤分组键的 `operand` / E5 撤泛型形状检查 / E6 撤 `splitWidenerFiles` 的 classTweaker 头继承 / E7 只放进 `frobnicate` 一个未知名字 → 红在「反证：放宽判据不得把未知指令放进门」）。⚠️ **E7 第一版是废投毒**：用 `/^\w+$/` 过宽，连 `class|method|field` 关键字一起吃掉，结果红在更早的 S2 selfCheck 腿（`生成的行必须能过自家 AW 解析器`），证的不是我要证的那条 ⇒ 换成「只多收一个名字」才落到目标腿。**教训：投毒也要一次只改一处语义，宽投毒 = 假证明。**
- **B2 + B3 + AGENTS 口径（2026-09-25 23:0x，接用户「B2 改；B3 改；与 AGENTS.md 冲突面放开」）**。
  - **B2（读侧默认落点）**：派生对照表已入库 `data/_yarn-mojmap-pairs/`，但两个消费者仍按「`$MC_SKILL_CACHE` → tmpdir」解析 ⇒ 那条裁定只是意图。`assert-skill-mappings-key.mjs` 的 `PAIRS_DIR` 改为 **仓库副本 → `$MC_SKILL_CACHE/yarn-mojmap-pairs` → tmpdir** 取第一个真有 `index.json` 的落点，**显式 `--pairs-dir` 一律照办**（指错也响亮 skipped，不许静默回退去读别处的副本）；成功时新印一行 `leg2 对照源 <相对路径>（N 档 / M 对）`，把分母摆到输出里而不是写死在注释。生成器仍**从不写仓库**（写盘守卫不变）。同批改掉三处过时散文：本文件头「许可不可再分发 ⇒ 数据由 build 现拉到 cache」、`assert-cross-layer-names.mjs:52-55`「**从不入库、CI 永不可得**」、`build-yarn-mojmap-pairs.mjs` 产物段。**实测（`MC_SKILL_CACHE` 指空目录 = 干净 clone 处境）**：`leg2 对照源 data/_yarn-mojmap-pairs（15 档 / 112 971 对）`、判 483 件、冲突 0、无对照产物 85 件，rc=0；`assert-cross-layer-names --require-pairs` 同环境下 ok（判 847 / 扫 1249，pairs 档 15、无对照产物 0）。**投毒 5/5**（P0 未变异仍判到件；P1 `PAIRS_DEFAULT` 改指 tmpdir → 退化成 skipped；P2 目录名写错 → skipped；P3 存在性判据换成恒取 tmp → skipped；P4 让 `--pairs-dir` 被忽略 → 显式指空目录却仍判到件 = 被抓）。⚠️ **P1 第一版是废投毒**：只从候选数组里删 `PAIRS_DEFAULT` 一项**不会**退化，因为数组全落空时的兜底就是它 —— 证到的是「兜底路径也指向仓库副本」，不是我要证的那条；改 `PAIRS_DEFAULT` 本身才落到目标腿。另记：`data/_yarn-mojmap-pairs/yarn-mojmap-pairs-provenance.json` 的 `consumedBy` 行仍写「尚未默认读本目录」，本次改口后为假 —— 该文件由对照表拥有者下次发布时按 `refreshHint` 重算，我没动（避免与并发会话抢写 + 自哈希表）。
  - **B3（摘要位裸标记）**：`get_fabric_doc_summary` / 按 `index-l2.json` 烘焙 meta 服务的 `get_*_doc_full`，其 `sections[].summary` 是**烘焙期按未展开原文**截的首段，会把 `<<< @/…` 当正文交给模型（正文侧早已由 `expandTranscludesFor` 展开，两机制一直不同源）。新增 `transclude.ts:stripTranscludeMarkers()` + `store.ts:sanitizeBakedSummaries()` 挂在 `loadCachedIndex`（三个 index 载入的唯一切点），`firstParagraph` 也先剪标记再挑首段。**语料字节 0 改动**（`data/**` 上游逐字，见仓根 AGENTS.md §数据链口径）。**爆炸半径先量后修**：`data/` 下 63 目录 / 183 个 `index-l*.json` 里 **72 处标记**，全部落在 `[].sections[].summary` 一个字段，只 `fabric_1.21.11`（2）与 `fabric_26.1.2`（70）两档，去重后 **24 页**，且剪后 **0 条变空**（无损）。源头分母：`processed/*.md` 557 页里 325 页本体含裸标记（不改）。**实测**：538 次真调用（4 档 × summary+full）响应残留 0；新腿挂在 `assert-fabric-transcludes.mjs` §C（造真 store 调 `loadSummary`/`loadFullDoc`，不比对文件字符串），打印「靶子 24 页 / 扫出字段 222 条 / 残留 0 条」，另配 2 条合成串单元腿（语料哪天重烘焙、靶子归 0 时仍不失去判据）；该门 1.1 s → **2.0 s**。**投毒 3/3 各红在该当原因**（M1 摘掉载入处的清洗调用 → 残留回到 72；M2 让清洗器空转、字符串全在 → 72；M3 让 `stripTranscludeMarkers` 原样返回 → 72），跑完从备份逐字还原、dist 锚点回读 = 清洗器 2 处 + `return text;` 0 处。
  - **AGENTS.md 冲突面（用户点头放开）**：仓根 AGENTS.md「对照数据出处」那条原写「**整表不得提交**」，与已入库的派生表对着。改成三层口径：原始 `client.txt` 仍 gitignored 不入库（许可）；**派生 join 表**（15 档 / 112 971 类名对，只有 `obf/mojmap/yarn` 三类名 + 两侧 FQCN，不含成员名）按 2026-09-25 裁定发布在 `data/_yarn-mojmap-pairs/`、逐件 sha 钉 provenance；读侧候选顺序与 `--pairs-dir` 语义写进文档；「Skill 正文只写逐件用到的少数名字、不得整段抄表」保留不变。
  - **补记两条先前漏写的完成条**（此前只进了任务面板与 docs，没进本文件）：**S6-③** `generate_lang` 空 `entries` 不再静默 —— 追加一条空骨架披露 warning + 机读字段 `emptyEntries:true`（`src/generators/index.ts` / `common.ts`），`test-core.mjs` §S6-③ 23 条断言行，CLI 实测 `--entries={}` 回 `ok:true` + `resultKind:"ok"` + 该 warning；**S5′** 库坐标 × maven-metadata 互证 —— 联网抓取器 `scripts/check-lib-coords.mjs`（opt-in、默认 dry-run）+ 离线门 `scripts/assert-lib-coord-snapshot.mjs`（selftest 23 组）+ 快照 `mcp-server/data/lib-coords/maven-metadata-snapshot.json`，实测 36 件 SKILL.md / 23 条坐标声明（正文 9 + versions.json slots 14）/ 3 个唯一坐标**全部非 central**，故 Central 那条腿由 `--probe-control`（gson）证明活着、不由存量背书；已挂 `test-scripts.mjs` 真跑 + selftest 两条数组。**仍未做**：`emptyEntries` 在 AGENTS.md / README.md / mcp-server/README.md 三处 0 命中（文档面欠账）。
- **未做（本轮）**：① Mixin 目标行（`@Mixin(targets=…)` / `@Accessor`）没做 —— 本仓 fabric 语料 `develop_mixins_*` 与 1.20.4 docs 里 `@Mixin(` / `targets = "` **逐字 0 命中**，不凭训练记忆补格式；② ~~AW `inject-interface` 条目生成与解析（解析器目前只认 accessible/extendable/mutable 三 directive）~~ **解析侧已由 #55 闭环**（五族 + v2 头，见上方 #55 条）；**生成侧仍未做** —— 要吐 `inject-interface` 行得给 `convert_mapping` 新增「注入哪个接口」的入参（工具面参数合同变化），未默默加；③ 1.16.5–1.20.x 的 `m_/f_` SRG 层入库（要 Forge 官方 SRG 产物，不是抓取缺页）；④ ~~新门未挂~~ **本轮更正**：`assert-generator-rejection-codes.mjs`（S6-①）在我写完之后的 16:0x 已被**并发会话**挂进 `test-scripts.mjs` 两条数组（`:2058` 真跑 / `:2120` selftest），我按「写门后先 grep 再决定挂不挂」核过、**没有双挂**；⑤ S5′（库坐标 × maven-metadata 互证的维护侧 opt-in 脚本）未开工（S4′ 原列于此，已由上面「S4′」完成条闭环，见「自审补丁」条的更正）。

- **N0 + L7（2026-09-25）**：**N0** A4a 探针落地 —— `src/tool-registry.ts` 工具回显面加 `clientCapabilities: server.server.getClientCapabilities?.() ?? null`（宿主能力原样回显、零载荷变更；CLI 直调为 null），`npm run build` 绿；防丢钉挂 `test-scripts.mjs` #18（dist 含探针 + src 恰 1 处）。**L7 闭环** —— 全 16 个 Forge 档 `ForgeRegistries` 字段名一手取证（12 档 maven 最新 `universal.jar` + 1.13.2–1.16.5 按 Gradle 缓存版本号补下 25.0.223/28.2.26/31.2.50/36.2.34；缓存里这四档只有 `-launcher.jar` 不含该类），逐档 `javap -p` 单类；清单与构件坐标 = 新文档 `docs/evidence-forge-registries-all-versions.md`，原始产物 = `D:\mc-skill-temp\l7-registries\`（用户机本地不入库，同 L6）。要点：1.7.10/1.8.9 无 ForgeRegistries；**1.19 是注册表键名分水岭**（ENTITIES→ENTITY_TYPES、BLOCK_ENTITIES→BLOCK_ENTITY_TYPES、CONTAINERS→MENU_TYPES、PAINTING_TYPES→PAINTING_VARIANTS、PROFESSIONS→VILLAGER_PROFESSIONS、DATA_SERIALIZERS→ENTITY_DATA_SERIALIZERS、LOOT_MODIFIER_SERIALIZERS→GLOBAL_LOOT_MODIFIER_SERIALIZERS 共 7 对）；1.21.1 首次无 ENCHANTMENTS；1.18.2 无 FLUID_TYPES（与 L6 互证）。`CONTRIBUTING.md` 未排期清单 L7 行改判「已闭环」。取证过程另录：CDN 对 node https 客户端与连发请求回 404/空体，改 `curl.exe` 串行单发成功（复核时注意）。
- **S1′（2026-09-25，用户裁定「挂在兼容工具 `query_api` 上，不开新工具」）**：mcmap / Linkie 两家共同的第一强项 —— 映射名的模糊 / 简名 / 成员级检索 —— 接进现有工具面。新函数 `mappingsNameProbe(version, className, limit)`（`src/mappings/yarn-sqlite.ts`）查在盘 `data/<平台>_<版本>/mappings/yarn-mappings.sqlite`，返回 `exists / matchKind(exact｜simple-unique｜ambiguous｜contains｜none) / named / intermediary / official / memberCounts / memberSample / mappingEra / dbKind`；`queryApi`（`src/api/index.ts`）在 **api-index 未命中** 与 **该档根本没有 Parchment 索引（`DATA_UNAVAILABLE`）** 两条出口上挂第二档 `nameIndex` + 三句披露。**三条边界写死在代码与文档里**：① `found` 恒仍 `false`（存在性不得冒充签名命中）；② `mappingEra` 必须念（`yarn-tiny` 的 named 是 Yarn 名，`forge-srg`/`tsrg`/`mcp-csv` 的 named 是 MCP `func_/field_` 名 —— 「文件名里的 yarn 会骗人」那条裁定在工具面的延伸）；③ 无库 / `classes` 表为空 ⇒ 整键缺席（不给空库背书，与 `assert-skill-yarn-attest` 双闸同口径），26.1+ 无库自然缺席。实测（`MC_SKILL_DATA` 指仓库 `data/`，走 dist 真调用）：`StatusEffect@1.21.1` → `named=net/minecraft/entity/effect/StatusEffect` 27 方法 / 11 字段；`World@1.21.1` 102/36；`Identifier@1.21.1` 32/9；`LivingEntity@1.14.4`（api-index 空档）175/78；`MobEffect@1.20.1` 照旧 `found:true` 且 **nameIndex 缺席**（命中路径零改动）；乱造名 `exists:false`。盘上各档 `classes` 行数实测 1.14.4=4976 / 1.16.5=5445 / 1.20.1=7431 / 1.21.1=8262 / 1.21.4=8850 / 1.21.11=10274。钉子：`test-core.mjs` 五组断言（接线 / 证伪 / 披露 / 无库档 / 命中路径），**逐腿投毒各红在该当原因**（A 摘探针 ⇒ 三腿全红；B 永真化 ⇒ 红在「成员数恒 0」与证伪腿；C 摘披露 ⇒ 只红在披露腿），投毒跑完按备份字节还原（`grep -c POISON` = 0）。工具面数 **82 不变**（这就是"不开新工具"换掉的东西：`list-tools` 锚 / `assert-cli-full` 全量档 / 根 README + AGENTS 工具表四处计数全部免改）。同批更正：**A1 的「46 处 `code:null`」前提被读歪** —— `GeneratorResult.code` 是**生成的源码正文**不是状态码（`src/generators/common.ts:61-62`），拒绝时 null 是正常形状；真正的机读位在 A1 已落地的 `action.code` + `resultKind` 上。剩两条真残差登记未做：**兜底码 `GENERATION_FAILED` 无棘轮**（全仓 grep：只在 `utils/actionable.ts:105` 定义处，测试 0 处 ⇒ 新加生成器若文案不匹配 `REJECT_RULES` 会静默落兜底），**schema 层拒绝是一整条中文串**（`errorKind:"validation"`，机器要按字段分支得再解析）。

- **A1（2026-09-24）**：`generate_*` 拒绝出口配机器可读码 —— `ActionCodes` 15→18 键（`VERSION_UNSUPPORTED` / `NO_NATIVE_GENERATOR` / `GENERATION_FAILED`），`generators/common.ts` 单点分类器把 63 个拒绝位点的自由文本归到 `result.action.code`；唯一出口（`write-helper.ts`）附信封，46 处 `code: null` 骨架位与既有 `errors[]` 原文**一字未动**。`test-cli.mjs` C1 探针补「同 tool 不同拒绝 ⇒ 互异且非空 + nextSteps 非空」。
- **A2 + A3（2026-09-24）**：四个检索面（`search_docs` / `search_forge_docs` / `search_fabric_docs` / `search_neoforge_docs`，另 quilt 透传）加按调用 `limit`：默认窗口与逐字载荷不变、只放宽不收紧、显式传参带 `limitWindow{requestedLimit,resultLimit,candidates,limitMax,clamped}` 且超池必披露；上界派生自各面构造上界（L0 腿 cap ∪ 语义腿 10）。GENRE 门判据⑥扩到四面真跑 × 2 探针，并把「内部窗口与声明上界脱钩」翻成真投毒（池够大却不多给即红）。
- **A8（2026-09-24）**：新门 `assert-test-chain-coverage.mjs`（盘上 `*.test.mjs` ↔ `node --test` 链双向差集 + 5 态自证），挂进 `test-scripts.mjs` 两条数组。
- **A9（2026-09-24）**：`query_upstream_releases` 回 `versionType`（modrinth `version_type` / Mojang 清单 `type` 逐字；其它源上游不提供 ⇒ 缺席，缺席 ≠ release），input/output schema 与描述同步。
- **A4c（2026-09-24，用户裁定「全补 8+3」）**：上游端点表铺到 mcmap 表的 **16/16 主机** —— 新增 `legacyfabric-loader`（过滤走 `?game_version=`）、`mojang-manifest`（官方版本清单）、`maven`（`slug=<alias>:<group>/<artifact>`，12 条写死别名）；主机白名单同步扩（含实测重定向落点 `repo.legacyfabric.net`，另有 `maven.legacyfabric.net` 302 与 shedaniel 驼峰 artifact 两条实测发现）；每条新端点配「上游确实没有」正对照（`ok:true + available:false`）。
- **A4c 口径洞补丁（2026-09-24 晚，用户回报）**：`source=maven` 的 404 **只证「该坐标没有 maven-metadata.xml」**，分不清「构件不存在」与「写法不对」⇒ `notFoundPayload()` 给 maven 的 404 附 `hint`（出口指向 `source=modrinth` 与父目录索引），其余源形状不变；`test-scripts.mjs` #18 加一条离线 selftest 腿。连带把吸收台账里 `modmaven`（补正值 `mezz/jei/jei-1.20.1-forge`）、`progwml6`（**正值未证 ⇒ available:false 只证伪该坐标**）、`shedaniel`（正值改现役 `me/shedaniel/cloth/cloth-config-fabric`；老构件 `ClothConfig` latest 0.3.3 降级为机制证据）三行口径改准。
- **A5 + A6（2026-09-24）**：台账 85→86（W5-4 结论落账，含「`_anchor-lint.mjs` 存在但未纳管」的事实口径）；`temp/audit-20260918/REPORT.md` 追加 W5-5 四条事实。
- **A7（2026-09-24）**：`build-yarn-mojmap-pairs.mjs` 入库（Yarn↔Mojmap 对照产物，12/13 档 / 91,597 对，落 `$MC_SKILL_CACHE`，缺 `client.txt` 现拉、从不写仓库）；`assert-skill-mappings-key.mjs` 加 leg2（键值 ↔ 围栏实名 + 披露点名腿；无产物打印 `leg2 skipped` 且不改退出码）；存量 123 件冲突按棘轮登记（只许降不许增）。台账该条 open → closed。
- **A4a / A4b（2026-09-24 用户裁定）**：A4a 只做探测不改形状（探测结论与「会打断谁」清单见 `docs/mcmap-linkie-absorption.md` §4）；A4b `outputSchema` 暂不铺（保持 1/82，触发条件见同文件）。
- **清尾① 未版本化现行页门（2026-09-25）**：新门 `assert-quilt-unversioned-wiki.mjs` —— quilt 14 topic × 6 档 wiki 页的**拷贝事实**（除「> 抓取时间」行外逐字同）+ 页内「未版本化现行页」警告在册 + 双向登记（新 wiki 页未三分类即红）；真跑 84 页全绿、自证 7 例；挂 `test-scripts.mjs` 两数组（真跑 35 / 自证 24）。
- **跨层名缺口①②落地（2026-09-25 第三遍）**：① leg2 值域按**族**扩到 `parchment`/`mcp`（判件 315→**437**；首测 8 件冲突全按内容修正，无一进基线），并给 leg1 加 `parchment ≥ 1.16.5` 值-版本判据（自证 31→**37**）；为复用名字集合，该门加 `isMain` 守卫 + 导出 `loadPairs/LEG2_META_LINE/PAIRS_DIR`。② 新门 `assert-cross-layer-names.mjs` 覆盖 leg2 之外的面（knowledge / code-patterns / .cursor/rules / AGENTS / CLAUDE / scaffold），判据 = 档位类名层 × 四腿（A yarn 档用 mojmap 名 / B mojmap 档用 yarn 名 / C mojmap 档用本档不存在的 MCP 顶层名 / D MCP 档用同代 mojmap 名）；首测 A=82 B=48 C=33 D=109，基线登记 A/B=130 · C=33 · D=109（棘轮只许降，`--strict` 看全量），自证 15 例（含端到端先红后绿）；两门都挂进 `test-scripts.mjs`（真跑 **36** / 自证 **25**）。③ 两个未决项只取证不动：1.16.5 的 official/mcp **两通道都合法**（Forge 1.16.x 文档逐字 + Forge-Class-Remapper），待核的是 23 件正文方法名；1.15.2 的 `official` 仍无可判定论（网侧只有 FG 通道表）。详见 `docs/knowledge-coverage-sweep-20260924.md` §6.9。**当日续（§6.10）**：③ 两项用一手证据定案 —— ① 1.16.5 的 MCP named 表从 Forge maven 下到（`mcp_snapshot-20210309-1.16.5`，HEAD 200 / 478,481B / sha1 `f6273df8…`），解出的 `fields/methods/params.csv` **已纳管** `data/forge_1.16.5/mappings/mcp_snapshot-20210309/`（附 provenance），据此机判：**23 件 `mcp` 声明里 MCP 侧证据 0 件**（Mojang 侧为主 3 件 / 无判定证据 20 件；对照 12 件 `official` Mojang 侧为主 6 件）⇒ 那 23 件应与骨架统一为 `official`（待裁，不动文字）；② 1.15.2 用本机 Gradle 缓存里的 `forge-1.15.2-31.2.50_mapped_official_1.15.2.jar` / `…_mapped_snapshot_20200514-1.15.1.jar` 跑 javap 定案：**official = MCP 类名 + Mojang 成员名（可读：`Block$Properties.of`、`World.isClientSide`/`getLevel`）**、snapshot = MCP 类名 + SRG 成员名（不可读）⇒ scaffold `official` 与 `AGENTS.md:29` 成立，未决的是 35 件 skills 的 `mappings: mcp` 与正文成员名混用（`isClientSide` 20 处 vs `isRemote` 1 处；`Properties.create(` 24 处）。**当日续（腿 E，用户裁定「A + 文件内提醒混合档」，否决统一到 official）**：`assert-cross-layer-names.mjs` 增**腿 E「成员名族」**——混合档（forge ≤1.16.5）声明件的 `mappings` 值与正文成员名族**相反**才红（两套并用/无判别名不判）；MCP 侧 = 纳管的 `mcp_snapshot-20210309`，Mojang 侧 = `client.txt`（缺档计 `noMemberTables` 不静默绿）；首测 E=2＝基线 2（`forge/1.16.5` `mc-networking`/`mc-renderer`），自证 +6 例；接线坑已修（声明件在 `.cursor/skills/**` 被主循环排除 ⇒ 改 `collectDeclared()` 独立取件）；文件内提醒落在两档 `AGENTS.md` Mappings 行 + 被点名件 `mc-networking` 正文头（`mc-renderer` 前已有）。**审阅修正（同日，外部审阅指名三条全成立）**：`noMemberTables` 分桶（**许可**=缺 client.txt 合法跳过 / **数据洞**=有 client.txt 缺已纳管 csv ⇒ 计 rc），杜绝「clone 上腿 E 静默失效被读成许可原因」；新增 `provenanceAudit`（`*provenance*.json`=应入库标记：csv 缺 ⇒ 红、在盘未 add ⇒ 每次可见 WARN）；1.16.5 MCP 表**扁平化**到 `mappings/` 顶层（与 1.14.4/1.15.2 同形——平铺消费方 `mcp-csv-extractor.js:223`/`build-yarn-sqlite.mjs:429` 此前不可见；`--all` 下次将对 1.16.5 建 `mcp-csv` era sqlite，同 1.15.2）；L10（`CONTRIBUTING.md:393`）补第二遍现扫 **14 路径/17 文件**。**N0–N3 对盘（同日）**：N1/N2 已在盘不做（`releases.ts:288/297/317/506`、`write-helper.ts:200`；46 处 `code:null` 属已裁定保留的骨架位）；N0 拆两半——`get_server_status` 回显 `clientCapabilities` 探针不做（改输出形状，A4a 裁定「只探测不改形状」），#18 新增 **A4b 棘轮：outputSchema 保持 1/82**；N3 落地 `assert-skill-yarn-attest` **26.1.2 放行腿 + 覆盖披露**（清单 33＝判 33+放行 0；fabric 共 532 ⇒ 未入清单 499），**清单全量扩面实测 rc1/约 1615 处 ⇒ 批量工程不做**（数字进门摘要）。
- **全平台跨层名审计 + 修复（2026-09-25 第二遍）**：确立「档位类名层」判据（forge ≤1.16.5 = **MCP 类名**（该档 official 不含类名）/ 1.17.1+ = mojmap（official·parchment 都叠其上）/ fabric = yarn、26.1.2 = 去混淆官方名 / quilt = yarn / liteloader·rift·modloader = MCP 系 / bedrock = 无 Java 映射），据此全量盘存 `ServerWorld`/`ServerLevel`/`WorldServer`。**修 15 处（14 件 + 7 档镜像同步）**：1.12.2 与 1.13.2 的 `mc-weather:17` 再修 `ServerWorld`→`WorldServer`（`joined.srg`=116/0、`joined.tsrg` 直证 —— 上一轮把这两档写成 `ServerWorld` 是**错的**，§6.7-B 已就地标注修正）；1.17.1/1.18.2/1.19.4/1.20.1/1.20.4 的 `knowledge/antipatterns/networking.md:71` 与 `.cursor/rules/06-networking.mdc:13` 共 9 处 `ServerWorld`→`ServerLevel`；1.16.5 `code-patterns/01-block-patterns.md:88`、1.14.4 `knowledge/antipatterns/entity.md:80/86` 的 `Level`→`World`；1.14.4 `mc-sound` 的 `mappings: parchment`→`mcp`（1.14.4 无 parchment 通道）与 `Level.playSound`→`World.playSound`。门链 rc=0（mappings 1247 件 / leg2 判 315 / 冲突 0 / 自证 31 · mirrors · ledger）。**登记四条门覆盖缺口**：leg2 只看 `.cursor/skills/**` 围栏代码且只认 `yarn|mojmap|official` ⇒ `parchment`(140 件) 与 `mcp` 声明件不进判据；`knowledge/**`·`code-patterns/**`·`.cursor/rules/**`·`AGENTS.md`·`scaffold/**` 无门（本轮 15 处里 13 处在盲区）；散文行不判。**未决**：1.15.2 `scaffold=official` 与同档 skills/rules/manifest 的 `mcp` 矛盾（两侧均有本仓证据，未裁不动）；1.16.5 同档 12 official + 23 mcp 分档（缺 1.16.5 MCP named 方法表）。详见 `docs/knowledge-coverage-sweep-20260924.md` §6.8。
- **清尾② A10 证据纳管（2026-09-25）**：`temp/ralph-20260922/evidence-forge-1182-registries.md` 抽成 tracked `docs/evidence-forge-1182-registries.md`，含 2026-09-25 javap 重跑的**完整 32 字段名单**（29 `IForgeRegistry` + 3 `Supplier`）、jar sha256、双机制复核命令、四表与 assert 重签落地状态。
- **清尾③ G4 qsl-verified 索引 sha（2026-09-25）**：亲核缺口比报告更宽（**6 档全部**缺 sha256，18 条目中唯一；l0/l1/l2 三件同缺）；`repair-quilt-indexes.js` 增 sha 补齐/纠偏 + `--audit` 判 rc（6 档各补 1 条）；根 `scripts/index-qsl-verified.mjs` 补 sha 写/刷新（dry-run 幂等验证）；`assert-qsl-verified-sync.mjs` 加判据④（三方 sha 对账；先红 18 行→修后绿，自证 7 例）。
- **A7 leg2 判据四修 + 清理批（2026-09-25）**：① 等名对进两侧集合；② 两侧并入**嵌套类最内段**作保护（tiny named 列 / client.txt 现读，缺源退产物 `$` 形态）；③ **段名只保护、不建旗**；④ 第三方库撞名走**逐文件豁免台账**（cloth-config）；⑤ **元语境行不算用法**（`不要/禁止/❌` 禁止句 + `Minecraft Wiki/EULA` 专有名词）。加只读 `--dump-leg2`；自证 **24/24**；基线 **123 → 53 → 31 → 21 → 11**（判 315 件）。清理批（子代理逐件分类 + 落地）：**fabric 19 → 0**（10 件元语境假阳性 + 9 件补披露行/新建披露块；行内容全部由 pairs 逐档 join 取证，`mappings_alt` 件 59→61）、forge/1.17.1 `Chunk`→`LevelChunk`；镜像已 sync、`assert-skill-mirrors` 绿。**⑥ MCP 类名保护（同日，外网一手裁决后）**：Forge 官方 1.16.x 文档逐字「The official mappings provide all method and field names, with the class names coming in 1.17」+ 1.16.5 MDK `build.gradle` 注释「official … Official field/method names from Mojang mapping files」⇒ **1.16.5 的 official 通道不含类名，档内成文（「方法/字段官方名、类名仍是 MCP 名」）是对的、此前判「成文冲突」是门侧误读**（`client.txt` 是 Mojang 原始文件，FG 1.16.5 不取类名）。leg2 的 official 方向改为扣除仓内 `data/forge_<v>/mappings/*.tsrg|*.srg` 的类行简名（`mcpClassNameProtect`；1.16.5 有源、1.17.1+ 空集，恰对应「1.17 起 official 含类名」）⇒ 11 件全绿，**基线归 0（零容忍）**、自证 27/27。
- **错修/遗留修复批（2026-09-25 同日，用户问「同型问题之前被错修了吗」）**：全仓只 1 处真错修 —— `sweep69` 把 1.16.5 `mc-particle` 的 `ServerWorld` 误改成 `ServerLevel`（语料 `ServerWorld=4/ServerLevel=0`），已回改；其余同型是初代遗留：`mc-weather` ×5 个 MCP 时代档的 `Level / ServerLevel` → `World / ServerWorld`（1.17+ 不动）、1.16.5 `01-registry.mdc` 的 MCP 方法 `.requiresTool()`/`.maxStackSize(64)` → official `.requiresCorrectToolForDrops()`/`.stacksTo(64)`（`client.txt` 直证）、`mappings: hint` → `parchment`、**94 件 `mappings: mcp` → `parchment`**（1.18.2/1.19.4/1.20.1/1.20.4；FG5 文档「stable/snapshot 自 1.17 起不再存在」+ 四档 scaffold 全钉 parchment）。leg1 增**值域前缀白名单 + mcp 值-版本约束**（≤1.16.5 的 forge 档）防复发，自证 31/31；另修 `forge/1.20.4` 与 `neoforge` 两处 scaffold 注释误读（「official = MCP」+「Mojang 混淆名」→ 改准）。9 档镜像已 sync、`assert-skill-mirrors`/`test-scripts`/`assert-rule-ledger`/`assert-script-write-guard` 全绿。详见 `docs/knowledge-coverage-sweep-20260924.md` §6.7。
- `search_forge_docs` / `search_docs` 的 `VERSION_NOT_FOUND` 载荷四平台同键：`ok:false` + `platform` + 顶层数值序 `availableVersions` + `error` 对象（旧的字符串 `error` + 顶层 `code`/`hint` 形状废除）。
- 反编译链：TOML 三引号值解析修复（此前整份 `mods.toml` 解析失败）、输出目录身份/清目录与包名归属白名单校验。
- 新增只读一致性门（G1 反编译身份与归属 / G2 解析器可用性 / G3 语料保真 / G4 索引自洽 / `assert-forge-1182-registry-consts` / `assert-legacy-isolation`），均带投毒自测并接入 `test-scripts.mjs`。
- `npm run community:index` 补 `--write`（此前该链是 dry-run，`wrote N entries` 不可复现）。
- `tools/list` 现 80 个工具（`release-smoke` 实跑），CI 步骤名同步。
- 知识包文档面：forge / fabric / quilt / neoforge 各档「本档不存在的 API」、版本分界与第三方库上界按一手语料 + Modrinth/maven 实读更正。
- **当前为 81 个工具**（上方「`tools/list` 现 80 个工具」是当时的记录）：新增 `resolve_lib_skills`（按平台 + 精确 MC 版本解析 `knowledge/libs` 库 skill 源稿；与 CLI `lib resolve` 同一 core，只解析不返回正文）。
- **CLI 双入口提级（2026-09-17）**：工具线 `mc-skill`（= `dist/cli.js`，dispatch 全部工具）+ 仓库线 `mc-skill-scripts`（`bin/mc-skill-scripts.mjs`；`lib` / `corpus` / `cloth` / `gate` 四组共 9 个子命令，薄壳转发既有脚本）。
- **CLI 审计修复（1 高 5 中 4 低）**：`provision-26x-docs.mjs` 收口 write-guard（默认干跑、`--write` 才删、`--data-root` 沙盒）；`--timeout` 超时信封后强制退出；转发失败打印诊断、EPIPE 静默退出、入口崩溃输出 JSON 信封；`fetch-embedding-model.mjs` 下载加超时与重试提示；`gate list` 目录错非 0、帮助用真实入口名、版本读失败不静默。
- **CLI 测试双档**：`assert-cli-quick.mjs`（快档，进默认门链）与 `assert-cli-full.mjs`（81 工具全量档，**不默认跑**；含逐条豁免原因与汇总表）。
- **CLI 审计第二轮修复（NP-1~NP-13，2026-09-17）**：`update --action=apply` 的 npm 调用改走 `cmd.exe /d /s /c`（Node 对 `.bat/.cmd` 的无 shell 加固实测 `EINVAL`，此前 Windows 构建步恒败）并带步骤归因；EPIPE 不再洗白已置的非零退出码；sqlite 边界改**运行期加载**（22.5–22.12 无 `--experimental-sqlite` 时入口横幅真正可达）；`mdk` 解压失败清残树 + 成功落 `.mdk-unpack-ok` 哨兵 + `allowCacheFallback` 强判据；`update --action=apply` 新增 `update-apply` 跨进程锁（`utils/dir-lock.ts`，与反编译缓存锁同源）；`--timeout`/崩溃强退前同步收掉在跑的 java 子进程；`provision-26x-docs.mjs` 的 `--data-root` 覆盖到抓取段（6 个子脚本同一解析器）；`fetch-bedrock-docs.js` 的 fetch 加 30s 超时；`-V`/`-v` 短别名与 `-h/-V/camel` 帮助文案；`argv[1]` 缺失时入口明示 + 非零退出。
- **勘误（计数，不回改历史行）**：上方「CLI 审计修复（1 高 5 中 4 低）」的枚举与计数不对应（枚举 9 条；标题按 1 高 + 5 中 + 4 低 = 10 条，且未含同批的 S1/S2）。该批真实清单以 `temp/ralph-cli-audit-PLAN.md` 为准。另：根 `README.md` 的「MCP 服务名（80 个工具）」当时并未改净，本轮修为 81。
- **当前为 82 个工具**（2026-09-21；上方 80/81 均为当时记录）：新增 `query_upstream_releases`（`src/upstream/releases.ts`）—— 查**上游发布源**某版本存不存在 / 最新到第几 build，补的是本仓唯一缺的能力面（`list_*_versions` 只列已入库档位）。七源：forge / neoforge maven-metadata、fabric-loader / fabric-yarn / quilt-loader / parchment 的 meta 端点、modrinth。三态分离（`ok:false` 没查到 ≠ `ok:true`+`available:false` 上游确实没有）、`matchRule` 回显版本归属规则、入口与重定向落点双白名单、fetch/curl 双腿。**仓库首个带 `outputSchema` + `structuredContent` 的工具**（P0-2 分层输出：省 token 靠 `limit` 截断 + `total`/`truncated` 把截断说破，`content[0].text` 仍是完整 JSON，CLI 的 `unwrapHandlerResult` 与退出码不劣化）。判据与 neoforge 前缀反证挂在 `test-scripts.mjs` #18。
- **①B 事故与收口（2026-09-22，forge_javadoc）**：按用户裁定整 6 档 `--force` 重抓后实测发现 —— `parseClassPage` 只认新式 javadoc 的 `<pre class="memberSignatures">`，而 skmedix 这套镜像是**旧式表格**（`memberNameLink` / `colFirst`，实测 1.12.2 `Block.html` 361 KB / 228 个 `memberNameLink` 解析出 0 成员）⇒ 一次重抓把 **17772/18079 页刷成 288 字节空壳**，而当时**全部门是绿的**（G3 比 raw↔processed 同源、parity 比计数、新门比 frontmatter 形状，都不看正文）。已 `git checkout -- data/forge_javadoc` 回滚（HEAD 未丢一页，回滚后 `Block.md` 1.7.10=10258 / 1.10.2=9927 / 1.12.2=9917 字节复现），并落四道防线：① 抓取器「解析空 ⇒ 绝不覆盖盘上已有正文」+ 「HTTP 200 实为 GitHub Pages 404 页 ⇒ 不写盘」（实测：错档 URL `client/event/ITextComponent.html` 返回 200 + 9379B "Page not found"）；② 批量拒覆盖 ≥50 页 ⇒ 打印「是解析器读不懂形状，不是这些页没成员」；③ 新门 `assert-javadoc-build-provenance.mjs` 判据⑥「整版无正文率 >2% 即红」（HEAD 实测基线 22–27 页/档 = 0.74%–1.17%，全是合法薄页）；④ ~~解析器本身仍未修 ⇒ 任何 `--force` 不得再跑~~ **已由下一条推翻（解析器已重写并重抓收口）**。
- **解析器重写 + c4 全量重抓（2026-09-22，用户裁定「2.修复,然后重抓」）**：`parseClassPage` 改成按 jdk8 旧式形状解析 —— 成员取 `<h3>X Summary</h3>` + `table.memberSummary` 的 `colFirst`（修饰符与类型）/ `colLast`（`memberNameLink` + 参数），注解页另认 `Required / Optional Element Summary`（第一版漏了它 ⇒ 六档 47 页被自己的「解析空」守卫挡下，页级腿 `memberNameLinks` 计数把它抓住，没有写成空壳）；类声明改为「从声明关键字起切」（`@Deprecated` / `@Retention(…) @Target(…)` 前导行不再吃掉声明，且 `collapseGenerics` 把 `PACKAGE) public` 的空格压掉会让 `^` 锚定的判据失效 ⇒ 结构判定改在未压空格的文本上做）；渲染新增 `## Elements` 与页首 `**Inheritance:**`。**判据⑥做成两条独立腿**：页面级 `isUnreadableParse`（成员 0 且「HTML 自己声称有成员」或「连类声明都没有」）+ 批量级 `ZERO_MEMBER_RATIO_MAX = 0.05`（分母只算本轮真解析过的页，超线 ⇒ `process.exitCode = 1`；盘上语料真·空类率实测 1.3–2.0%，事故那轮是 98%）—— 原写法的「0 成员 && 0 类声明」在**新式页**这个形状上恰好两腿都不成立，已在 `test-scripts.mjs` §S9 用夹具钉住。全量 HEAD↔本树比对（18078 页逐页，非抽样）：`worse=0 / zeroed=0 / missing=0`，`sigLost=1` 追出第三处回归 —— 继承链按「外层切 6000 字」收 li 会把链后面的 `<li class="blockList">`（类注释 / 类声明 / Summary 表）一并并进 `**Inheritance:**`，实测 **14704/16423** 页被污染、个别页因此丢签名块 ⇒ 改按 `ul` 深度计数只取那一棵子树 + 逐条要求类名形状，`PRODUCER_REV` 提 `c4-2026-09-22` 重跑六档。
- **新门 ②（`assert-javadoc-build-provenance.mjs`，已挂 `test-scripts.mjs` 真跑 + selftest 两腿）**：逐页核 `source:` 形状 `<mc>-<build>/<pkg>/<Class>.html`、单档 build 唯一且等于 `fetch-forge-javadoc.js` 钉表、目录包 == URL 包、文件名去 `.md` == URL 类名（含 `planClassWrites` 的 `~<hash>` 碰撞后缀与前缀截断两种合法形状）、`raw/` 下不得有非 `.md` 残留。判据自证 20 组夹具 + 7 例天花板投毒 + 8 例文件名形状，**每例都断言"红在该当的原因"**（第一版 11 例里 10 例红在无关判据上 = 假绿）。存量脏页按 `DEFAULT_CEILING=319` 的「只许减不许增」放行 —— 319 与六档 `_orphan-report.json` 的孤儿数**逐档相等**（9/30/41/73/67/99），两套独立机制互证；删除待用户点头，清单与可回退性已出（638 个文件全部 tracked 且工作树干净 ⇒ `git checkout` 可逐字节复原）。
- **⑤ 基岩 Script API 拆树收口（2026-09-22）**：`bedrock-scriptapi` 独立成语料树（624 页 / 624 条 index / 自建 semantic db），`bedrock-docs` 树缩回 258 页 Learn 文档。消费侧 `search_bedrock_docs` / `get_bedrock_doc_*` 改为两树各跑 L0+语义再按 id 去重（实测两树 id 零重叠；`System class runCommand` 20 条里 5 条来自 scriptapi 树），只有 `DocNotFoundError` 才换树，其它异常不掩盖；`platform-data.ts` 的 `hasPlatformDocData` / `docSourcesOf` 同步补第二源。台账：G4 `entries 60→61`、`perPlatform.bedrock.entries 1→2`（**chunks/embedded 守恒** 5054+9878=14932、3291+1166=4457）；`corpus-semantics-baseline.json` 拆成两行，`bedrock-docs` 的 `medianFloor 360→3526`（这才是拆树的真正收益：混树时两种体裁把中位拉到 360，厚度判据形同虚设）。
- **写盘形状门不再为仓库外的产物发红**：`assert-script-write-guard` 的扫描面原先把 gitignored 的 `mcp-server/scripts/_temp/**` 也扫，本机只要跑过一次 `tsc --outDir scripts/_temp/tsdist`，153 个编译产物就让第 8 步链恒红且谁都"修不了"（实测 2026-09-22）。现按 `git check-ignore` 过滤，**失败即不跳（fail closed）**，ok 行念出跳过数（当前 34），清单覆盖腿仍按「文件在不在盘上」判僵尸（否则三条 `_debug_*.mjs` 登记项被误报）。同批把两处 harness 硬钉改成从生产侧取数：G4 真跑锚点的 `台账 60` / `60 库` 现从门源码读 `entries`，`mergeSemanticResults` 点位认 `allowedIds,` 简写形（并要求该文件真的用 `getAllDocIds(...)` 喂全集，否则简写就是后门）。
- **抓取器两处通用缺陷修复（forge_javadoc）**：`writeWithRetry` 的失败报告 `_page-failures.json` 在本轮 0 失败时删除（实测 1.7.10 试点缺 1 页、全量重抓 0 失败，旧报告仍留在档里 ⇒ 读的人以为这版还缺页）；新增 `--prune-orphans` 一条独立腿（默认只出清单，`--confirm=prune-orphans` 才双边删 raw + processed，报告缺失/档位不符/producerRev 过期一律不动）；raw 下的非 `.md` 抖动残留每档收尾清扫。
- **新门 `assert-bedrock-genre-demote.mjs`（2026-09-22 用户裁定④，真跑 + selftest 两腿）**：基岩「版本更新说明」体裁 = **打标签 + 检索降权**，钉的是「两件事都成立且都没越界」。判据：① tag 拼写三方逐字一致（生产者 `RELEASE_NOTES_TAG` / 消费者 src / **编译产物 dist** —— 第三腿存在的理由是宿主跑 dist，只改 src 不 `npm run build` 时运行时没降权而前两条照绿）；② 在盘 `index-l0.json` 的 release-notes 集合与**用生产者判据现算的集合逐条相等**（缺标 = 改了判据没重跑 `--retag`，多标 = 手改 JSON，`mergeIndexL0` 下次抓取即覆盖）；③ scriptapi 树该类页必须为 0；④ 降权**只重排不过滤**（输入 N 行 ⇒ 输出 min(N,limit) 行、被降权行一行不少、非降权页相对次序逐字不变、factor ∈ (0,1)）；⑤ tagIndex 查不到 id 时回退行内 tags。真跑实测：bedrock-docs 258 条里 31 条该体裁、scriptapi 624 条里 0 条，两树均与判据逐条相等。投毒：4 例数据侧（少标 / 多标 / scriptapi 误标 / 全清）+ 1 例 dist 拼写漂移各红在该当判据，正对照（未投毒的临时数据根）rc=0；dist 投毒跑完按备份字节还原。
- **`assert-corpus-semantics` 补判据⑥ `p10Floor`（2026-09-22 用户裁定③）**：median 对「一页一声明」体裁不敏感 —— 把最薄的一成刷成 60 字，中位照旧。新判据按树 opt-in（不给全局地板，多数树 p10 天然低会假红），基线钉了 `p10Floor` 而统计没算 p10 ⇒ 当场红（不许空转）。scriptapi 树接入：门自己的 `measureTree` 现算 files 624 / stub 0 / sectionless 7 / **p10 123** / median 363 ⇒ `p10Floor = 74`（RELEDGER 同口径 0.6×）。投毒实测：刷薄最薄 63 页（10.1%）后 **medianFloor 那条腿不红、p10 这条红**（p10 73 < 74）⇒ 新腿抓的正是旧腿抓不到的形状。`--selftest` 另加 3 例（达标绿 / 尾塌红 / 统计缺 p10 红）。
- **判据⑦ `membersFloor` + 新门 `assert-bedrock-scriptapi-members.mjs`（2026-09-22 用户裁定④「members 接线，并找一个稳定计数源」）**：⑥ 只看正文厚度，看不见「成员列表被截断」—— 删掉一半成员小节的页正文照样几百字。`measureTree` 新增两条口径：`members`（页内 `## Members（N）` 逐页求和）与 `memberMismatch`（自报 N ≠ 实际 `### \`x\`` 小节数的页数），判定层 ⑦ 拆成 **⑦a 逐页自洽**（不依赖地板，当场红）+ **⑦b Σ 地板**（按树 opt-in，同 0.6 口径），并保留「基线钉了 `membersFloor` 而统计没算 ⇒ 判据空转必红」。稳定计数源不是手抄总数：专族门把渲染结果接回**生产者自己留在树上的 `scriptapi-typed.json`**（它解析 npm `@minecraft/server` 的 `index.d.ts` 得到的声明清单，含 `members[]` 与源文件 `sha256`/`bytes`/`version`），核 ① 三方计数（decls / index-l0 / 页面）② `name (kind)` 唯一且 label 可连 ③ 逐页四口径（清单成员数 == 页自报 == `###` 小节 == ` ```ts ` 块数−1）④ 每页 sha256 == index 记录 ⑤ 源留痕（64 hex / bytes ≥ 生产者下限 / label 版本单一且等于清单版本）。真跑实测：624/624/624 三方相等、Σ 两侧同为 **2590**、sha 对账 **624/624** 逐字节一致、来源 `@minecraft/server@2.9.0`（unpkg，760872 B）；`membersFloor = 1554`（= 0.6×2590，由门自己 RELEDGER 复算得同一值，非手抄）。投毒：场景 A（成员标题降级为普通 `###`，字节几乎不变）⇒ **只有 ⑦a 红**、median/p10 全绿；场景 B（成员段换成 1600 字散文，正文更厚）⇒ **只有 ⑦b 红**（Σ 0 < 1554）；正对照原样拷贝 ⇒ 两道门都绿。专族门另有 17 例内存夹具（含「改一个字符必须改变摘要」这条反证，防 ④ 成装饰）。
- **孤儿记录不再自我覆盖（2026-09-22 用户裁定③「提交，你做一个门去看它」）**：设计门时实测发现 `_orphan-report.json` 是**当前快照**而非删除记录 —— 删完 319 页后 c4 又跑过一轮，六档报告已变成 `count: 0 / files: []`（generatedAt 与 mtime 到秒吻合，确认是同一次抓取写的），于是「删了什么」在库里零留痕，只能靠 `git status` 的 638 条 ` D` 反推。修法在生产者：`pruneOrphans` 真删时按档另写 `_orphan-pruned.json`（`orphanPrunedRecord` 一处成形，形状由 `test-scripts.mjs` §S9 直接断言：count==files.length、带当前 `PRODUCER_REV`、`prunedAt` 可解析、点名内容不被改）。新门 `assert-javadoc-orphan-record.mjs` 核四件事：① 每档必须有对账记录，缺档只在 `_page-failures.json` 的 `count>0`（生产者"这轮没跑完 ⇒ 刻意跳过对账"）时豁免，且豁免要点名；② 记录自洽（`count == files.length`、`version == 目录名`、`producerRev == 当前生产者`）；③ 点名路径必须合法（相对 `.md`、无 `..`/绝对/盘符、不许指 processed）—— 与 `pruneOrphans` 内部把关同口径、从外面再核一次，手改 JSON 不能变成删除指令；④ 点名的页在 raw 与 processed **双边**都必须不在盘上（抓"说删了没删"与"后来复活"）。真跑现状：六档记录齐、rev 均为 `c4-2026-09-22`、`count=0`、留痕 0 档（机制上线前的那次清理 ⇒ 只打 info）。投毒走**真跑路径**（假数据根七档 + 只含合规档的正对照）：版本错档 / 旧 rev / 缺记录无凭据 / 页还在盘上 / 只剩镜像 / 留痕缺时间戳 ⇒ rc=1 且逐条红在该当判据，正对照 rc=0。
- **javadoc 生产者两处形状回归 + 一条测试面收口**：`pageHasBody` / `isShellPage` 的正文分节清单补 `## Elements`（注解页只有元素表，旧清单会把合法注解页认成空壳）；`forge-javadoc-indexer.js` 补 `invokedDirectly` 守卫（它顶层读 argv + 走盘 + **写六档 index-l0/1/2.json**，被任何测试 import 就等于在测试进程里重建索引并覆盖刚落盘的产物），`test-scripts.mjs` §S9 的守卫腿清单从 3 个抓取器扩到 4 个并加 `runAll()` 裸调用检查。抓取器尾行也不再无条件喊「✅ 全部完成」—— 有页未成功时打「⛔ 本轮未收口」，因为四小时作业崩在第 22 页也曾以 rc=0 收工。
- **`search_bedrock_docs` 加按调用的 `limit`（2026-09-22，接上「三参数里只有窗口值得动」的实测结论）**：全局 `resultLimit=20` 与 `factor=0.25` 都不动（factor 实测坐在平台区：0.42 才是 RN 开始赢过最低分普通页的分水岭，(0, 0.42] 内输出一字不差），改给调用方一个逃生口。schema `limit?: int 1..BEDROCK_RESULT_LIMIT_MAX`（上界**派生自候选池上限**而非新常量：池加宽窗口上界自动跟着走，且"声称能给 200"本身就是谎 —— 检索层每棵树只交 L0 10 + 语义 10）；handler 里候选池跟着放宽 `Math.max(60, resultLimit)`，截断仍在降权之后。新增可选回显 `demotion.requestedLimit / limitMax`，并在 `limit` 比池还宽时打一条 warning（防"调大没变多"被读成丢页）。真跑实测：默认 20 条（池 30、窗口内 RN 6、被降权挤到窗口外 10）→ `limit=60` 得 30 条（RN 16、窗口外 0），**前缀逐 id 相同**、默认集是放宽集子集、追加的 10 条正是被挤出去的 RN；`limit=0/61/2.5/"abc"` 一律 `errorKind:validation`。GENRE 门补判据⑥（真跑腿用真 handler 跑两次核前缀稳定性 / RN 只增不减 / 池≥窗口 / 超池必披露；另有 4 形 schema 腿），13 例新夹具。投毒走 dist：忽略调用方 limit、**截断点被整个跳过**、schema 去掉上界、删掉超池披露 ⇒ 各红在 ⑥；跑完按备份字节还原并复跑回绿。两条踩到的坑记在案：① 只核 `demotion.resultLimit` 回显会被骗（截断跳过后回显照旧、实吐 30 条）⇒ 必须同时核 `results.length`；② `Math.max(60, limit≤60)` 与常量 60 在当前取值域内是**等价变异**，期望它绿（哪天变红说明两个常数脱钩）。
- **基岩语料面三处缺陷（2026-09-21 A3 收口）**：① `@minecraft/server` d.ts 里 `System`(class) 与 `system`(const) 只差大小写，按名直写在 NTFS 上后者覆盖前者 ⇒ 丢的是两页 class 而 `index-l0` 仍登记 624 条；新增 `planScriptApiWrites`（碰撞者拿确定性后缀，与 `planClassWrites` 同法）+ `pruneIndexToDisk`（大小写敏感核对，只剪本命名空间且目录取不到时不动），实跑得 624 页 / 882 条索引 / 882 个正文。② **语义索引构建器只扫 `processed/` 顶层**：基岩新页在 `processed/documents/**` 与 `processed/scriptapi/**`，构建器报「20 docs」并以退出码 0 收工，把 44 倍覆盖缺口藏在一次「成功」里；页发现改递归（`version/rel` 优先命中，其余 59 棵树 flat==deep 故行为不变），并给 `indexLooksComplete` 补 `expectedDocs` —— 「语料指纹没变」不再等于「这份 db 覆盖了当前语料」。③ parchment 端点原先指向 `parchment-data`（实测 404），真实坐标是 `parchment-<mc>` 且版本串是日期 ⇒ 改为必带 `minecraftVersion` 并按「端点已分列」匹配。

## Plan 1 — validate_project / diagnose_gradle 返回值

- **权威字段**是 `status`（`"passed"` | `"failed"` | `"skipped"`）与 `skipped`。`passed` 仅在真正跑完检查且无 error 时为 `true`。
- `validate_project` 对 LiteLoader / Rift / ModLoader / 基岩早退：`status: "skipped"`、`passed: null`、`ok: true`。这不是工程损坏，而是本工具未跑检查。旧客户端勿把 skipped 当失败。
- `deprecated_legacy_passed: true` **仅**出现在 Plan 1 的 `validate_project` 早退（旧逻辑曾误报 `passed: true`）。`diagnose_gradle` 早退 **从不带** 此字段。
- **新测试、新 Agent 逻辑禁止把 `deprecated_legacy_passed === true` 当成功条件。** 成功只看 `status` / `skipped` / `passed`。
- Wave C 起删除该过渡字段（含仍 skipped 的 LiteLoader / Rift / ModLoader / 基岩）。
- `diagnose_gradle` 早退同样 `status: "skipped"` + `action`（改口）。`suggestions` 是检查建议，**不得当错误**；`action` **不得当「校验通过」**。新 Agent 只依赖 `status` / `skipped`。
- Review 提示：新增代码不得依赖 `deprecated_legacy_passed`。
