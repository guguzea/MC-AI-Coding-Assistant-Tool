# MC MCP Server

?? **stdio** MCP Server?? Cursor ? AI ???? Minecraft ???????Forge / Fabric / NeoForge??

**???Node.js >= 22.5**?Yarn ?????? `node:sqlite`??

??? GitHub Release **???? `node_modules`**???????????

```bash
cd mcp-server
node -v   # ?? v22.5+
npm ci
npm run build
```

## ????

- ? **31** ? MCP ???API ????????? Yarn SQLite ???????????Gradle ???DataGen???????????Forge/Fabric/NeoForge ??????????Mod ?????
- ???? `data/`?????????????????????parchment/mcp?yarn JSON ? + **yarn-mappings.sqlite?????**?porting ???
- ???????L0 ???? ? L1 ?? ? L2/L2+ ??
- **????**???????? `yarn-mappings.json`??? >1.5GB ??????????

---

## ????

### 1. ?? Node ?????

```bash
node -v
# ?????
node -e "const [maj,min]=process.versions.node.split('.').map(Number); if(maj<22||(maj===22&&min<5)){console.error('Need Node >=22.5');process.exit(1)} else console.log('OK',process.versions.node)"

cd mcp-server
npm ci
npm run build
# ? data ??? sqlite?npm run build:yarn-sqlite
```

### 2. ?? MCP ???

???? **????** + `MC_SKILL_DATA` ??????? `data/`?

```json
{
  "mcpServers": {
    "MC-AI-Coding-Assistant-Tool": {
      "command": "node",
      "args": ["H:/MC_skill/mcp-server/dist/index.js"],
      "env": {
        "MC_SKILL_DATA": "H:/MC_skill/data"
      }
    }
  }
}
```

????`port_project` ? `dryRun=false`??????

```json
"env": {
  "MC_SKILL_DATA": "H:/MC_skill/data",
  "MC_SKILL_ALLOW_WRITE": "1",
  "MC_SKILL_PROJECT_ROOT": "H:/path/to/your/mod"
}
```

`MC_SKILL_PROJECT_ROOT` **???????**?????????? `realpathSync.native` ???? Windows Junction / symlink ????

### 3. ?????

??????**?????????** Cursor????????? `MC-AI-Coding-Assistant-Tool`?? **31** ????

### 4. ????

| ?? | ?? | ?? |
|------|------|------|
| `MC_SKILL_DATA` | data ???????????? | `H:/MC_skill/data` |
| `MC_SKILL_ALLOW_WRITE` | `1` ??? port_project ?? | `1` |
| `MC_SKILL_PROJECT_ROOT` | ??????????? | `H:/mods/my-mod` |
| `MC_SKILL_STRICT` | `1` ???????????? | `1` |
| `MC_SKILL_DEBUG_PATHS` | `1` ?????? | `1` |

### 5. ????

```bash
npm run build
npm test
node dist/index.js
```

```bash
npx @modelcontextprotocol/inspector node dist/index.js
```

---

## ????????

| ?? | ???? |
|------|----------|
| API / ?? | `query_api`?`get_method_params`?`convert_mapping`?`get_version_info` |
| ?? | `diagnose_gradle`?`generate_datagen`?`crash_analyze`?`validate_project` |
| Forge ?? | `list_forge_versions`?`search_forge_docs`?`get_forge_doc_*` |
| Fabric ?? | `list_fabric_versions`?`search_fabric_docs`?`get_fabric_doc_*` |
| NeoForge ?? | `list_neoforge_versions`?`search_neoforge_docs`?`get_neoforge_doc_*`?`1.20.1` ?? Forge ??? |
| ???? | `list_doc_versions`?`search_docs`?`get_doc_*` |
| ?? / ?? | `analyze_porting_path`?`port_project`?`diagnose_data_paths` |

???????????? [README.md](../README.md) ??MCP ????????

---

## FAQ

**Q: NeoForge ????**  
A: ????????????`version=1.20.1` ??? Forge 1.20.1 ???API ??????????? `forgeCompatible`?

**Q: ????? node_modules?**  
A: ????????? `npm ci && npm run build`?

**Q: Yarn ?? OOM?**  
A: ?????????? `yarn-mappings.sqlite`??????? JSON?
