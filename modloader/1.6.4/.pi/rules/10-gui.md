---
description: 10 — GUI
---

# 10 — GUI

仅安全表内钩子：`ModLoader.setInGUIHook(BaseMod, boolean, boolean)`。

具体 `GuiScreen` / `GuiContainer` 方法以 MCP 1.6.4 named 为准，表外禁止输出。
禁止 1.16 `AbstractContainerScreen`。
