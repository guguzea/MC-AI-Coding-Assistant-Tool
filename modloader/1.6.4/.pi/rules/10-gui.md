---
description: 10 — GUI
---

# 10 — GUI

仅安全表内钩子：`ModLoader.setInGUIHook(BaseMod, boolean, boolean)`。

具体 `GuiScreen` / `GuiContainer` 方法以本档安全表 `knowledge/common/safe-api.md` 为准；表外一律 `TODO(未核实)`。（A-43 改写：原写「以 MCP 1.6.4 named 为准」——该 MCP 表不在仓内、不可达；与 :3「仅安全表内钩子」同口径。）
禁止 1.16 `AbstractContainerScreen`。
