# 贡献指南

感谢你愿意为 **MC AI Coding Assistant Tool** 贡献力量。本文说明如何扩展规则、数据与 MCP Server,CLI 。

---

## 模块总览

| 模块 | 路径 | 状态 | 适合贡献 |
|------|------|------|----------|
| Forge 规则 / Skills | `forge/<version>/` | 多版本已完成（主推 1.20.1） | 扩展规则、Skill、scaffold |
| Fabric 规则 / Skills | `fabric/<version>/` | 多版本已完成（主推 1.20.1 / 1.21.x） | 同上 + Fabric 专有 Skill |
| NeoForge 规则 | `neoforge/` | 已完成（主推 1.20.4+） | 扩展规则与知识 |
| Quilt 规则（QSL 差异） | `quilt/<version>/` | 只写 QSL 差异（02–10 读同版 fabric） | QSL 注册 / 事件 / 网络 |
| LiteLoader / Rift / ModLoader | `liteloader/` `rift/` `modloader/` | 短规则树（主推 1.12.2 / 1.13.2 / 1.6.4） | 短规则、verify 表 |
| 基岩 Add-On | `bedrock/` | 扁平目录（无版本档） | 规则、pack 校验、Script API |
| 库模组 Skill 源稿 | `knowledge/libs/` | 按组维护（`all-platforms` / `fabric-only` / …） | 新库 Skill、版本映射 |
| 社区实务知识 | `community_knowledge/` | 持续维护 | 发布 / 崩溃 / 软依赖 |
| MCP Server | `mcp-server/` | 以 list-tools / 实际注册为准 | 新工具、脚本、测试 |
| 离线数据 | `data/` | 8 平台多版本（档面以 `ls data/` 为准） | 抓取、索引、审计 |
| 根文档 | `README.md` / `AUTO_SETUP.md` / `AGENTS.md` / `CONTRIBUTING.md` | 持续维护 | 修正与同步 |

哪个平台有哪几档、哪条是主推，**以根 `README.md`「平台说明」表为准**，本节不重述；核对用 `ls -d <平台>/*/` 与 `ls -d data/*/`。

知识库与反模式通常位于 **各平台版本目录** 下的 `knowledge/`（不是仓库根目录）。

---

## 添加新平台或新版本

### 步骤 1：复制目录模板

以已完成的同平台版本为模板（Forge → `forge/1.20.1/`，Fabric → `fabric/1.20.1/`）：

```
平台/版本/
├── AGENTS.md
├── sync-skills.ps1          # 若该平台使用多 IDE 同步
├── .cursor/
│   ├── rules/               # 00–10 .mdc
│   ├── skills/
│   └── agents/
├── .claude/ / .continue/ / .trae/
├── .opencode/ / .agents/ / .zcode/ / .pi/   # OpenCode / Codex / ZCode / Pi（代表版本已支持）
├── scaffold/                # 可选：Gradle 骨架
├── code-patterns/           # 可选
└── knowledge/               # antipatterns / common / porting / version-changes
```

社区实务知识写在仓库根 `community_knowledge/`（不要双写进 `forge/*/knowledge/`）。库模组 Skill 源稿在 `knowledge/libs/`（见该目录 `README.md`）；社区用法见 `community_knowledge/AGENT_USAGE.md`。

### 步骤 2：改版本相关字段

- `AGENTS.md`：平台、MC 版本、Java、mappings、Decision Flow
- `scaffold/gradle.properties` / `build.gradle` / `fabric.mod.json` 或 `mods.toml`
- Skill frontmatter 中的 `platform` / `version` / `mappings`

### 步骤 3：同步多 IDE

修改 `.cursor/` 后：

```powershell
cd <平台>/<版本>
./sync-skills.ps1
```

或在仓库根目录批量同步全部版本（统一 8 IDE）：

```powershell
.\scripts\sync-skills.ps1 -All
.\scripts\sync-skills.ps1 -TargetDir .\forge\1.19.4
```

各版本下的 `sync-skills.ps1` 是对 `scripts/sync-skills.ps1` 的薄包装；逻辑以仓库根脚本为准。

### 步骤 4：数据与总览

1. 在 `data/` 下按 `<平台>_<版本>/` 约定建立目录（已建档的不止 Java 三平台：还有 `quilt_*` / `liteloader_*` / `modloader_*` / `rift_*` / `bedrock_stable` / `vanilla_*` 等；清单以 `ls data/` 为准）
2. 用 `mcp-server/scripts/` 抓取并生成 L0/L1/L2 + processed
3. 更新根 `README.md`「平台说明」与 `AGENTS.md` 路由
4. `cd mcp-server && set MC_SKILL_DATA=<data绝对路径> && npm run audit:data`

---

## 扩展现有规则

每个 `.mdc` 建议包含：

1. **约束（Constraints）**
2. **Decision Flow**
3. **示例代码**（使用该版本正确的 mappings）

Decision Flow 格式：

```text
### Decision: 场景描述

IF 条件 A
  → 方案 A

IF 条件 B
  → 方案 B

ELSE
  → 默认或询问用户
```

反模式写在 `knowledge/antipatterns/` 或 `09-anti-patterns.mdc`，条目需含：错误写法、症状、正确方案、原因。

---

## 添加新 Skill

目录：`平台/版本/.cursor/skills/<skill-name>/`

```
mc-block/
├── SKILL.md          # 必须
├── snippets/         # 可选
└── README.md         # 可选
```

`SKILL.md` 顶部元数据示例：

```yaml
---
platform: forge         # forge / fabric / neoforge
version: "1.20.1"
dependencies: []
mappings: mcp           # mcp / yarn / parchment
---
```

创建或修改后必须跑 `sync-skills.ps1`。

Fabric 可额外贡献平台专有 Skill（如 Fabric API、Kotlin、Cloth Config）。

---

## 官方文档与映射数据

### 数据布局（示意）

```
data/
├── forge_1.20.1/
│   ├── forge-docs/<ver>/{raw,processed,index-l0.json,index-l1.json,index-l2.json}
│   ├── mappings/
│   └── extracted/
├── fabric_1.20.1/
│   ├── fabric-docs/<ver>/...
│   ├── fabric-wiki/<ver>/...
│   ├── mappings/          # yarn-*.jar/tiny、yarn-mappings.json、yarn-mappings.sqlite、parchment*、yarn-tiny-provenance.json、upstream/*.bak
│   └── meta.json
└── neoforge_*/
    └── neoforge-docs/...
```

fabric 档 `mappings/` 里的 `yarn-tiny-provenance.json` 记录 tiny named 列修复的溯源（上游 v1 备份的 sha256 与修复后 tiny 的 sha256），`upstream/*.bak` 是修复前的上游 v1 原件备份；两者由 `assert-yarn-named-integrity.mjs` 逐档存在性检查 + sha 双向对账，不许删。

文档仍遵循 **L0 → L1 → L2 → L2+（processed）** 分层，不可跳层。

### 常用脚本（均在 `mcp-server/scripts/`）

| 方向 | 示例脚本 |
|------|----------|
| Forge 文档 | `fetch-forge-docs.js`、`process-forge-docs.js` |
| Fabric Docs / Wiki / Meta / Mappings | `fetch-fabric-*.js`、`process-fabric-*.js` |
| Parchment / API 提取 | `parchment-extractor.js` 等 |
| Yarn SQLite | `npm run build:yarn-sqlite`（推荐；运行时禁止全量读 JSON） |
| 一致性审计 | `npm run audit:data` |

表内是示例不是全集；**脚本是否存在以磁盘为准**（`ls mcp-server/scripts/`）。

### 贡献注意

- **不要**把 `agent-tools/`、`临时文件*.md`、本地 audit 输出提交进仓库（见 `.gitignore`）
- 根 `.gitignore` 的 `/.cursor/` **只**排除仓库根 IDE 目录。平台包 `fabric/<ver>/.cursor/rules` 等必须入库（`activate_platform_pack session` 只从这里注入规则）。**不要**改回 `.cursor/`，否则新档规则对 clone 不可见、`rules[]` 为空却仍提示「已加载底座 00/01/09」
- **不要**假设 `mcp-server/data/` 是运行时路径；MCP 读取的是 `MC_SKILL_DATA` 指向的仓库根 `data/`
- 大体积 `*.jar` / `*.zip` 的忽略规则见根 `.gitignore`；完整包走 Release artifact + `SHA256SUMS` + `data-manifest.json`
- Redistribute `data/` 时附带 [`THIRD_PARTY_NOTICES.md`](./THIRD_PARTY_NOTICES.md)
- **新增仓库顶层目录 / 顶层模块时**：交付汇报的「受管面」是根 `AGENTS.md` §交付汇报 里**手列的清单**（现为 8 个平台树 + `data/` / `knowledge/` / `community_knowledge/` / `mcp-server/` / `scripts/` / `agent-tools/` / `openspec/` / `ralphy-spec/` + 根 `*.md`）。**新目录落地必须同步补进那份清单**（清单以那份为准，本节不复述），否则改动它的那一轮会被判成主档、丢掉维护档的台账与门验证汇报。核对方法：把仓库顶层 `ls`（`Get-ChildItem -Directory`）结果与那份清单逐个比对。

---

## 数据链口径（官方标准，2026-09-12 裁定）

本节把 `temp/PLAN-2026-09-08-销账-*.md` 里逐条登记的**口径偏离**固化为标准。口径与数字冲突时，**以本节 + 门禁实算为准**，旧台账只作历史。

### 转引标签怎么读（`<<<` 与 `@[code`）

processed 正文里的这两行都是**转引标记**，不是可执行代码；读取期由 `mcp-server/src/docs-platform/fabric/transclude.ts` 展开成代码块。`<<<` 的完整形态：

```
<<< @/相对路径[#区段名][{行选}][[标签页标题]] [其余 attrs]
```

| 构件 | 读法 | 实测处数（26.1.2 + 1.21.x） |
|------|------|------|
| `#区段名` | 上游 VitePress 区段导入。标记行是 `// #region 名` / `// #endregion 名`（注释前缀随语言变），**首尾标记行不计入正文** | 521 |
| 无 `#` | 整份文件引用（含 `@/public/…`、`@/.github/…`） | 112 |
| `[[Label]]` 或紧跟的 `[Label]` | **标签页标题**，既不是路径也不是区段名；从两者剥掉，只留在出处注释 | 13 |
| `{5-7}` / `{2}` / `{1,3}` | 区段内（无区段则全文）**1-based 闭区间**行选，允许逗号并列 | 15 |
| `{classtweaker:no-line-numbers}` 等非纯数字花括号 | **选项**，一律忽略并整段给出（当成行号会让整个区段消失） | 3 |
| 语料写 kebab 而 blob 标 snake_case | `regionCandidates()` 先**逐字**匹配、再试 `-`↔`_` 变体；逐字命中 500 处 ⇒ 变体是兜底不是主路径 | 6 |

- **判据只有一份**：`parseAngleSpec()` / `angleResolve()` / `pickLines()` 由 `transclude.ts` 导出，G3 门 `import` 复用。**禁止**在门或探针里另写一份「区段名在不在」的正则——曾经分叉过一次，后果是同一页读者展开成功、门算成「未取件」并跳过核对（13 处）。
- 读者放行条件**同时认两种占位符**（`@[code` 或行首 `<<<`）。只认 `@[code` 会让「1.21.4+ 与 26.x 只用 `<<<`」的整页原样吐占位符。

### 计数器分工（禁止混用分母）

| 字段 | 口径 |
|------|------|
| `sites` / `expanded` / `missing` | **只算 `@[code`**。既有台账按此钉：`sites` 3344 处标记行、全局唯一目标 669 个（`assert-fabric-transcludes.mjs` 的 `EXPECTED_UNIQUE_TARGETS`）、processed 全展开 1672 篇 |
| `angleSites` / `angleMissing` / `angleRegionMiss` | `<<<` 独立字段：标记行数 / 本地镜像取不到的目标 / 目标取到但区段对不上 |
| 全部占位符 | `sites + angleSites`。判断「本页是否还有未展开残留」用 `hasUnexpandedMarker()`，它认两种形态 |
| 展开正确性归属 | `@[code` 归 `assert-fabric-transcludes.mjs`，`<<<` 归 G3 `assert-corpus-faithfulness.mjs`。**两套不重叠**，谁也别补谁 |

- 任何计数**必须写明分母口径**。中介名一例：S7 时点为 355 行命中 / 979 token 出现（一行两个名字记 1 行），今日盘面门重算后为 **288 行命中**——两个分母都对，只有钉住一个才有回归意义 ⇒ 门内注释与台账字段名都带「行命中」字样。
- 仓库级计数**整体排除 `temp/**`**（不做点名式排除：temp 下同形副本 ≥3 份，且卷抖动会让 `statSync` 结果漂）。

### 台账、豁免与债务

- **数字只能由门自己重算**：G3 `MC_SKILL_CORPUS_RELEDGER=1`、G4 `MC_SKILL_INDEX_RELEDGER=1` 转储后回填；G1/G2 的 LEDGER 常量取门成功行打印的实测值。**文档不重述会腐烂的计数**，只述口径。
- **台账外新增即红；台账条目停止复现也即红**（drain check，G1/G3/G4 均已实现双向对账）。债务清单**清空而不删除**：留空数组 = 零容忍，复发才响亮。
- 门禁**一律不删文件**。`data/` 下的残留（`db.sqlite.old` / `tmp-*`）只钉进 `DEBT_RESIDUE` 并出待删清单，删除动作归数据拥有者。
- 每个门禁条款必须**可被点名的投毒红**（`mcp-server/test-scripts.mjs` 内逐条 poison 已在册）。

### 验证纪律（2026-09-15 追加；三条都来自实测事故，不是预防性条款）

- **改了 `mcp-server/scripts/**` 或 `scripts/**`，收口必须跑第 8 步**：`cd mcp-server && node test-scripts.mjs`。
  单列的理由：`package.json` 的 `npm test` 链很长，一轮里往往只抽跑其中几道门（只跑
  `assert-lib-ownership` / `lint-skill-verified-api` / `assert-scripts-parse` 这类）。2026-09-15 实测到
  这样一轮「抽跑」之后 `test-scripts.mjs` **本身是红的**（生产者 `scripts/build-api-summaries.mjs`
  被改写、harness 里的投毒锚点没跟着同步），而那一轮销账只登记了跑过的那 5 道门 ⇒ 台账上看不出链在冒烟。
  第 8 步是门禁总入口（`mcp-server/scripts/assert-*.mjs` 串链 + G1–G4 全量 + 假根投毒），**它绿才叫「链上无已知红」**。
  道数不写死（会随迭代增长），以 `ls mcp-server/scripts/assert-*.mjs` 为准。
- **harness 里硬钉的文本锚点与计数必须随批次同步，且只许「先对齐生产侧、再改 harness」**。
  `mcp-server/test-scripts.mjs` 用硬值做**第二道独立钉**（例：`已证实包根 47`、投毒替换用的源码原文片段），
  这是刻意的双机制，不是冗余。代价是生产侧改写法/改口径后 harness 会**当场断言失败**而非静默失效 ——
  这是设计意图，**不要靠删断言让它变绿**。同步时在注释里写清「改了什么 / 为什么这不是放松」，并与
  被钉的那道门自己的 `LEDGER` 常量对齐（对齐不上说明真漂移，该改的是门台账而不是 harness）。
- **`npm test` 不能与语料/文档抓取并发跑**。`test-cli.mjs` 的 4000 ms lag 门与磁盘负载耦合
  （实测：一边跑 forge javadoc 抓取一边跑全链，`convert_mapping` 三次都在 4000 ms 内无 stdout ⇒ 假红）。
  终局验收与任何全链跑**必须独占该卷**。
- **引用门名以磁盘为准**，别照抄台账里的名字：`assert-skill-raw-normalize.mjs` 从来不存在
  （`git log --all --diff-filter=AD` 零命中），真正的门是已在链上的 `assert-sync-normalizers.mjs`。
  清单看 `ls mcp-server/scripts/assert-*.mjs`。
- **重建 `build-api-summaries.mjs` 的产物时必须显式给足 `--max-*`**：脚本默认
  `maxVersions=40 / maxClasses=500 / maxMethods=2000`，而**在库的产物是用远高于默认的上限生成的**。
  照默认重跑 = **静默劣化**（实测 `kotlin-for-forge`：给足前 类 505 / 丢 7 版 / 上限跳过 9 版；
  给足后 类 699 / 零截断）。判据：产物里的 `truncated` / `skippedVersions` / `droppedVersions` 三个键
  **出现即说明这次重建不完整**，不许拿它覆盖旧产物。KFF 那轮用的下限见
  `assert-lib-ownership.mjs` 的 `LEDGER` 注释。

### 语料忠实性不变量

- raw ↔ processed 的不变量是**逐树「篇数相等」+ 变换类别台账**（`identical` / `contentDiff` / `markerOnly` / `fmOnly` / `noTwin`），**不是** 1:1 同名配对——后者会造出 19747 处假缺失（真实 `noTwin` 19565）。
- neoforge 侧更强：`processed == stripFrontmatter(raw)` 逐字节成立。
- 泛型丢失判据 = 先解 `&lt;` / `&gt;` 实体，再用 `\b[A-Z]\w*<(?!\/)[^>\n]*>` 取**多重集**比对 raw→processed（同数改写也算未存活）。
- 上游镜像的前端元数据（front matter）**不改**：语料层字节忠实优先于本地偏好。

### jar 身份与 `packages` 归属

- 反编译产物目录身份段 = **jar 字节内容的 sha512 前 12 位**（不读文件名、不读元数据）。
- 取消 `?? "unknown-mod"` 回落：解不出 modId = 结构化失败 `MOD_ID_UNKNOWN`，**永不**坍缩进共享目录。
- 身份优先级：① jar 自己的元数据 → ② **它自己声明的内层 jar**（`META-INF/jars|jarjar/*.jar`）→ ③ 调用方给的标签（必须被该 jar 自身条目路径证实）。结果行的 `modIdEvidence` ∈ `jar` / `jarjar-self` / `jarjar-labeled` / `external`。外部证据通道**只供内部批处理器**，不进 MCP 工具 schema。
- 同 `modId` + 同 `version` 的多个 jar 是合法常态（JiJ / `.supp` / fork / repack）⇒ 按内容分叶，第二个 jar 应当**成功**。
- `meta.modId` 为字符串 `"null"` 是合法 id；只有 JSON null / 空 / `unknown*` 判未知。
- 摘要与 catalog 的 `packages` 登记**实测包**，不是声明白名单的回声：声明前缀先按本树校验，全不成立则按「modId 是路径一段（`-`/`_` 不敏感）」重建，两者都不成立时**留空并告警**，禁止退化成全收（`-all` 胖 jar 会把 Kotlin stdlib 当成本库 API）。归属判据是**段级自有**（`ROOT_SEGMENTS=3`）且只拒「命中他方已证实包根」；字面「以 modId 开头」会否掉 92.7% 的真数据。
- `packages` 是**启发式产物，不可当 import 依据**。
- `verifiedApi[key].packageOwnership` 三态 ∈ `own` / `bundled` / `unresolved`，由 `merge-verified-api.mjs` 的
  `tagPackages()` 生产（2026-09-13 裁定固化为标准）：
  - **只有非 `own` 行必须有标签**。按字面要求「每行都要有标签」在今天是 no-op（`merge --write` 新增 0 / 覆盖 0 ⇒
    目录里 0 行带标签），要先做一遍全量 retag 才满足；而 `own` 本就能由规则当场推出，强制存标签**不增加任何检出力**。
  - **债务只算「该包根确实由别的条目证实自有」那一类**（F113 的实际伤害）。纯用「含 modId 段」当债务判据会把 **866 行**
    真数据打成非 own（GeckoLib 真身 `software.bernie`、KubeJS `dev.latvian.mods`，库名根本不在包里），反而把 8 行真冒领冲掉；
    其余非 own 行靠标签可见、不记债。
  - `bundled` 必须带证据：只认**外壳自己声明的捆绑件** + 该件 zip 条目里真实的顶层包根，不猜包名 ⇒ Moonlight 早期版本的
    `net.mehvahdjukaar.selene` 会正确落 `unresolved` 而不是被冒认成 own。
  - A6 除「新增即红」外还要过 drain 对账：catalog 的 unresolved 行集与台账**双向**比，清一行必须显式删一行。

### 映射与版本口径

- 未具名映射的口径 = `name_named == name_intermediary`；G4 A7 逐档钉 `classesNamed` / `unresolvedMethods` / `unresolvedFields`。yarn 覆盖率本身 30–36%、`field_*` 与 forge SRG/TSRG era 的 `classes named == intermediary` 都属**上游事实**，不是导入缺陷。
- 成员数解析顺序一律 `meta` → `methods` → `searge_*`（**取用，不求和**），门与读者共用同一顺序。
- Forge 依赖坐标 = maven **recommended** build，并带 `forgeVersionSource` 溯源字段。
- 审计前提在磁盘上不可复现时（如已不存在的 JSONL 计数），**既不沿用为基线、也不判审计为假**：验收改用磁盘可复现的固定样本集，并把前后计数并排给出。
- 吞异常裁定判据：折叠成 `success` 的必须修真缺陷；落成可见 `failed` 的带证据关闭——**不豁免、不转挂下一档**。
- **`yarn-mappings.sqlite` 是可再生物，不是孤本**：源 `yarn-*-tiny.gz` 与它同目录且已跟踪 ⇒ 离线逐档重建即可，无需网络：
  `node mcp-server/scripts/_lib/build-yarn-sqlite.mjs data/fabric_<ver>/mappings --version=<ver>`（仓库根直接可跑：脚本参数走 `path.resolve`，根与 mcp-server 两种 cwd 都指到同一目录）。重建会把 `schemaVersion` 往前带
  （实测 3 → 4，只多 `name_official`/`name_intermediary` 单列索引），属正常迁移 ⇒ 台账按门的 `MC_SKILL_INDEX_RELEDGER=1`
  重算回填，**不许手改数字**；回填前先比对「变化是否只有 schema 那几项」，多一项就说明不是迁移而是数据变了。
- **`yarn-*-tiny.gz` 的 named 列是修复产物，不是上游原件**：2026-09-19 起按上游 v2 jar 逐行重写（v1 原件备份与 sha 溯源在
  各档 `mappings/yarn-tiny-provenance.json` + `mappings/upstream/*.bak`）。从 maven 重拉上游 v1 会把 named 打回
  `named==intermediary`，立即命中 `assert-yarn-named-integrity` 的降级判定；自愈路径 = 先重建 named（新管线
  `scripts/_lib/repair-yarn-named.mjs` / `npm run build:yarn-named`），再按上一条重建 sqlite。
- **损坏可以长得像「表存在、但 `COUNT(*)` 抛错」**（单个 b-tree 页坏），而不是「表不存在」；因此门的 `-1` 哨兵必须继续与
  「缺表」区分开上报。判定「坏在提交之前还是拷贝造成」的唯一办法是**在两个独立副本上跑同一条查询对比**：2026-09-13 实测
  `1.21.8`/`1.21.10` 的 `methods`、`1.21.11` 的 `fields` 在故障卷与其抢救副本上报错逐字一致 ⇒ 已提交字节本身坏，与拷贝无关；
  重建后每档 `methods`/`fields`/`classes` 与自身 `meta` 全部相符（46592 / 48591 / 49730 等），即内容未变、仅页坏。

---

## MCP Server 贡献

### 技术栈

- TypeScript、Node.js **>= 22.5**（**22.5–22.12 与 23.0–23.3 需 `--experimental-sqlite`**；22.13+ / 23.4+ 免）
- `@modelcontextprotocol/sdk` + `zod`
- 包名 / Cursor 服务名：`mc-ai-coding-assistant-tool` / **`MC-AI-Coding-Assistant-Tool`**

### 源码结构（摘要）

```
mcp-server/
├── src/
│   ├── index.ts                 # 入口，注册核心工具
│   ├── wave/register.ts         # Wave B/C 扩展工具
│   ├── api/                     # query_api、get_method_params、get_version_info
│   ├── mappings/                # convert_mapping + yarn-sqlite
│   ├── docs-platform/
│   │   ├── forge/ | fabric/ | neoforge/
│   │   └── store.ts             # 共享文档存储抽象
│   ├── porting/                 # analyze_porting_path、port_project
│   ├── datagen/ | crash/ | validate/ | gradle/
│   ├── utils/                   # path、project-sandbox
│   └── workers/
├── scripts/                     # 抓取 / 处理 / 审计 / yarn sqlite
├── test-*.mjs
└── dist/                        # npm run build（git 忽略）
```

### 添加新工具

1. 在对应模块实现并导出
2. 在 `src/index.ts` 或 `src/wave/register.ts` 注册
3. 补充测试（`npm test`）与 `assert-no-yarn-json-slurp` 相关约束（若触及 Yarn）
4. 更新 `README.md`、`AUTO_SETUP.md`、`mcp-server/README.md` 的工具列表
5. 用户字符串当 `Record` 键时必须用 `ownGet`（`src/utils/own-record.ts`），禁止 `obj[query]`。`constructor` / `toString` 曾让 `search_*_docs` 崩溃、让 `get_version_info` 把 Function 当版本。工具边界与版本矩阵见根 README「按版本选工具」与「工具陷阱」。

### 本地开发命令

```bash
cd mcp-server
npm ci
npm run build
npm run build:yarn-sqlite
set MC_SKILL_DATA=<仓库>/data
npm test
npm run audit:data
npm run smoke:release   # 可选
```

写盘类工具默认关闭；测试 `port_project` 真写时才设置 `MC_SKILL_ALLOW_WRITE` + `MC_SKILL_PROJECT_ROOT`。

---

## Commit 规范

使用中文 commit message，例如：

```
feat(forge/1.20.1): 添加方块实体注册规则
fix(mcp-server): 修正 Yarn sqlite 路径解析
docs(AUTO_SETUP): 同步工具清单与配置草稿流程
chore(data): 忽略临时 plan 文件
docs(fabric/1.21.1): 补充 mixin 反模式
```

---

## 未排期清单（as-of 2026-09-24）

这张表登记的是**已发现、已登记、无人欠做但需要拍板或排期**的遗留项，**不是 TODO**：

- `现状` 列只回答「本轮实测到了什么（分母 + 口径 + as-of）」，测不了的就明写 `未复核`——它**不构成**「该项已修」的声称，也不构成「上游/本仓没有」的断言。
- `需要谁拍板` 列点出归属：写「用户」的不得由维护侧代做；写「排期」的在有人写判据之前**不受任何门保护**，下一个人仍会重做一遍。
- 与台账的分工：`temp/ralph-20260922/PLAN-STATE-r2.md`（逐轮 R 行）记「发生了什么」，本表记「还欠谁一个决定」。台账历史行不回改，故本表是状态的**唯一现行读法**。
- 与 `openspec/` 的关系一句话点名：`openspec/changes|specs|archive` 目录**在盘**，但根 `.gitignore:76` 写的是 `openspec/` ⇒ 整棵树 0 文件受纳管，而根 `AGENTS.md:410` 把 `openspec/**` 列为受管面；该不对称已由 2026-09-24 裁定**本轮不修**（不改 `.gitignore`、不为 `openspec/` 解封、不新建第二份清单文件），故在此留痕。

| 条目 | 现状（带分母 + 口径 + as-of） | 需要谁拍板 |
|---|---|---|
| `L1` `platforms` 点名某 loader 却无窗口 ⇒ 该组落回 union（反向不对称） | **本轮实测**（rc=0）：`mcp-server/scripts/assert-lib-session-resolve-parity.mjs` 判据④b 打印「无窗口可判组 **6**（union 判 2 + 开放形不判 4 + 快照无纯点分 release 不判 0）」/ ④c 写了该键却零窗口的源稿 **0** 份 / ④d 正向不对称 **0** 组。分母：扫 36 份 `SKILL.md`、其中 9 份声明 `mcVersionsByPlatform`、27 组窗口口径、地板 30/1/4/8/12。as-of 2026-09-24 | 维护者裁「6 组是否升判红」；逐库补窗口或删点名属**可代劳但要解释**（口径见根 `AGENTS.md` 库 Skill 节） |
| `L2` 「同面两名并存」（同一面既当处方又被禁）无门覆盖 | **本轮实测**：`assert-rule-ban-vs-example.mjs` 是**表驱动回归守卫**，自述「表内 **5** 对 / 判 **3** 档 / 被扫文件 **19**（登记表去重实际读到的件数）/ 拒 0；地板 文件≥12 且 活禁令≥3」。**B7 收编（2026-09-25）**：`FLUIDTYPES` 形已进表（`forge-1182-fluidtypes-ban` 对，禁令锚 = `forge/1.18.2/.cursor/skills/mc-fluid/SKILL.md:22`，shape 命中 `FLUIDTYPES` 与 `FLUID_TYPES` 两个键名写法、判据源 = L6 已闭环裁定）⇒ 该形状回归时门会红，不再只靠人记。其 `--discover` 只读普查报「交叉候选 3301 条」且明写**不参与 rc** ⇒ 候选数**不是**覆盖率证据，仍须人工裁定后才进 `PAIRS`。as-of 2026-09-25 | 无人欠做；要再扩表需用户排期（新腿须配投毒红 + 不判对照 + 覆盖下界，否则又是装饰） |
| `L3` A-27 第二跳是**纯行号锚**（`isError` 位点漂移链） | **本轮实测在盘**：`mcp-server/src/utils/actionable.ts:25` 漂移链仍是 `424→429→444→454→516→518`，`:28` 自述「这条位点是纯行号锚，任何在 tool-registry.ts 上方增行的改动都要跟着改这里」⇒ 脆性写在散文里、**无机制**每轮提醒 | 用户/判据作者：改成「代码片段 needle + 行号仅报告」= **改判据**，须批准 |
| `L4` `mc-terrablender` 的 `:11` 与 `:13-16` 口径冲突 | **本轮实测**：`knowledge/libs/all-platforms/mc-terrablender/SKILL.md:11` 那句 2026-09-14 现拉 Modrinth「三端近100内无 release」仍在；`:13-16` 已按构件快照（`mcp-server/data/lib-manifests/all.json`，as-of 2026-09-16）收口窗口口径，`:15` 自标「`:11` 未复核」。⇒ 窗口口径失配**已修**，剩的是 `:11` 该句本身 | 用户：是否授权再拉一次 Modrinth（联网取证）。不授权则该句永久挂 `未核实` |
| `L5` 社区短文里的外链是否 404 | **已闭环**（第 29 轮，2026-09-24 联网取证）：`saveddata-world-persistence.md:113` 的原写法 `docs.neoforged.net/1.20.4/docs/datastorage/saveddata/` 实测 **404** ⇒ 已改为 `docs.neoforged.net/docs/1.20.4/datastorage/saveddata/`（**200**）；latest `…/docs/datastorage/saveddata/` = **200**（本来就对）。⚠️ 同轮证伪了 `AGENT_USAGE.md`「URL 形态判据台账」里**本仓自己上一轮写的**判据（那句「两套形状都合法：版本存档 = `/<版本>/docs/<路径>`」是用形状推断代替取证的产物）——该行已改写并**留痕不删**。⇒ 结论：**NeoForge 版本存档的唯一合法形状 = `docs.neoforged.net/docs/<版本>/<路径>`**，`/<版本>/docs/…` 是 404。**复核命令**：`curl.exe -s -o /dev/null --ssl-no-revoke -w "%{http_code}" -L --max-time 25 <URL>` 三条各跑一次；同形状残留扫 `grep -rn "neoforged.net/[0-9]" community_knowledge/`（as-of 2026-09-24 实测 = **1 处**，且那处是 `:113` 行内记录「原写法 404」的那句话本身、不是活链） | 无（已闭环）。其余 `community_knowledge/**` 外链的逐条取证仍在 `L31` 未做面上 |
| `L6` `FLUIDTYPES` / `FLUID_TYPES` / `FLUIDS` 三档出处 | **已闭环**（2026-09-24 一手取证，第 24 轮落地）：Forge 1.18.2 **没有**流体类型注册表，`FLUIDTYPES` 与 `FLUID_TYPES` 都不存在，流体注册表 = `ForgeRegistries.FLUIDS`；由两个 build、两种独立机制互证（40.1.80 `sources.jar` 逐字 + 40.3.12 `javap -p` 的 **32 个**注册表字段全分母）。**复核命令**：① `unzip -p "<gradle 缓存>/…/forge-1.18.2-40.1.80-sources.jar" net/minecraftforge/registries/ForgeRegistries.java` ② `javap -p -classpath <forge-1.18.2-40.3.12-universal.jar> net.minecraftforge.registries.ForgeRegistries`，两条与 sha256 均记在 `temp/ralph-20260922/evidence-forge-1182-registries.md`。⚠️ 该证据文件与两个 jar 都在 `temp/` / 用户机本地 ⇒ **不在库内可复核** | 无（已裁定）。若要证据入库需用户另行授权 |
| `L7` 其余 Forge 档的 `ForgeRegistries` 字段名未扫 | **已闭环**（2026-09-25 全 16 档一手取证）：12 档 `universal.jar` 逐档 `javap -p` 单类（maven 最新；1.13.2–1.16.5 按 Gradle 缓存同版本号 25.0.223/28.2.26/31.2.50/36.2.34 补下——缓存里这四档只有 `-launcher.jar`，**不含**该类，判据只认 universal）。分母与全字段清单 = `mcp-server/docs/evidence-forge-registries-all-versions.md`；原始 javap 产物 = `D:\mc-skill-temp\l7-registries\<ver>__<类>.txt`（用户机本地，**不入库**，同 L6 警告）。要点：1.7.10/1.8.9 **无** ForgeRegistries（用 `cpw.mods.fml.common.registry.GameData`）；1.9.4–1.12.2 的 ForgeRegistries 在 `net.minecraftforge.fml.common.registry` 包；**1.19 是键名分水岭**（7 对改名：ENTITIES→ENTITY_TYPES 等，见证据文档 §跨档要点），跨 1.19 的 registry 技能不得平推字段名；1.21.1 首次无 ENCHANTMENTS。as-of 2026-09-25 | 无（已闭环）。若证据入库需用户 `git add` `mcp-server/docs/evidence-forge-registries-all-versions.md`（同 L10） |
| `L8` `mcp-server/README.md` 的过期聚合计数 | **已修（本轮）**：`:305` 与 `:322` 的「当前 **45 个**」→ **48**。两口径**相等**才落笔：`src/prompts/templates.ts` 顶层键手数 = 48，编译件 `dist/prompts/templates.js` 的 `Object.keys(WORKFLOW_TEMPLATES).length` = 48，且两集合逐名相同（only-in-dist / only-in-src 均 0）。⚠️ `dist/` 不入库 ⇒ 可复算口径是 **src 那一份**。as-of 2026-09-24。本轮未发现任何门钉 45（`assert-lib-census-counts.mjs` 判面是 lib 侧，不含 README 工具表） | 不需用户 |
| `L9` `query_upstream_releases` 不回 `versionType` | **本轮实测复算**：`versionType` 在 `mcp-server/src/` 全树 grep 命中 **1 处** = `src/diagnostics/index.ts:505`（`lib-manifests` 缓存的 TS 类型），**不在** upstream-releases 响应形状里 ⇒ `L4` 的 release/beta 之辨因此证不了 | 用户/排期：改 `src/**` ⇒ 必须 `npm run build` + 重载宿主 MCP |
| `L10` 用户侧欠 `git add` 的未纳管件 ⇒ **不是整洁问题，是可复现性问题：干净 clone 会在引用位点直接断链**（第 40 轮整行改写） | **第 40 轮现扫**（as-of 2026-09-25，口径 = `git ls-files --others --exclude-standard` 逐行计数，**不是**旧口径 `git status --porcelain -uall` 的状态字段过滤）：未纳管 **10** 件，其中 `mcp-server/scripts/` 下 **8** 件 `.mjs`（`_lib/bedrock-corpus.test.mjs` · `assert-backlog-table-shape.mjs` · `assert-doc-absence-claims.mjs` · `assert-lib-session-resolve-parity.mjs` · `assert-rule-ban-vs-example.mjs` · `assert-scaffold-mixin-shape.mjs` · `assert-test-chain-coverage.mjs` · `build-yarn-mojmap-pairs.mjs`）+ `mcp-server/docs/` 2 份 `.md`（`knowledge-coverage-sweep-20260924.md`、`mcmap-linkie-absorption.md`）。⇒ **7 件被受纳管 runner 引用**（口径 = `git grep -c` 只在受纳管的 `mcp-server/test-scripts.mjs` 与 `mcp-server/package.json` 两个 runner 里数命中，本轮实测）：`test-scripts.mjs` 引用 6 道门 = `assert-backlog-table-shape`(2 处) / `assert-doc-absence-claims`(2) / `assert-lib-session-resolve-parity`(2) / `assert-rule-ban-vs-example`(2) / `assert-scaffold-mixin-shape`(2) / `assert-test-chain-coverage`(3)，另 `_lib/bedrock-corpus.test.mjs` 由 `test-scripts.mjs`(1) 与 `package.json`(1) **共同引用**；只有 `build-yarn-mojmap-pairs.mjs` 两 runner 均 **0 命中**（盘上存在、`git ls-files` 查无）。**后果要写明**：干净 clone 拿不到这 8 件 ⇒ `node test-scripts.mjs` 与 `npm test` 会**在引用位点直接失败**（spawn 与 `node --test` 打不开不存在的文件，不是「少跑几道门仍算绿」）；且 `assert-scripts-parse.mjs` 的扫描面由 `trackedFiles()` = `git ls-files -z -- "*.mjs" "*.js" "*.cjs"` 决定（本轮实测该文件 `:13`/`:16`/`:38` 三行自述）⇒ 未纳管的门**对语法门不可见**，它的判据失步没人报警。计数历史：第 25 轮 5 件、第 36 轮 7 件、本轮 10 件（面一直在变）⇒ **引用本行任何数字前先现扫**。⚠️ **2026-09-25 第二遍现扫**：`git status --porcelain` = **14 路径 / 17 文件**——在第 40 轮 10 件之外新增 4 件：`assert-quilt-unversioned-wiki.mjs`（`test-scripts.mjs` 引用 ×2）、`assert-cross-layer-names.mjs`（**新门**，`test-scripts.mjs` 引用 ×2）、`docs/evidence-forge-1182-registries.md`、以及 **4 件数据** `data/forge_1.16.5/mappings/{methods,fields,params}.csv + mcp_snapshot-20210309.provenance.json`（**被 `assert-cross-layer-names.mjs` 腿 E 消费，且该门已把「在盘未 add」做成每次可见的 WARN ⇒ 不 add 则 clean clone 上腿 E 静默失效**）。 | 用户：`git add` 这 8 件 `.mjs` + 2 份 `mcp-server/docs/*.md`（**提交由用户执行，agent 不 `git add`/commit/push**）。维护侧无可代劳：纳管是 git 索引动作，且「禁动 `.gitignore`」已裁定 ⇒ 只能等人。 |
| `L11` `mcp-server/package.json` 第 8 轮 `node --test` 那行留/撤 | **只读确认现状**（本轮禁改该文件）：`test` 链 **62** 支（口径：按 ` && ` 切分现算），第 **16** 支 = `node --test scripts/_lib/*.test.mjs`（列 11 个文件，含 `L10` 那件未纳管的 `bedrock-corpus.test.mjs`）。as-of 2026-09-24 | 用户：留（则必须先 `git add`）/ 撤（则 11 个 `_lib` 测试退出默认链） |
| `L12` 「两项待裁」 | **已答**（2026-09-24 三问全答）：S21 t1 = 选项 (a)、S26 落点 = 本节、`FLUIDTYPES` = 自备 jar 坐实（→ `L6`）。⚠️ 点名一处计数错：第 22 轮写单把「**三**项待裁」写成「两项」（台账 R119⑦/R120⑧ 实为三项），该误写已由第 22 轮报告 `temp/ralph-20260922/round-22-final-sweep.md` §4 块 5 第 5 条登记 | 无 |
| `L13` 「可达 ≠ 真跑」只靠人读打印语句 | **已由第 26 轮接成机制**（旧欠账「打印并断言四数」已实现）：`mcp-server/test-scripts.mjs` §S4 现算并打印「门链四数」行 = 盘上 K / 可达 P / §S18-S19 真跑 N / W1-2 selftest M。口径：`K` = `readdirSync(mcp-server/scripts)` 的 `assert-*.mjs` 全量；`P` = 名字被任一 `test-*.mjs` 或 `package.json` **引用**的数量（旧那句「全部可达」即此，仍**不是**被执行）；`N` / `M` = 两条数组 `REAL_RUN_GATES` / `SELFTEST_GATES` 的**去重长度**（数组已提到模块作用域、成为唯一真值源，普查只读其 `length`，不再抄第二份名单）。断言 `P === K` ＋ `N ≥ 30` ＋ `M ≥ 15`（**下界、非等式棘轮**；as-of 2026-09-24 实测 K=P=57 · N=32 · M=18，差额 K−N=25 道只由 `npm test` 链覆盖）。反证跑法：`cd mcp-server && node test-scripts.mjs` ⇒ §S4 那 6 行里三记投毒各印「红在哪句话」（摘一门的引用 ⇒ 点名该门 / N 地板抬到 K+1 / selftest 数组删一项且地板取实测 M），第四行「只增不减」对照须 `errors=0`；投毒全在内存夹具（纯函数 `censusChain({…})`），未改任何生产文件。as-of 2026-09-24 | 无（已闭环）。复核命令见上；地板只许**降**不许改成等式 |
| `L14` flat `.md` 技能源稿被跳 sha 比对 | **本轮实测**：`assert-skill-mirrors.mjs:142-146` 对 `statSync(flat).isFile() && skillName.endsWith(".md")` 直接 `continue`（只在 `:466` 做 frontmatter 检查），而 `scripts/sync-skills.ps1` **确实**投影这些文件。分母：8 个平台树下 `.cursor/skills/*.md`（直接位于 `skills/` 下的 flat 件）= **380** 份，全宿主同形 = **1637** 份。本轮实测该面 drift 0 ⇒ 是**潜伏盲区**、不是现症。as-of 2026-09-24 | 排期：补 sha 比对前须先确认 `sync-skills.ps1` 的行尾/规范化差异不致假红 |
| `L15` `F6` 投影比对漏 `.claude/commands/` 形状 | **本轮实测部分复现 + 行号更正**：写单给的「`:169`」实为 `for (const f of src) {`；`F6` 的真身在 `assert-forge-1182-registry-consts.mjs:14`（自述「六个技能源稿与其 7 份宿主投影逐字节一致」）与 `:210` `F6:mirror-drift`，采集清单在 `:198-207`：只试 `pr + '/skills/' + n + '/SKILL.md'` 与 `pr + '/skills/' + n + '.md'` 两形，**不含** `.claude/commands/<去 mc- 前缀>.md`（该形状由 `assert-skill-mirrors.mjs:157` 使用）。⇒ 「`.claude` 那一投影被跳过」形状成立、但「每 skill 少扫 1 份（42 中 6）」的**份数未复核** | 排期（低风险实修）：给候选清单补第三形，并按「先对齐生产侧、再改 harness」重钉 `LEDGER.mirrorDrift` |
| `L16` `knowledge/**` 与档内 skills 不在任何 shape 门判面内 | **本轮实测采集面**：`assert-rule-java-shapes.mjs:55` 自述口径 = 「`collectRuleFiles()` 实采的 `<平台>[/<版本>]/.cursor/rules/*.mdc` 文件数，**不含 skills**、不含 7 宿主投影、不含 scaffold」，`:20` 另写明「skills 面仍不在本门采集面」。仓库根 `knowledge/` 实际只有 `libs/` 与 `patterns/` 两个子树 ⇒ 这两个面的修法**只能靠读回自证** | 排期：要么扩采集面（须给地板防「采到 0 份 = 采集器失效」），要么明文承认无人守 |
| `L17` 循环 lint 活在 gitignored `temp/` | **本轮实测**：`git check-ignore -v` → `temp/ralph-20260922/_anchor-lint.mjs` 命中 `.gitignore:44` 的 `temp/`；该文件 115 行，`:8-9` 显示默认判面**只有** `PLAN-STATE-r2.md`（argv 可加文件）⇒ 轮次报告与本节都不在其内。它不被任何门引用、不进 `npm test`、不受版本控制。**明确不做**：不为「落生效路径」把它搬进 `mcp-server/scripts/`——它的判据依赖 `temp/plan-20260921-fix.json`（S1–S27 名单），进门链会让所有人 clone 后必红。as-of 2026-09-24 | 用户：要不要把它改造成「判面可配、名单入库」的正式门（那是新门 + 排期，不是搬运） |
| `L18` `test-scripts.mjs` 的 selftest 自述计数过期（第 24 轮新暴露） | **已由第 26 轮接成机制**：①`mcp-server/test-scripts.mjs` W1-2 块注释（第 24 轮改成不漂写法那处）再加一行**指向 §S4「门链四数」打印**，注释里不写任何固定数字；②「无机制盯 N」的欠账已由该打印背后的 `censusChain` 断言补上——`N` / `M` 现取自 `REAL_RUN_GATES` / `SELFTEST_GATES` 数组本身（去重长度），地板 `N ≥ 30` / `M ≥ 15` 用 `<` 判红，故**合法加门仍绿、被摘门才红**（as-of 2026-09-24 实测 N=32 / M=18，口径 = 数组去重长度）；③`mcp-server/scripts/assertion-ledger.json` 的 closed 条目 `gate-selftest-default-chain` 与 `mcp-server/CHANGELOG.md:18`「四道防线」按第 24 轮裁定仍**不动**（前者是接线当时的记录，改它 = 篡改 closed 记录；后者另一个所指）。复核命令：`cd mcp-server && node test-scripts.mjs`。as-of 2026-09-24 | 无（已闭环） |
| `L19` 移植表的孤儿行形状 | **已由第 26 轮合并（不删判据、不新增结论）**：`forge/1.18.2/knowledge/porting/01-api-cross-loader.md` 原 `:32`（全角「：」开头、无 bullet 前缀的半行残留）里 `:31` 尚未包含的信息——与本包 `.cursor/skills/mc-fluid/SKILL.md:22`、`:102` 的 ❌ 禁令**同源并存**（该 Skill 主张 1.18.2 走 `FluidAttributes` + `ForgeRegistries.FLUIDS`）、当时两侧均无逐字证据、2026-09-24 判给禁令侧——已补进 `:31`，随后**删掉 `:32`**；`:31` 原有的四档出处/证伪结论一字未改，未写入任何新 API 口径。怎么确认孤儿行没了：`grep -n "^：" forge/1.18.2/knowledge/porting/01-api-cross-loader.md` 无输出（rc=1）。该文件属 `knowledge/**`、**无镜像投影** ⇒ 读回即可（`assert-skill-mirrors` 看不见它）。`holds.FLUIDTYPES` **未变、仍 10**（实测原 `:32` 内 `FLUIDTYPES` 出现 0 次 ⇒ 删行不动计数，未走 `MC_SKILL_F146_RELEDGER=1`、门注释的 1..10 清单无需改）。as-of 2026-09-24 | 无（已闭环） |
| `L20` 同一条「字段名四档出处」结论在**两处正文各写一遍**，且**无门比对两者** | **本轮实测（主代理验收时发现并纠正）**：第 25 轮往 `mcp-server/README.md:360` 新增的那条 bullet 带回的是**已被第 24 轮作废的三档口径**（把 `SOUND_EVENTS`/`PARTICLE_TYPES`/`FLUIDS` 写成「处方-only·无外部出处」、`ENTITIES`@1.18.2 写成「外部-only」，且未提 `FLUIDTYPES` 已证伪）⇒ 已改为与根 `AGENTS.md:376` 同源的**四档**、删掉指向 `temp/**`（不入库）的证据指针、把「本轮 node 重读，2026-09-23」换成可复核口径。复测数字（node 直读 json 累加，非工具自述）：`1.18.2-forge.json` `classCount` 988 = `classes` 实数 988、methods 累加 4950、fields 累加 **0**、`fqcnIndex` 617 条、`ForgeRegistries` 两处 **0** 条目。分母：`grep -c 四档出处` → `AGENTS.md` 1 / README 1；`三档出处` 两处正文 **0**。as-of 2026-09-24 | 排期：收成**单处真值 + 另一处只引不抄**，或造一道跨文件比对门（现无门管：`assert-config-platform-face.mjs` 管配置口径、`[docs-tool-tables]` 管工具表，均不含这条） |
| `L21` 假红发生器：`test-decompile.mjs` 的 mtime 夹具缺前置断言 | **已闭环**（第 27 轮修，第 28 轮复跑取证）：旧现象 = 整链 `149 passed, 1 failed`、单跑该文件却 `rc=0`、重跑变 `150 passed, 0 failed`——即「夹具依赖前序块留下的 mtime」造成的顺序性假红，根因是夹具**没有前置断言**去自己保证那个前提。**复核命令**：`cd mcp-server && node test-decompile.mjs`。**第 28 轮真实输出**（as-of 2026-09-24 22:4x，该文件 mtime 09-24 22:02）：末行 `150 passed, 0 failed`、`rc=0` ⇒ 顺序依赖已消除，本文件单跑即全绿 | 无（已闭环）。再遇该文件红，先复跑再归因，别改夹具 |
| `L22` 「门链四数」的差额口径错（把 `K−N` 当 `npm test` 专属） | **已闭环**（第 27 轮把差额改成按数组现算，第 28 轮复跑并现扫）。旧错：`K−N=25` 把「本文件其它块现 spawn 的门」也算成 `npm test` 专属，且旧记 57/45/12 是 21:xx 快照。**第 28 轮实测**（as-of 2026-09-24 22:4x）：`cd mcp-server && node test-scripts.mjs` ⇒ `rc=0`，§S4 打印「盘上 **58** 道 assert-* · 可达 **58** 道（= 被 19 个 `test-*.mjs` + `package.json` 引用，不是被执行）· §S18/S19 真跑 **33** · W1-2 selftest **21**」；另以 `node -e` 现扫引用面：被 `test-scripts.mjs` **本文件**引用 **48** / 被任一 `test-*.mjs`+`package.json` 引用 58 / **本文件零出现 Y=10**（旧记 12 = 57−45 的 21:xx 快照；现值 10 = 58−48，探针 `_v29-census.mjs` 与门内 `censusChain` 同口径）——⚠️ 别把 Y 与「真孤儿」混：孤儿 = 盘上门在**全部 19 个 `test-*.mjs` + `package.json`** 里零出现，现值 **0**（`assert-cli-smoke.mjs` 属前者不属后者，按只读 `test-scripts.mjs` 一个文件数会误报 1 孤儿）。⚠️ 本表一切「门今日绿」的说法都只是**带 as-of 的复跑值**：首轮该命令曾在 `test-scripts.mjs:330` 的 `assert-powershell` 自检崩掉（`rc=1`），同状态复跑即 `rc=0`，`scripts/sync-skills.ps1` 实测在盘 ⇒ 判为并发写窗口的一次性红，非本仓事实 | 无（已闭环）。地板 `N ≥ 30` / `M ≥ 15` / `Y ≥ 8`（`test-scripts.mjs:2096`）是**下界**，加门会让 58/33/21 一起涨，不得改成等式棘轮 |
| `L23` `S20/T3` 的 CI 面：四支 workflow 未设 `MC_SKILL_SKIP_DOWNLOAD` + `actions/checkout` 版本不齐 | **只登记，第 28 轮刻意未执行**（改 `.github/**` 属用户 CI/配置面，agent 不代做）。**本轮实测**（as-of 2026-09-24 22:4x）：`grep -rn MC_SKILL_SKIP_DOWNLOAD .github` ⇒ **0 命中**；`grep -rn actions/checkout@ .github/workflows` ⇒ `@v6` **1** 处（`opencode.yml:30`）+ `@v4` **4** 处（`mcp-server-ci.yml:19,50`、`release-mcp-server.yml:18`、`wrapper-upstream-check.yml:26`）。文档侧口径已改：`README.md:245` 与 `mcp-server/README.md:104` 不再写「（CI 语义）」暗示 CI 已设，改成「须显式设逐字 `1`；本仓 workflows 目前**未设**」，默认值列同时按实现落准（`mcp-server/src/decompile/java/java-process.ts:168` 是 `process.env.MC_SKILL_SKIP_DOWNLOAD === "1"` 严格比较 ⇒ 不设或设 `true` 都不生效）。paste-ready 片段 = `temp/ralph-20260922/l23-workflow-snippet.txt`（**不入库**） | 用户：要不要按片段给四支 workflow 加 env 并统一 checkout 版本（`@v4` vs `@v6` 二选一） |
| `L24` `mcp-server/README.md` 数据链行的三个计数（同一行往往不止一个漂） | **已按各自命名面现扫**（as-of 2026-09-24 22:4x）：`library-catalog.ts` 的 `{ id: '` 对象字面量 = **50**（口径 = 直读 `mcp-server/src/diagnostics/library-catalog.ts` 源文件正则计数，非工具自述）⇒ **复核未变**；`mcp-server/data/lib-manifests/all.json` = **顶层数组、`length` 48**（原写 45 ⇒ 已改；48 个 `slug` 去重后仍 48）；`mcp-server/data/lib-api-summaries/` 的 `*.json` = **48**（0 子目录）⇒ **复核未变**。⚠️ 与 `L8` 的「workflow 模板 45→48」是两个无关的 45，同因 = 文档抄旧快照 | 不需用户（已改）。要防复发得给该行加一道「README 计数 ↔ 盘面」比对门，现无门 |
| `L25`（计划 `D-6`）fabric `meta.json` 页数台账少于盘上、且缺 sha 钉 | **计划要求的第一步已做，结论 = 快照已取代该台账，该改的是 `meta.json` 侧**。**两面实存都量了**（as-of 2026-09-24 22:4x）：`find data -name 'upstream-*.json'` = **23** 份（14 份 `data/fabric_<ver>/fabric-docs/<ver>/upstream-tree.json` + 9 份 `data/neoforge_<ver>/neoforge-docs/<ver>/upstream-sitemap.json`；⚠️ 写单猜的 `find data/fabric_* -name 'upstream-*.json'` 按 `maxdepth 2` 数出 **0** 是假阴性，快照在第 3 层）。`upstream-tree.json` 字段 = `platform,version,source=github_trees,repo,branch,treeSha,truncated,discoveredAt,pageCount,pages[]`，14 档 `treeSha` 同值 `53dd9650…`、`discoveredAt` 落 2026-09-21T11:56–12:32；**逐档 `pageCount` == 盘上 `processed/*.md` 数**：1.20.4 31=31 / 1.21.1 45=45 / 1.21.10 79=79 / **1.21.11 93=93** / 1.21.4 51=51 / 1.21.8 67=67 / 26.1.2 100=100，无正文的 7 档 =0=0 ⇒ 自洽。而 `meta.json` 的 `meta.docs.pages` 是残缺子集且**无 sha**：1.21.11 **69** vs 93（计划举例复现）/ 1.21.10 **1** vs 79 / 1.21.1 30 vs 45 / 1.21.4 34 vs 51 / 1.21.8 48 vs 67 / 1.20.4 22 vs 31 / 26.1.2 77 vs 100；14 档 `meta.json` 全文 `grep -e commitSha -e treeSha -e treeEta` **0 命中** ⇒ 计划「缺 `commitSha`/`treeEta`」复现。**未动面**：`meta.json` 的消费者含 `mcp-server/src/platform-pack/{catalog,session,write}.ts` 与 `assert-pack-meta-coverage.mjs`，快照的消费者是 `assert-upstream-chapters.mjs` / `_lib/upstream-inventory.mjs` / `fetch-fabric-docs.js` ⇒ 删列或改指向都会打断按旧形状解析的一方 | 用户/排期：`data/**` 面 + 两侧都有消费者，须先定「`meta.json` 页面台账退役为只指快照」还是「按快照回填」 |
| `L26`（计划 `D-7`）neoforge 语料 HTML 处理残留、forge 为 0、无门覆盖 | **两个数复测：分母复现、分子不复现**（口径 = `data/neoforge_<ver>/neoforge-docs/<ver>/processed/*.md`，9 档合计 **627** 页，逐档 61/64/63/75/75/67/72/73/77 ⇒ 计划 627 ✓）。带残留的页数按三口径分别测得：**HTML 标签**（`</?(p / br / div / img / a / hr / span / table / tr / td / th / ul / ol / li / h1-6)>`，闭不闭皆算）= **223**/627；`<!--` 注释 = **142**/627；两者并集 = 239/627 ⇒ **计划给的 203 三口径都不复现**，本轮按实测 223 落笔并点名。同一「HTML 标签」口径打在 `data/forge_<ver>/forge-docs/*/processed`（12 档合计 602 页）= **0** ⇒ 计划的「forge（mkdocs 源）实测 0」**只在标签口径下成立**；换成注释口径 forge 反而 **490**/602 > neoforge 142/627，因为那些 `<!-- source: … -->` 是本仓 reader 展开转引时**自己写的**。MDX 组件（`<Tabs>` / `<DocsAlert>` / `<VersionBadge>` 等）= **0**。**处置按既有裁定走「显式惯例」侧**：语料逐字保留、永不打补丁，也不为此加清洗门（加了会把「忠实上游」判成红腿）。**无门覆盖 = 实况，非漏** | 无（裁定已是惯例侧）。要翻成「缺陷须清洗」需用户重开裁定 |
| `L27`（计划 `D-8`）vanilla 注册表来源 `minecraft-data@master` 无 commit 钉 | **本轮只登记，未动任何一面**（计划原文即「要先跟 owner 确认」）。实测来源 = `mcp-server/scripts/fetch-vanilla-registries.mjs:23` `const CDN = "https://cdn.jsdelivr.net/gh/PrismarineJS/minecraft-data@master/data"`（移动 ref），`:258` 把 provenance 标成字面量 `minecraft-data@master`，`:190-191`/`:210` 已带 MIT 署名行 ⇒ **无 commit / 无 sha 钉**。产物新旧：`data/vanilla_1.21.1/registries/` 目录 mtime = **08-09 10:48**（as-of 2026-09-24 ⇒ 约 **6.6 周**，计划「6 周旧」复现）。**钉的后果** = 可复现、可逐字节重算，但不会自动跟上游 registry 更新；**不钉的后果** = 任何人重跑必漂移、历史产物无法复算、`docs/registry-data-source.md` 的「来源」一格永远只能写分支名。⚠️ 同面一致性：`data/porting/knowledge-base/versions.json` 实测 mtime **09-22 19:28**（近期被 sweep 重写过），钉与不钉都要与它同批决策，否则出现「一个钉一个漂」 | 用户/owner：钉不钉（计划明写需先确认）。定案前不改脚本、不改产物 |
| `L28`（计划 `D-9`·register-item）quilt 每档只有 4 篇自写页 / fabric-wiki 同名 7 页 | **明确不做 + 原因**，并**更正计划口径**（as-of 2026-09-24 22:4x 实测）。quilt 侧：`data/quilt_*` = **6 档**（1.18.2/1.19.4/1.20.1/1.20.4/1.21.1/1.21.11），每档 `processed/*.md` = **18**，文件名集合 6 档**完全相同** = 14 篇 `wiki-*`（上游 `QuiltMC/developer-wiki` 英文正文）+ **4 篇非 wiki**（`qsl-qfapi` / `qsl-readme` / `qsl-verified` / `quilt-mod-json`）⇒ 计划的「6 包各只有 4 篇自写页」按**档**读才成立（不是 QSL「包」）。fabric 侧：13 档各有 `fabric-wiki/` 且各 **7** 页，`data/fabric_26.1.2` **没有** `fabric-wiki`（0 页）；⚠️ **「13 包共用同一份 7 页拷贝」不成立**——按「7 文件名 + 各自 sha1 前 12」分组，13 档落进 **13 个不同组**（同名、同数、逐字节不同），所以它不是重复拷贝、去重也省不出空间。**不做的原因**：上游 developer-wiki 对多数分支只留这些页（补不出正文 = 上游没有，不是抓取漏，见根 `AGENTS.md` Fabric/Quilt 建档面），而 10 档规则树 vs 6 档语料的差额、7 档无 docs 正文，都已在 `AGENTS.md` 登记；按档逐字节去重会破坏「按档读取」的既有形状 | 无（明确不做）。要扩语料需用户排期并重新界定抓取范围 |
| `L29`（计划 `D-10`·register-item）薄档只有 `index-l0` / ScriptAPI 包覆盖 | **明确不做 + 原因**，并点名一个对不上的分母。**薄档实测**：全 `data/` 里「有 `index-l0.json` 而 `processed/` 为 0 份 md」= **7 个** `data/fabric_<ver>/fabric-docs/<ver>/`（`1.14.4`/`1.16.5`/`1.17.1`/`1.18.2`/`1.19.4`/`1.20.1`/`1.21.3`），与根 `AGENTS.md`「这 7 档上游 `fabric-docs` 从来没有 docs 树、本地只有 fabric-wiki 7 页 + `failures.json`」**逐档相符** ⇒ 抓取范围外，**禁止**拿邻版正文顶替（既有裁定）。对照：`data/forge_javadoc/*` 6 个旧档反而是**厚**的（各带 `index-l0/l1/l2` + `processed` + `raw`，md 3758–7108 份/档）⇒ 「薄」不是按 `index-l0` 存在判的。ScriptAPI 面：`data/bedrock_stable/bedrock-scriptapi/stable/` = `index-l0.json` + `processed/`（**624** md）+ `semantic/` + `scriptapi-typed.json`；`bedrock-docs/stable/processed` = **258** md；`scriptapi-typed.json` 顶层键是**单个 `pkg`** 的形状（`pkg,version,sourceUrl,sourceKind,bytes,sha256,npmLatest,integrity,unpackedSize,retrievedAt,decls`）⇒ 计划「只覆盖 6 个包里的 1 个」里的「**6 个包**」这一分母本轮**没能从该文件数出来**，记 `未核实`。既有裁定不变：1657 页 Script API 应走 npm `.d.ts` 而非爬站，用户 2026-09-21 已推迟 | 用户：何时改用 `.d.ts` 源（成本口径已量好）。改之前本项维持「不做」 |
| `L30` `S17` 剩余腿：manifest 写盘器去重、转引端到端 | **逐腿实测，三条状态不同**（as-of 2026-09-24 22:5x）：① **数据侧已净**——`data/forge_1.20.1/forge-docs/_manifest.json` 以「2 空格缩进顶层键」原始计数 = **71**，`JSON.parse` 后唯一键 = **71**，重复键 **0** ⇒ 计划点名的 `recipes/ingredients` / `recipes/incode` 双份**今天不在盘上**（是谁改的未查），但「让**写盘器**去重」那条腿 `未复核`（本轮没读抓取器写 manifest 的代码 ⇒ 双份可能只是被某轮整表重写盖掉、机制未修）。② **转引别名已落**——`mcp-server/src/docs-platform/fabric/transclude.ts:126-137` 已实现「`.yml`↔`.yaml` 别名 + 父目录内大小写无关 basename 命中」，注释里的动机正是 `@/.github/workflows/build.yml` → 镜像 `build.yaml`。③ **端到端未证**——盘上仍有裸标记（实测 `data/fabric_1.21.8/fabric-docs/1.21.8/processed/develop_automatic-testing.md:113,123,125` 三行 `<<< @/reference/…`）；这**按设计是正常的**（reader 在 `get_*_doc_full` 读取时才展开），所以「返回正文 0 裸标记」这一断言本轮**没有**由真跑工具验证 ⇒ `未复核`，不得据此说转引已修好或没修好 | 排期：判 ③ 须真跑一次 `get_fabric_doc_full`（要 `MC_SKILL_DATA` + 已 `npm run build` 的 `dist/`）；判 ① 的机制腿须先读写盘器 |
| `L31` `S21/t2` 剩余腿：mcmod 协议列的**逐行取证 pass**（旧标题「`ATTRIBUTION.md:3836` 逐行取证」= 我方误读，见下） | **第 38 轮更正标题（正文证据保留）**：本行旧写法把「3836」当成**该有而缺**的一行 —— 计划原文（`temp/plan-20260921-fix.json` → `stories[20].tasks[2]`）说的是 `ATTRIBUTION.md has no 3836 entry at all`，即**根本没有 3836 这个条目**；该误读自第 27 轮起从 `_audit-S14-S27.md` 一路抄递进各轮写单与本行。⇒ 「3836 行仍缺」这条**欠账不成立**（纠正 ≠ 收口）；`t2` 的**真欠项**是同一句要求的「open a per-row evidence pass instead」（逐条记录每个「显式」从哪读到），**至今没做**，已随 `L35`（用户 2026-09-24 按反爬关闭 `C-1`）挂起，登记处 = `L46`。**旧证据仍成立**：全仓 `ATTRIBUTION.md` 实测只有两例——`community_knowledge/ATTRIBUTION.md` **50 行**、`data/vanilla_ATTRIBUTION.md` **13 行**（`wc -l`），**都没有第 3836 行** ⇒ 审计给的行锚无法打开、无法逐行取证，据此写的任何「已按 :3836 核对」都是空话。协议列 tally 本轮**未重算**：既有裁定已明写「不要按审计的数字批量改 `协议` 列」（审计给的数不 reproduce：实测该列为 21「显式 BY-NC-SA」+ 1「显式 BY-NC-ND」/ 12「默认 BY-NC-SA」/ 8「未声明」变体 / 4「❌ 禁转载」）。⚠️ 本行旧句「`L5` 那条外链 404 也仍是 `未复核`」**已过期**：第 38 轮按 curl 实测结案 —— 盘上旧形状 `…/1.20.4/docs/…` **确实 404**、新形状 `…/docs/1.20.4/…` **200**，短文已换成 200 那条并留注记 ⇒ `L5` 闭环（复核命令：`grep -n "docs/1.20.4" community_knowledge/authored/saveddata-world-persistence.md`） | 判据作者：把该腿换成**真实文件 + 真实行**（原要求，第 38 轮已按此改标题）。**`t2` 逐行取证要推进 = 用户**：授权一次浏览器逐帖，或明确「永久维持 `:36` / `L35` 那类自我更正」后本行改写成「不做 + 复核命令」；批量改列需用户授权 |
| `L32` `S16` 剩余腿：会「静默变绿」的门今日是否绿 | **本轮禁改 `mcp-server/scripts/**`，故只复核到一处**：`test-scripts.mjs` §S4 的五记投毒 + 一记对照在同一次 `rc=0` 运行里逐条打印「红在哪句话」（as-of 2026-09-24 22:4x；含「凭空假门 ⇒ 可达 P=58 ≠ 盘上 K=59」那记）⇒ 该门的「可红性」有自证。S16 其余逐腿**全部 `未复核`**：`assert-corpus-semantics.mjs` 的 `baseline.files` ↔ 实测计数比较、`test-assistant-gaps.mjs` A-43 的 `q26.total === 17` 等式、`assert-no-yarn-json-slurp.mjs` 的注释豁免、`assert-skill-mirrors.mjs:24` `PLATS` 缺 `bedrock`、`assert-forge-1204-material.mjs:179-191` 的 premise 只设 `premiseNote`、`assert-powershell.mjs:160-164` 的 `indexOf` 锚点（第 46 轮回读：该锚点腿连同「-All 把 neoforge 列进 targets」「源稿复活」两条已抽进 `assert-powershell.mjs` 的判定核 `rootGuardTextFailures`，并由该门新增的 `--selftest` 13 例投毒 + 6 反退化断言钉住；as-of 2026-09-25，日志 `temp/ralph-20260922/logs/r46-A-selftest.log`）、`assert-qsl-verified-sync.mjs` 与 `scripts/index-qsl-verified.mjs` 的交叉核。⚠️ 本行的价值只到该 as-of 为止：「门绿」永远不是永久事实；**第 47 轮（as-of 2026-09-25）再补一条**：本行点名的 `assert-forge-1204-material.mjs:179-191` 那条 premise 三态腿已抽进判定核 `premiseCheck` / `gateVerdict` / `collectorFloorFailures`，并由该门新增的 `--selftest` 27 例（13 投毒必红 + 6 反退化断言 + 8 正对照）逐态投毒，五个 `MC_SKILL_MAT_*` 钩子从「只有文件自己读」变成有 harness 消费者 ⇒ **该 `:179-191` 行号锚点随重写漂移**，下轮引用一律按函数名、不按行号（日志 `temp/ralph-20260922/logs/r47-C-selftest.log` 与 `r47-C-poison.log`） | 排期：逐腿补投毒红（新腿须配「不判对照 + 覆盖下界」，否则是装饰，见 `L2`）|
| `L33` bedrock `--reprocess` 只覆盖 20 份精选页 = **设计**，审查项 P-1 的「仍只覆盖 20 页」不成立为缺陷 | **本轮实测**：`mcp-server/scripts/fetch-bedrock-docs.js:17` 头注释自述「`--reprocess` 只重处理**顶部精选页**（PAGES 那 20 个 id，`--ids=` 可缩到子集），不碰 toc 抓来的」；`export const PAGES` 到 `];` 之间现扫 `{ id: "…" }` = **20 个**（机制：node 正则数 `{ id: "` 出现次数），与注释同数 ⇒ 「20 页」不是覆盖率缺陷而是该开关的既定作用域。⇒ 剩下的**唯一**真问题是另一件事：**toc 抓来的那批页是否已过期**（审查代理称 238 页停在 2026-09-21 口径，本轮 `未复核`，未跑 `--probe`/未读 `fingerprints.json` 时间戳）。复核：`node -e "…"` 数 PAGES + `sed -n '10,22p' mcp-server/scripts/fetch-bedrock-docs.js`。as-of 2026-09-24 | 并行会话（bedrock 重处理车道，正在按 `temp/sidechat-reprocess-20260924/TODO-MASTER-20260924.md` 写盘）+ 用户：是否把 `--reprocess` 扩出 toc 面（`--scope=toc` 属新增能力，不是修 bug） |
| `L34`（审查项 `P-2`）bedrock 抓取器把 `sectionCount` 恒写成 **1** ⇒ 检索的 section 特征失效（**真缺陷，2026-09-25 已修 + 已重建 + 命中形状已复核，只剩提交**） | **本行原断言已作废**（原写「`fetch-bedrock-docs.js:473` 与 `:593` 两处硬编码 `sectionCount: 1,` ⇒ 恰好这 2 行」）。**2026-09-25 23:45 现扫**：`grep -n sectionCount mcp-server/scripts/fetch-bedrock-docs.js` ⇒ `:475` 与 `:598` 两处均为 `sectionCount: [...md.matchAll(/^## /gm)].length`，`:586-587` 注释自述「P-2（2026-09-25）… 此前硬编码 1」；该件 `git status` = **` M`（未提交）**。**第二步（重建）也已发生**：`bedrock-docs` 语义库 meta `built_at = 2026-09-25T12:07:19.151Z`（UTC+8 = **20:07**，由 bedrock 车道自跑，非旁路插队），docs 258 / **chunks 5312** / **embedded 3374**（09-24 口径 5054 / 3291 ⇒ **+258 / +83**，分母 = 同一 `chunks`、`chunk_embeddings` 两表现扫）。`docs.section_count` 分布：**244/258 行 > 1**、max 32、Σ1545；剩 **14 行 = 1** 且 `index-l0.json` 同 14 行 = 1（那 14 页正文确实没有 `^## ` 章节，不是硬编码残留）。**第三步（复核命中形状）本轮补上**：现跑 `MC_SKILL_DATA=$PWD/data node mcp-server/dist/cli.js search_bedrock_docs --query="custom block geometry material_instances"` ⇒ `ok:true` `semantic:true` `total:20`，首条命中 `stable/documents/addcustomdieblock` 带 **`sectionCount: 5`**（旧形态恒 1），`matches[].sectionHeading` 给的是真章节名「Define the block geometry」；同轮 G4（`assert-index-consistency.mjs`，23:47 只读复跑）rc=0：61 库 · Σchunks 51533 · Σembedded 31857 · sha256 全对账 · 残留 0，另我按「行 sha ↔ `processed/<id 去 stable/ 前缀>.md`」逐文件复算 = **258/258**（`bedrock-scriptapi` **624/624**）全对。**且确有消费者，不是死字段**：`mcp-server/src/docs-platform/semantic/search.ts:485` 从 `docs` 表 `SELECT … section_count …`、`:507` 塞进结果（两行号 23:45 复测仍对）；分块侧注释自述后果在 `mcp-server/src/docs-platform/fabric/store.ts`「sectionCount=1 ⇒ `sectionKws` 稀疏 ⇒ …在 L2 下交集为 0」（⚠️ 原写单给的 `store.ts:678` 与上一版行的行号**均已漂**，23:45 该注释在 `:704`，且该件 23:41 正被并行会话改 ⇒ 本行改按注释文字检索，不再钉行号；另实测 `data/bedrock_stable/bedrock-docs/stable/` 只有 `fingerprints.json` / `index-l0.json` / `processed` / `semantic` 四项，**bedrock 没有 `index-l1/l2` 层** ⇒「L2 下交集为 0」那句只约束 fabric/quilt 面，bedrock 侧的真症状是命中 `sectionCount` 恒 1 与 `sectionHeading` 缺失）。⇒ 修它的三步「改抓取器 → 重建索引 → 复核命中形状」**本轮全部有凭据**（上一条那段），但**长期约束继续有效**：**禁止**在并行写窗口重建任何 `data/**` 索引，且**只改抓取器不重建 = 半修**（本行的重建发生在 20:07 的 bedrock 车道内，不是旁路插队）。**交叉点名（第 40 轮立，23:45 复测行号）**：本行**不等于**「S14 的 `sectionCount` 改真数」那条 task —— 后者修的是 `mcp-server/scripts/fetch-bedrock-script-api.mjs`（现 `:378` = `const sectionCount = [...md.matchAll(/^## /gm)].length;`，`:372` 注释自述「S14-T3：`sectionCount` 改真数（此前 `:386` 硬编码 1）」⇒ **已成**），与本行的 `fetch-bedrock-docs.js:475`/`:598` 是**两个脚本、两处位点**；两件事现在**都已修** ⇒ 但**不许合并销账**（编号跨表错位的危害见 `L48`）。⚠️ 顺带记下：scriptapi 树本轮复测 `docs.section_count` 仍是 **617/624 = 1**（另 7 行 = 0），而该转换器产出的每页本就只有一个 `## ` ⇒ **未复核是否真缺陷**，禁止顺手并案或顺手立案。 | **用户**：`git add` 提交 —— 修复件 `mcp-server/scripts/fetch-bedrock-docs.js` 至今 ` M`（不提交 = 下一个人仍会照本行旧断言「仍在」重做一遍）。维护侧无可代劳（三步已实测落地，agent 禁 add/commit）。 |
| `L35` mcmod 短文的协议列表 / 死链（审查项 `C-1`）——**已按用户裁定关闭（2026-09-24）**，**仅覆盖风控型主机** | **关闭依据（用户原话）**：「社区文档的死链是反爬的问题，不是缺陷，推荐使用 IDE 的内置浏览器或者 computer use 解决；C-1/C-3 关」。**本轮实测复核**（`curl.exe -s -o /dev/null --ssl-no-revoke -w "%{http_code}" -L --max-time 20`，2026-09-25）：本仓**确证存在**的 `post/3993.html` ⇒ **403**（风控页）、`class/1.html` ⇒ 三趟 **429 / 403 / 403**（**同一 URL 的码在趟与趟之间漂移**）、`post/6071.html` ⇒ **429** ⇒ 403/429/404 三者不可分辨，脚本探测**不得**当死链结论，也不修。**要取证走 IDE 内置浏览器 / computer use 逐条开**。⚠️ **本关闭不覆盖 `github.com`**（那边 13 条是真 404，见 `L37`）——把「反爬」写成通则会让下一个 agent 跳过真失效链。判据已落 `community_knowledge/AGENT_USAGE.md`「外部链接存活判定：按主机分判据」一节。as-of 2026-09-25 | 无人欠做（用户已裁定关闭）；维护侧**禁止**再承诺「脚本能判 mcmod 死链」或据 403/429 改任何链接 |
| `L36` HITL 门的兜底腿被 **verify 语义**喂饱（审查项 `P-3` 更正前提后的真缺陷） | **前提更正**：`P-3` 原述「48 个模板里 12 个没有 HITL 条款」**不成立**——实跑 `node mcp-server/scripts/assert-workflow-hitl.mjs` ⇒ rc=0，打印「模板 48 个：引 `WORKFLOW_HITL` 36 个 / 语义字样兜底 12 个 ≤ 上限 12」，且 12 个在门现用词表（`人在环 / 确认`）下**逐个**命中「确认」。上限恰等于实况是**只降棘轮的基线**（该文件 `:13` 自述「棘轮只降」），不是缺陷。**真缺陷**：把判据换成 approval 词表（`人在环 / 用户确认 / 确认后 / 须经用户 / 等待用户 / 不代跑 / 不得代跑 / dryRun`）后兜底 12 个里有 **7 个**转红 = `mc-new-entity, mc-new-gui, mc-decompile-mod, mc-new-item, mc-new-blockentity, mc-mixin, mc-worldgen`——它们的「确认」全是 `1. 确认平台与精确 MC 版本`（*verify* 语义，不是「写盘/跑 Gradle 前停下等用户」的 *approval* 语义）。⚠️ **点名写单前提错**：写单给的是 8 个并含 `mc-config`，按它**自己给的词表**复算 `mc-config` 不过红——其 body 在 `mcp-server/src/prompts/templates.ts:215` 含 `dryRun`（「`ingest_loader_api`（默认 dryRun…）」）⇒ 严格词表下过关；`mc-new-block` 同理靠 `:22` 的 `download_official_mdk（dryRun 先看）` 过关。⇒ 实况 **7 红 / 5 绿**，分母 12、机制 = 按 `^  "mc-…": \{` 切块 + 词表正则（两机制：matchAll 与逐行打印命中行）。as-of 2026-09-24 | 排期：收紧 `SEMANTIC_RE` 为 approval 语义 + 给这 7 个模板补 `${WORKFLOW_HITL}` + 上限 12→0 + 投毒「正文只有『确认平台』字样 ⇒ 必红」；因改 `src/**` 须 `npm run build` + 重载宿主 MCP，**单开一轮**（第 29 轮明令不动 `src/prompts/templates.ts`）|
| `L37` GitHub 死链（审查项 `C-3`）：**13 条真 404，不得并入 `L35` 的反爬关闭** | **实测**：`community_knowledge/**` 引用的 85 条去重 `github.com` URL，单趟 8 并发 ⇒ **72×200 / 13×404**，该趟 **0 条 `000`**（逐条码 `temp/ralph-20260922/_r30-gh-probe.log`，本轮 `grep -c 404` = 13、`wc -l` = 85 复算）；复测死链 4 条 ⇒ 3 条仍 **404**、1 条 `000`（`Darkhax/Bookshelf`，未推翻也未复核，见 `_r30-recheck.log`）⇒ 同客户端能拿 72 个 200 ⇒ GitHub 没拦脚本，那 13 条是**真失效**（多为上游改名）。13 条：`Darkhax/Bookshelf`、`BlayTheNinth/Balm`、`ElocinDev/Necronomicon`、`Grend-G/Iceberg`、`Patbox/text_placeholder_api`、`Mojang/minecraft-preview-changelogs`、`NucleoidMC/server-translations-api`、`Tutorials-By-Kaupenjoe/{Fabric-Course-118,Fabric-Course-1.20.X,Forge-Course-118}`、`architectury/ObsidianUI`、`fzzyhmstrs/fzzy_config`、`isxander/yet-another-config-lib`。**处置（2026-09-25 定）**：**不做脚本逐条替换**——新 home 反查实测不稳定（同一候选两遍跑出 404 / 000 / 200 不等，见 `_r30-gh-homes.log`；`000` 是连接没跑完、不是服务器码，按「探测失败 ≠ 负面事实」不得读成「上游没有」），改为**在正文 13 条旁标注「原链接已失效（实测 404）+ 新 home 未核实」**；唯一实测 200 的 `Patbox/TextPlaceholderAPI` **也只算候选**，禁止写进正文当出处。as-of 2026-09-25 | 排期：逐条标注未做（本轮只落判据，未动 `community_knowledge/authored/**` 正文，见 r30 报告未做清单）；真要替换须浏览器 / computer use 核内容后**单开一轮** |
| `L38` `knowledge/libs/**/SKILL.md` 的 CRLF 件 ⇒ 按 LF 写的 frontmatter 探测会拿到空声明（**危害在探测侧，不在产品侧**） | **本轮字节级普查推翻写单数字**：写单 §2 说「36 份里 **11** 份是 CRLF」并列了 11 个名字，实测 `knowledge/libs/**/SKILL.md` 共 **36** 份、含 CRLF 的只有 **1** 份 = `all-platforms/mc-config/SKILL.md`（69 行**全** CRLF，mtime 2026-09-14）；其余 10 份点名件 CR 字节数 = **0**（脚本 `temp/ralph-20260922/_r30-crlf-census.mjs`，机制 = 逐字节数 0x0D，非行正则）。git 侧抽样 5 份（architectury / resourceful-lib / script-server / trinkets / forge-only curios）HEAD blob 亦无 CR；根 `.gitattributes:14` 自 2026-09-20 起钉 `* text=auto eol=lf` 且本机 `core.autocrlf=true` ⇒ 检出即 LF，「11 份」应是该裁定之前测的、或探测面本身是 LF 假设的。**产品不受影响（本轮现跑四组）**：`node mcp-server/bin/mc-skill-scripts.mjs lib resolve --platform <p> --version 1.21.4` ⇒ fabric 23 条里 `mc-config` **在**、quilt 12 条里**不在**（其 frontmatter `platforms: [fabric, forge, neoforge]` 被正确读到）、forge 11 / neoforge 17 均在 ⇒ `src/lib-skills/` 解析 CRLF 正常。**危害只在探测面**：`^---\n` / `^platforms:` 一类按 LF 写的正则在 CRLF 件上匹配不到 frontmatter ⇒ 把「显式排除」误报成「未声明」；第 29 轮正是这样把**这一份** `mc-config` 判成「根本没有 `platforms:` 键」的（见 R129 §6.2 自述）。as-of 2026-09-25 | 排期：二选一 —— 给该份统一行尾，或在所有 frontmatter 探测口径里写 `\r?\n`；两者都属**改判据面**，需批准（当前面只有 1 份，成本极低但必须先确认无别的 CRLF 件混进投影） |
| `L39` 第 29 轮**越界改动**（授权面之外）：内容为真故保留，流程违规在此登记 | 第 29 轮写单只授权改指定段落，该轮实际还动了：**`README.md`** `:49` fabric-only `9→10`、`:245` `MC_SKILL_SKIP_DOWNLOAD` 严格 `=== "1"`、`:283`「三组合」→ 逐组合 `VALIDATE_COMBOS`、`:579` 库源稿 `35/33→36/34`、`:795` 口径 A `1830→2632` 与口径 B `44 份 320 组→48 份 824 组`、`:960-962` 新增 3 行 `mc-events-forge` / `mc-events-neoforge` / `mc-events-fabric`；**`AUTO_SETUP.md`** `:71` 路径示例改写 + 新增整节「Step 0-pre 校验既有宿主配置里的绝对路径仍可解析」（现落 `:77`）。**第 30 轮逐条复核为真**：`:49`=10、`:579`=「36 份 / 34 唯一 skillId」、`:795` 含 2632、`:245` 与 `mcp-server/src/decompile/java/java-process.ts:168` 的 `process.env.MC_SKILL_SKIP_DOWNLOAD === "1"` 同调、三个 `mc-events-*` task 名在 `src/platform-pack/session.ts`(3 行) / `src/prompts/templates.ts`(7 行) / `src/wave/register.ts`(3 行) 均命中 ⇒ 真任务非捏造 ⇒ **保留、不回滚**。⚠️ 另注：写单 §4.1 说「并入既有的『未申报改动』类条目，别新开号」—— **本表没有这样一行**（L1–L38 逐行标题现扫，最近的是 `L10` 用户侧欠 `git add`，主题不同），故只能新开本行，且只开**一行**覆盖两处文件。口径 = 当轮写单授权面 ↔ 实际 diff 文件清单比对；结论 = 「内容为真故保留，但流程违规：未在写单授权面内」。顺带申报**用户侧实况**（不属本轮修复面，维护侧**不改**该文件）：`~/.cursor/mcp.json:6` = `"H:/MC_skill/mcp-server/dist/index.js"`、`:9` = `"MC_SKILL_DATA": "H:/MC_skill/data"`，而 `H:` 盘在本机不存在（`ls -d /h` ⇒ No such file or directory）⇒ **该宿主的 MCP 现在起不来**，须用户自己改成实际路径。as-of 2026-09-25 | 用户：是否把「越界改动」做成机制（写单授权面 ↔ `git diff --name-only` 比对门）；本行只是登记，不是判据 |
| `L40` `S17` 剩余腿：`_manifest.json` 去重腿**已在盘、且是读侧**（审计「未定位到实现」与写单「该脚本 = 写盘器」两处前提均被推翻），本轮补的是它缺失的**可红性** | **前提更正 ①**：审计 `_audit-S14-S27.md:146` 说「未定位到实现」⇒ 腿其实在 `mcp-server/scripts/audit-data-consistency.mjs:560-577`（检查名 `G-manifest-dup-key`，第 11 轮起；按**原始文本**数 2 空格缩进的顶层键，**不用** `JSON.parse`，因为重复键会被 last-wins 静默吞并）。**前提更正 ②**：写单 §2 说该脚本是 `_manifest.json` 的**写盘器** ⇒ 它只**读**（`:551` 读、`:603` 注释），全仓 `grep -rn "_manifest\.json" mcp-server/scripts mcp-server/src scripts` 除该脚本外只命中 `mcp-server/test-audit-data.mjs:88,109` 的夹具写入 ⇒ 真写盘器（历史一次性 fetch 脚本）**已不在盘上**，写侧无对象可改，去重只能永久留在读侧（脚本 `:562` 注释自述同一结论）。⚠️ **该腿本身也未提交**：`git show HEAD:mcp-server/scripts/audit-data-consistency.mjs` 内 `grep -c "G-manifest-dup-key"` = **0**、而工作树有 ⇒ 它与 `L10` 那 7 件同属「判据只活在未提交的工作树里，clone 者拿不到」，差别是本文件是 **tracked** 件、改动会进 `git diff`（不需要 `git add` 新文件，只需要提交）。**三口径实测**（探针 `temp/ralph-20260922/_r32-manifest-dup.mjs`；分母 = 仓内 **6** 份 `_manifest.json`，即 `data/forge_1.16.5` / `1.17.1` / `1.18.2` / `1.19.4` / `1.20.1` / `1.20.4` 各自的 `forge-docs/`，合计条目 **290**；as-of 2026-09-25）：口径1 原始文本顶层键 290 行 / 唯一 290 ⇒ 重复 **0**；口径2 `JSON.parse` 后条目 id 290 / 290 ⇒ 重复 **0**；口径3 条目的 `file` 路径值 290 / 唯一 287 ⇒ 重复 **3**（三条全在 `forge_1.20.1`：`rendering_modelloaders.md`、`misc_updatechecker.md`、`advanced_accesstransformers.md`）。⚠️ 这 3 条**不是缺陷、禁止照它建判据**：该文件按设计聚合多版本（`:603` 原文 "Manifest entries for other versions are valid in _manifest.json because the file aggregates all versions"），`file` 是裸文件名、不同版本键同名合法 ⇒ 按 `file` 去重会把 3 条正常数据判红。⇒ **本轮不新增判据**（写单 §2 第 3 步：0 重复 ⇒ 去重腿无对象，造恒真腿是本仓点名的伪守卫形状）。**本轮真做的 = 给既有腿补可红性**：`mcp-server/test-audit-data.mjs` 新增 `testManifestDupKeyLeg` 并注册进该文件 `tests` 数组 ⇒ 进默认链（`npm test` 第 9 腿 `test-audit-data`）。三段齐：正对照（干净 manifest ⇒ rc=0 且不触发）· 投毒（同一顶层键逐字写两遍，`file` 故意指向真实存在的 `processed/intro.md` 以排除 orphan 干扰 ⇒ rc=1，且 `ERROR` 集合**恰等于**单元素 `G-manifest-dup-key` = 唯一归因；另断言 `actual` 逐字含 `raw key occurrences 2`，钉住「采集器不是返回 0 条」）· 反证跑法 `node temp/ralph-20260922/_r32-poison-dupkey.mjs` ⇒ 三记全 PASS（P1 把生产腿 `dupKeys.size > 0` 改恒假 ⇒ 新腿红 · P2 把采集正则 `^ {2}` 改 `^ {9}` 即采集 0 条 ⇒ 新腿红 · P3 还原 ⇒ rc=0 且 FAIL 行数 0；`finally` 复验生产文件逐字还原、无 `POISON` 残留）。**顺带暴露的残留洞（本轮未修，属改判据）**：P2 正是「恒真于空」形状的存在证明 —— 该腿对**非 2 空格缩进**的 manifest（压缩形 / 4 空格）采集 0 条即静默放行；今日 6 份全为 2 空格 ⇒ 不现症。**（2026-09-25 第 33 轮更正）本行的『残留洞』已修，不再是「交用户裁」状态** —— 采集面已从「行首 2 空格」换成缩进无关的字符串态 + 括号深度扫描 `collectTopLevelJsonKeys`，并与 `JSON.parse` 的顶层唯一键集交叉核；新判据与四记反证见 `L41`。原文保留，以免毁掉「第 32 轮当时怎么看这件事」这条证据。**同条 task 点名的数字不符已核**：`temp/audit-20260921/REPORT.md:357` 写 `26.3.md 在盘 352,028 B`，node `statSync` 实测 = **352,118 B**（差 **90 B**；该行讲的是 neoforge primers 台账脱钩，与 manifest 无关，只是同属 `S17` task 点名）。`temp/**` 不入库 ⇒ **未改该文件**、**未动 `data/**`**。复核命令：`node -e "console.log(require('fs').statSync('data/neoforge_primers/26.3.md').size)"`。as-of 2026-09-25 | 用户：`S17` 判「降级为按设计无需」（写盘器已不在盘、读侧腿已存在、本轮已证其可红、三口径为 0 / 0 / 3 且第 3 口径按设计合法）还是「仍欠」。另裁是否放宽采集面（`^ {2}` → 兼容任意缩进，或改吃 `JSON.parse` + 键计数双机制）——那是**改判据**，须配同样三记反证 |
| `L41` 第 33 轮的门的一轮：`G-manifest-dup-key` 采集面放宽 + `audit-data-consistency` 退出码定性 + S22 三方腿（判据⑤）—— **立腿即抓到 4 条真实的 loader 过度声明** | **任务 A（放宽判据，不是削弱）**：`mcp-server/scripts/audit-data-consistency.mjs` 新增可投毒的 `collectTopLevelJsonKeys`（字符串态 + 括号深度，只收深度 1 的键，**解完转义再比**）与 `manifestKeyCensus`（第二机制 = `JSON.parse` 顶层唯一键集；两机制失配另报 `MECHANISM_DISAGREE`），旧采集正则「行首恰好 2 空格」退役 —— 那条形状对 4 空格 / tab / 压缩成一行的 `_manifest.json` 采 0 条即**静默放行**（第 32 轮 P2 抓到的「恒真于空」）。四记反证在 `mcp-server/test-audit-data.mjs :: testManifestDupKeyLeg`：① **4 空格 / tab / 单行压缩 + 重复键 ⇒ 必红**，且每记投毒前先自证「旧 2 空格正则在该形状采 0 条」（不自证就不构成反证）；② **现盘 6 份干净 ⇒ 必绿且非零**（分母 = `data/forge_{1.16.5,1.17.1,1.18.2,1.19.4,1.20.1,1.20.4}/forge-docs/_manifest.json`，顶层键 occurrence **290** = 40/40/33/38/71/68、唯一 **290**、重复 **0**，as-of 2026-09-25；整树审计 `scanned=48 / ERROR=0 / WARN=102` rc=0）；③ **采集 0 条 ⇒ `G-manifest-collector-zero` + `COLLECTOR_RETURNED_ZERO` + rc=1**（`{}` 夹具）；④ **不判对照**：重复键写在**嵌套层** ⇒ 顶层无重复 ⇒ 不得判红（钉住深度过滤，防「凡重复键就红」的过头判据）。**额外收益**：解转义之后，JSON 视为同一个键的写法（反斜杠转义引号 vs `\u0022`）这种**等价重复**从今必红 —— 旧做法按原文比字面，与缩进无关地漏判。 **任务 B（退出码到底是不是真门）**：`counts.ERROR > 0 ? 1 : 0` 这一腿**本来就是真门**（夹具投毒 rc=1，不是本仓踩过的那例「打印 11 FAIL + 5 NOFILE 却 exit 0」）；假绿在**空跑** —— 修前实测两条路径均 **rc=0** 且 payload `scanned:[]`（`--data-root=<不存在>` / `--platform=forge --version=9.9.9`），消费方会把「过滤器拼错、`MC_SKILL_DATA` 没指对」读成「查过且干净」。修后加 `Z-scanned-zero` ERROR ⇒ **同两条命令 rc=1**（`ERROR=1 / WARN=0`），并配「同一 data-root 命中真实档且干净 ⇒ 仍 rc=0」的对照。⚠️ 该修复**翻转一条旧断言**：`testEmptyFixture` 原文钉「空 fixture ⇒ rc=0」= 把假绿当期望值，已按新语义改并在注释留因由；未删任何判据、未动 `data/**`（0 字节）。 **任务 C（S22 三方腿 + 桥落点）**：真源 = `temp/plan-20260921-fix.json` · `stories[id=S22].tasks`，实测 **10 条**（计划 JSON 只有 `feature/source/codebase/stories` 四键、27 个 story、无 status 字段），逐条判 = **已成 9 / 真欠 1**（欠的第 10 条「补门」本轮做完，含 (a) 腿 `VALIDATE_COMBOS` 的 quilt 组 —— 现扫已在 `scripts/resolve-lib-skills.mjs` 内 ⇒ 那条也已成）。新**判据⑤** 落 `mcp-server/scripts/assert-lib-session-resolve-parity.mjs`：口径 = 构建面（2026-09-22 裁定④），三张面 = `SKILL.platforms`（36 份）↔ 生成物 `library-catalog.ts` 的 `loaders`（50 行）↔ `mcp-server/data/lib-manifests/all.json` 逐发布文件行（**48 slug / 2870 行**；顶层数组、行在 `entries` 里 —— 第一遍按顶层 `loader` 数出 **0** 条 quilt 是假阴性，与该 task 原文记的「grep 假阴性」同类坑；改读嵌套后 = **25** slug 有 quilt 行 / **509** 条，与 task 自述逐字相符）。**`modrinthSlug` 桥落点 = 解析侧三档**（1° 手填 `SKILL_TO_SLUG` → 2° 生成物行 `skillId === SKILL 名` → 3° 行 `id === authored/lib-<去 mc- 名>`），**没有**给 36 份 SKILL.md 新增 `modrinthSlug` 键（那要同时动库面 frontmatter + 7 个宿主镜像 + 重跑 `scripts/sync-skills.ps1`，属数据面另轮 ⇒ 在此点名「欠的是哪一个映射」：欠的是 SKILL 侧的 slug 显式键，当前靠生成物）。**立腿即抓到 4 条过度声明**（判据非装饰）：⑤A `authored/lib-libx` 声明 fabric 而该 slug fabric 行 0 条、⑤A `authored/lib-puzzles-lib` 声明 quilt 而 quilt 行 0 条、⑤B `mc-cloth-config/quilt`、⑤B `mc-fabric-language-kotlin/quilt` ⇒ 四条全进 `TRIAD_OVERCLAIM_DEBT`（逐条 basis；无 basis / 指向不存在的行或源稿 / 登记的违规已消失，三种「账与判面失步」都判红）。**⑤A 的成因是新发现**：生产者 `mcp-server/scripts/build-library-catalog-from-authored.mjs:200` 的 `loaders` 取自 authored 短文的 `meta.loaders`、**不是** all.json ⇒ 「重跑生产者就能修」不成立，须先改短文（S22 task 9 与本仓 `L40` 都没料到；该文件也**不在**仓库根 `scripts/`）。**不判方向显式列 INFO**（**5** 条欠账：bookshelf-lib 的 fabric+quilt、modern-ui/patchouli/satin 的 quilt、curios 的 fabric 有构件行而 `catalog.loaders` 没点名）⇒ 只打印、**不进退出公式**（那是「少推荐」，修它 = 动 authored 源稿 + 重跑生产者）。覆盖面自证：桥通 **29** 份（无桥 7）、实判 腿A **117** + 腿B **78** = 195 组、地板 40 slug / 2500 行 / 45 catalog 行 / 25 桥通 / 40 判组（全用 `<`，无等式棘轮），任一分母为 0 ⇒ `COLLECTOR_RETURNED_ZERO`。 **反证与台账（as-of 2026-09-25）**：`temp/ralph-20260922/_r33-poison-gate5.mjs` 五记 —— P0 逐字副本 ⇒ 绿（不判对照）· P1 关掉债务通道 ⇒ rc=1 且 4 条现形 · P2 腿 A 判据恒假 ⇒ 债务失步腿随即红（说明红不只来自债务） · P3 地板 2500→99999 ⇒ 红 · P4 采集器喂空 ⇒ 红；`GATE5_POISON_OVERALL=PASS`，且「生产文件未被改动 = true / 含 POISON 残留 = false / 副本已删 = true」。门 `--selftest` 现 **46 例（27 投毒必红 + 19 正对照）** rc=0、main rc=0；`node mcp-server/test-audit-data.mjs` **21** 条 ok rc=0（原 20）；`cd mcp-server && node test-scripts.mjs` rc=0；全链 `node temp/ralph-20260922/_r31-run-all-legs.mjs` ⇒ **62 腿 · 绿 62 · 红 0 · RUNNER_EXIT=0**；`_anchor-lint.mjs` rc=0（引用名 **239** / 缺失 **0**）。两处**自述改口**：门头注「三方一致性未纳入本门（需先定桥落点）」按实况重写；头注「as-of 2026-09-24 带该键 **1** 份」现扫 = **9** 份（S22 步2 的八份窗口 + `mc-caelus`），地板仍取 1（只许 `<`）但注明「别再按 1 读现状」。 **未做**：4 条债务的上游复核（`query_upstream_releases` 联网；`mc-cloth-config/quilt` 那条 S22 原文明点「不要直接删 quilt」）· 给 SKILL.md 加显式 `modrinthSlug` · 修 ⑤A 两条 authored 短文 + 重跑生产者 · 5 条 INFO 欠账方向 · 未 `git add`/commit（本行所涉 `audit-data-consistency.mjs` 是 **tracked 件、只差 commit，不需 `git add` 新文件**，与 `L10` 那批 untracked 件不同号）· 未跑聚合 `npm test`（按第 31 轮裁定走无短路逐腿）。复核命令：`node mcp-server/test-audit-data.mjs` · `node mcp-server/scripts/audit-data-consistency.mjs; rc=$?`（0）· `node mcp-server/scripts/audit-data-consistency.mjs --platform=forge --version=9.9.9; rc=$?`（1）· `node mcp-server/scripts/assert-lib-session-resolve-parity.mjs --selftest`（0）· `node temp/ralph-20260922/_r33-poison-gate5.mjs`（0）。报告 `temp/ralph-20260922/round-33-gates-manifest-exit-triad.md`，日志 `temp/ralph-20260922/logs/r33-verify.log`。 | 用户：4 条 ⑤A/⑤B 债务的上游复核要联网（`query_upstream_releases`），本行所涉 `audit-data-consistency.mjs` 是 tracked 件、只差 commit（提交由用户执行，agent 不 `git add`/commit）。排期：给 36 份 SKILL.md 加显式 `modrinthSlug`（动库面 frontmatter + 7 个宿主镜像 + 重跑 `sync-skills.ps1`）、修 ⑤A 两条 authored 短文后重跑生产者、以及 `L42` 点名的 5 条反向欠账。 |
| `L42` 第 34 轮：判据⑤ 的 4 条债务已清零，剩下的是**构件面新鲜度**与 **5 条反向欠账** | **① 欠一次构件面重抓（`data/lib-manifests/all.json`，本轮 `data/**` 零写）**：快照 as-of 2026-09-16，实测上游同日 `query_upstream_releases --source=modrinth`（`fetchedAt` 2026-09-24T18:24Z）`puzzles-lib` **total 645** 条发布 vs 快照仅 **15** 行、`libx` **154** vs **15** 行、`cloth-config` **161** vs **113** 行、`fabric-language-kotlin` **56** vs **237** 行（最后一条反向 = 快照比上游窗口宽）。判据⑤ 现按「快照即构件面」判，快照滞后只会造成**假阴性**（本仓有 quilt 行的 25 slug / 509 行是否仍成立未复算）。欠的动作 = 主代理/用户排一次 `data/lib-manifests` 重抓，随后复跑判据⑤ 看 4 条清零后是否又冒出新的过称。**② 反向欠账（构件面有行、`catalog.loaders` 未点名 = 少推荐，判据⑤ 只 INFO 不判）—— 已闭环（第 37 轮 2026-09-25 清；本行写时是 5 条，第 36 轮桥吃多值 slug 串后涨到 **6** 条，六条逐名去向与实测数见 `L45`）**：口径 = 腿 B 的 `onlyBuild` 分支；正解 = 改对应 `community_knowledge/authored/lib-*.md` 的 `meta.loaders` 后重跑 `mcp-server/scripts/build-library-catalog-from-authored.mjs`（禁止手改生成物）⇒ **本轮已按此做完：4 条补点名 + 2 条判据面不补、但已在正文逐 slug / 逐 versionType 披露，INFO 6 → 2**。⚠️ **本行原句要求的「`satin` / `patchouli` / `modern-ui` 的 quilt 行需逐条上游复核后才动」这一步第 37 轮未做**（该轮授权面禁联网）⇒ 定性依据 = 盘上快照 `all.json`（as-of 2026-09-16）的 release 行数 + 点分 `gameVersion` 数值序上界，并把「各自 release 止于何处」逐条写进对应短文正文；**若日后上游复核推翻快照（如某 slug 的 quilt 行系已撤回的历史发布），按快照点名的四条要回退**（复核命令 = `node mcp-server/scripts/assert-lib-session-resolve-parity.mjs` 看 INFO 段是否回到 6 条，逐名见 `L45`）。**③ 生产者丢补丁的坑已修**：`build-library-catalog-from-authored.mjs` 原先只保留 `verifiedApi`/`supportedVersions`/`officialUrls`，重跑会把 2026-09-16 直接打在生成物上的 `notes` 3 条与 `modIds` 的 `pal` 段回退（丢 `pal` ⇒ G1 把 120 个 `io.github.ladysnake.pal.*` 类判成冒领他方包根）；本轮补 `loadExistingNotes()` / `loadExistingModIds()`（并集）。**根治仍欠**：那 4 处补丁的正解是把它们写回 authored 源稿，之后可撤掉这两个保留钩子。复核 `node mcp-server/scripts/assert-lib-session-resolve-parity.mjs`（债务表剩余 0 条）· `node temp/ralph-20260922/_r34-debt-biting.mjs`（改前 4 条逐字必红 + 带旧表必绿）· `node mcp-server/scripts/assert-lib-ownership.mjs`（G1 rc=0）| 排期/用户：① 构件面重抓 `data/lib-manifests/all.json` —— 属 `data/**` 写面且需下载授权，agent 不代跑，重抓后复跑判据⑤ 看是否又冒新的过称；② 5 条反向欠账要改 authored 源稿的 `loaders` 再重跑生产者（禁止手改生成物），其中 `satin`/`patchouli`/`modern-ui` 的 quilt 行是否属「上游已停更的旧 Quilt 发布」须**用户逐条上游复核**后才动；③ 根治 = 把那 4 处补丁写回 authored 源稿，之后可撤 `loadExistingNotes()`/`loadExistingModIds()` 两个保留钩子。 |
| `L43` 判据⑤腿 B 的**桥覆盖面**（第 35 轮）+ 两条**口径待裁** | **① 口径待裁（`platforms` 语义）**：`platforms` 到底读作「**指引适用于该 loader**」还是「**该 loader 有发布构件**」—— 两种读法各自自洽，差别就在第 34 轮那种案子上（Cloth Config 无 quilt 构件行，但 Quilt 工程能不能用它）。判据⑤ 现按后者（构建面口径，2026-09-22 裁定④）判，**而本仓 `data/quilt_*/` 里找不到「Quilt 能加载 Fabric 库/构件」这句话的出处**（`quilt-mod-json.md` 只讲 quilt.mod.json 规范与 RFC）⇒ 该语义**从未被定义**。⚠️ **本行不是说第 34 轮做错了** —— 它按当时的判据与取证（上游 Modrinth 全量）做得对；欠的是这条判据的语义。`需要谁拍板` = **用户**，或「谁先拿到上游 Quilt 原文（联网/浏览器，非本仓）谁再判」；两侧都不许由维护侧代裁。现状：`fabric-only` 组 8/10 份写 `[fabric, quilt]` 且**那 8 份确有 quilt 构件行**（实测 trinkets 11 / polymer 50 / satin-api 23 / impersonate 9 条 ⇒ 「组内一致是被口径逼出来的」这条推测已被构件面反证，见 R135 §⑥）。 **② `mc-libgui` 案（现状登记，带分母 + 口径 + as-of）**：定性 = 写单 §2 四可能里的**第 3 种「桥根本没通」**，实测（探针 `temp/ralph-20260922/_r35-probe.mjs` + `_r35-probe2.mjs`，as-of 2026-09-25）：`mc-libgui` `platforms`=[fabric,quilt]，构件面 slug `libgui` **总 1 行 / fabric 1 / quilt 0**，而生成物行 `authored/lib-libgui` 的 `modrinthSlug: ""` ⇒ 2°/3° 两档自动桥**都要求该字段非空** ⇒ 桥返回 null ⇒ 腿 B `noSlug += 1; continue` **静默跳过**（既不进债务表也不进 INFO，与写单所见一致）。同类被静默跳过的还有 `mc-server-translations`（构件面有同名 slug、只有 fabric 行）与 `mc-spruceui`（构件面名字是 `spruceui-obsidianui`，只能从行 id 导出）⇒ 本轮补**第 4° 桥「构件面正名」**（只认逐字等于构件面 slug 的名字，禁止模糊/子串匹配），桥通 29 → **32 / 36**、腿 B 实判 76 → **85** 组，一次现形 **6 条**过称（libgui/quilt、server-translations/forge+neoforge、spruceui/forge+neoforge+quilt）⇒ 全部进 `TRIAD_OVERCLAIM_DEBT`（0 → 6，逐条 basis，`data/**` 与库面 frontmatter **零写**）。⚠️ 判面强度要点名的两条：`libgui` / `spruceui` 都是 Cotton 系、**Modrinth 已下架**（该 SKILL 正文自述），快照那 1 行来自 GitHub Releases 单个文件 ⇒ 「快照 0 行 quilt」是**弱证据**，禁止据此改语料（与 `L42` 的「欠一次构件面重抓」同源）。`需要谁拍板` = 排期（重抓后复跑本腿）。 **③ 桥覆盖欠项（本轮修掉一半）**：新增 `bridgeCoverageCheck` 覆盖下界腿 ⇒ 未通桥**逐名**打印 + 必须逐名登记在 `BRIDGE_BLINDSPOTS`（现 4 份：`mc-compat-jei` 多值串 slug `"emi,jei,rei"` 不被单 slug 解析、`mc-lib-catalog` 索引稿、`mc-script-server`/`mc-script-ui` 基岩 Script API），新增未登记 / 登记了却通了 / 无 basis / 条数越上界（`FLOOR_TRIAD_UNBRIDGED_MAX` = 5）四种都判红。剩下的「补桥」欠 = 让桥吃**多值 slug 串**（`mc-compat-jei` 的 fabric/forge/neoforge 三组至今未被判过）【**2026-09-25 第 36 轮已做完这一欠**：`splitSlugList` + `mergedFace` 让桥按逗号拆开取 loader 并集 ⇒ 三组真被判、腿B 85 → 88、未通桥 4 → 3 份、`mc-compat-jei` 已按「登记了却通了」那条腿从 `BRIDGE_BLINDSPOTS` 出表，实况见 `L44`】与「给 36 份 SKILL.md 加显式 `modrinthSlug`」（= `L41` 原欠项，动 frontmatter + 7 宿主镜像 + 重跑 `sync-skills.ps1`）。`需要谁拍板` = 前者属**改判据**（要配反证，另轮）、后者 = 排期。 **④ 门头注的数字错值已改口**：旧注「桥按 2°/3° 已能覆盖 **33/36** 份，余 3 份」是错的（第 33 轮 `L41` 实跑记的就是 **29/36、无桥 7**）⇒ 三处（顶注 / 判据⑤段注 / `FLOOR_TRIAD_BRIDGED` 的错误文本）已按现扫改写。复核命令：`node mcp-server/scripts/assert-lib-session-resolve-parity.mjs`（rc=0，末三行打印桥档分解 / 未通桥逐名 / 债务逐名）· `node ... --selftest`（**56** 例）· `node temp/ralph-20260922/_r35-poison-bridge.mjs`（4 记生产级投毒 `BRIDGE_POISON_OVERALL=PASS`）。 | **① 语义待裁 = 用户**（或由「谁先拿到上游 Quilt 原文谁再判」）；拿不到原文就维持构建面口径，维护侧不得代裁。 **② `mc-libgui` / `mc-spruceui` / `mc-server-translations` 这 6 条 = 排期**（`L42` 的构件面重抓做完后复跑本腿，再决定补构件行还是摘 `platforms`；本轮**未**改任何语料）。 **③ 补桥的两项**：多值 slug 串（`mc-compat-jei`）= **改判据**、须配反证、另轮；SKILL 侧显式 `modrinthSlug` = 排期（同 `L41`，动 frontmatter + 7 宿主镜像）。 |
| `L44` 第 36 轮：§未排期清单**表形**立了门 + 判据⑤ 的桥吃**多值 slug 串**（`mc-compat-jei` 三组自本轮起真被判） | **本轮实测**（as-of 2026-09-25）。表形：修前 = 43 行 / 首 1 末 42 / 断号 41->43 与 43->42 / 非 3 格行 2（`L41`、`L42` 的第三列**整列缺失**，两轮各写坏一次、全仓无门报警）；修后同口径复扫 = 43 行 / 首 1 末 43 / 断号 0 / 非 3 格 0 / 表内空行 0。新门 `mcp-server/scripts/assert-backlog-table-shape.mjs` 只判**形状**（列数 ≠ 3、第三列为空、号序非逐行 +1、首号 ≠ 1、行数 < 40 地板、采 0 行 `COLLECTOR_RETURNED_ZERO`、锚缺失 `ANCHOR_MISSING`），`--selftest` 13 例 = 9 投毒必红 + 4 不判对照（含「合法改词去掉 as-of 不红」「合法加一行 L44 不红」）；**第一次跑在坏盘上即 rc=1 红 3 项**，修完才绿，已进 `test-scripts.mjs` 的 `REAL_RUN_GATES` + `SELFTEST_GATES`。判据⑤侧：桥按逗号拆多值 slug（`splitSlugList` + `mergedFace` 取 loader **并集**，仍只认构件面逐字存在的 slug、不猜名字）⇒ 桥通 32 → **33** 份、腿B 实判 85 → **88** 组、腿A 115 → **118** 组、未通桥 4 → **3** 份（`mc-compat-jei` 出表），**0 条新过称**（`TRIAD_OVERCLAIM_DEBT` 仍 6 条、6 条在用）、INFO 5 → **6** 条（新增 `mc-compat-jei` 的 quilt「少推荐」方向，不判）；`--selftest` 56 → **65** 例 rc=0。复核：`node mcp-server/scripts/assert-backlog-table-shape.mjs`（rc=0）· `node mcp-server/scripts/assert-lib-session-resolve-parity.mjs`（rc=0，末三行打印桥档/未通桥逐名/债务逐名） | **无人欠做**（本轮两件事都做完）。仍欠两条：① 本门**只管形状**，`现状` 列里的数字与口径对不对**仍无人判** —— 这是本轮**故意**不做成红腿的（把 prose 口径做成棘轮，一次合法改词就会让无关门红，同 `L18` 的形状）⇒ 要判得另立「README/表内计数 ↔ 盘面复算」门，属**排期**；② `L43` 点名的「给 36 份 SKILL.md 加显式 `modrinthSlug`」仍在（动库面 frontmatter + 7 个宿主镜像 + 重跑 `sync-skills.ps1`）⇒ **排期** |
| `L45` 第 37 轮：判据⑤ 的 6 条「少推荐」欠账逐条定性（4 补点名 / 2 不补带披露），并点名**合并构件面会让一家替另一家背书** | **六条去向**（口径 = 2026-09-22 裁定④ + 2026-09-24 release 延伸；数据 = `mcp-server/data/lib-manifests/all.json` 快照 as-of 2026-09-16，分母 = 48 slug / 2870 行；上界一律按**点分 `gameVersion` 数值序**取，字典序会把 `22w42a` / `1.19-pre1` 排到 `1.20.4` 之上）：① **补点名** `authored/lib-bookshelf` += `fabric`（19 行 / **19 release**，上界 26.2，与 forge 行 fileName 交集 **0** = 独立 Fabric 构件）+ `quilt`（10 行 / 10 release，上界 **1.21.1**；9 行 fileName 是 `Bookshelf-Fabric-*`、**1 行是 `Bookshelf-Forge-1.18.1-12.0.23.jar` 被快照标成 quilt ⇒ 该行不算证据**，剥掉仍有 9 release）；② **补点名** `authored/lib-modern-ui` += `quilt`（8 行 / **8 release**、0 beta，上界 **1.21.3**，其余三家到 26.1.2 ⇒ 短文里点名「26.x 无 quilt 行」）；③ **补点名** `authored/lib-patchouli` += `quilt`（13 行 / **10 release** 上界 1.21.1 + 3 条 26.1.x 记 **beta** ⇒ 正是 2026-09-24 裁定点名的 patchouli 形状，已按「beta 构件至 26.1.2、无 release 支撑」写进正文）；④ **补点名** `authored/lib-satin` += `quilt`（23 行 / **20 release** 上界 1.21.1 + 3 alpha 至 1.21.4；fabric 与 quilt 的 fileName **完全重合**（各 12 个唯一名、交集 12）= 同一批 jar 双标 ⇒ 顺带把短文里 5 处「仅 Fabric」改成「Fabric / Quilt」）；⑤ **不补 + 披露** `authored/library-integration-jei-emi` 的 `quilt`：该档在生成物里是 `modrinthSlug: "emi,jei,rei"` 的**三库合并行**（authored 侧写作 `jei, emi, rei`，生产者按字典序归一），13 条 quilt 行**全部**来自 `emi` 一家（`jei` quilt **0** 行、`rei` 该 slug 根本不在构件面）⇒ 合并面不构成「库族支持 quilt」的证据，改为正文逐 slug 列表；⑥ **不补 + 披露** `authored/lib-curios` 的 `fabric`：唯一 1 行是 `1.16.5` / `curios-fabric-0.0.13-1.16.5.jar` / **`versionType=beta`（release 0）** ⇒ 按 2026-09-24 裁定「只有 beta ⇒ 不点名」，正文披露（当年把 `terrablender` 的 quilt 移出窗口的正是同一条裁定）。 **顺带点名的新局限（本轮未修）**：读 `jei` 自己那面 = fabric 19 行 **0 release**（17 beta + 2 alpha）、neoforge 15 行 **0 release**（全 beta）、forge 39 行 14 release（上界只到 **1.18.2**）⇒ 该合并行之所以能点名 fabric/neoforge，全靠 `emi` 的 release 行**替 `jei` 背书**；判据⑤ 腿 A / 腿 B 都按 `mergedFace` 并集判 ⇒ **看得见「族里有没有」、看不见「是哪一家有的」**，要判得把合并行拆成逐 slug 判（属**改判据**，须配反证 + 另轮）。 **验证台账（as-of 2026-09-25）**：`node mcp-server/scripts/assert-lib-session-resolve-parity.mjs` rc=**0**，INFO **6 → 2**（只剩 ⑤⑥ 两条已定性的「不补」，逐名打印）· 腿A 实判 118 → **123** 组（+5 = 四条点名各多判一行）· 腿B 88 组不变 · `--selftest` **65** 例 rc=0 · `assert-lib-ownership`(G1) rc=**0** 且四钉值 `catalogEntries 50 / summaries 48 / verifiedApiKeys 2632 / attestedRoots 99` **全部未动** · 生成物 `git diff --numstat` = **10 加 / 13 删**，其中**本轮净差 = 4 行 `loaders`**（逐条 = `authored/lib-bookshelf` / `lib-modern-ui` / `lib-patchouli` / `lib-satin`；其余 6/9 是第 34 轮那批未提交的重排噪声）· `notes` 3 条与 `modIds` 的 `"pal"` 段在重跑后**仍在**（生产者 `loadExistingNotes()` / `loadExistingModIds()` 两钩子生效；另实测该生产者**已幂等** = 改前重跑一次与旧文件逐字相同 ⇒ 上述 4 行差额全部归本轮）· **消费面** `check_dependencies` 的 `detectedLibraries[].loaders` 点名对（库,loader）**14 → 19**（夹具 = 六库同在一个 Fabric 工程依赖里；`lib resolve` 面**未动** —— 本轮只改 `meta.loaders`，`platforms` 一字未改，那条语义仍等裁定）。`data/**` 零写；动了 `src/**` ⇒ `npm run build` rc=0 ⇒ **用户需重载宿主 MCP**。 | 排期：① 「合并行让一家替另一家背书」要做成判据得把 `mergedFace` 拆成逐 slug 判，属**改判据** + 另轮；② ①②③④ 四条补点名**未做上游复核**（本轮授权面禁联网）⇒ `L42` ① 的构件面重抓做完后复跑本腿，快照被推翻则四条回退；③ `rei` / `roughlyenoughitems` 至今不在构件面 = 判面缺失（同 `L42` ①）。 |
| `L46` 第 38 轮：`S21` 四条按实况收口（`t0`/`t1`/`t3` 已成 · **`t2` 挂起**）+ 判据⑤ 的 INFO 加「已定案」通道 | **`S21` 收口态 = 部分（t0/t1/t3 收口，t2 挂起）**。四条口径 = 计划原文 `temp/plan-20260921-fix.json` → `stories[20].tasks[0..3]`（本轮 `node -e` 直读 JSON，输出 `temp/ralph-20260922/logs/r38-probe.log`，as-of 2026-09-25）。**`t0`**（`forge-event-system-practices.md:87` 把 2571 帖写成「显式 BY-NC-SA」）现文已是第三态措辞、与同文件 `:6` / `:12` 同向 ⇒ **已成**，本轮 0 写。**`t1`**（schema 版本口径）**正文对、计划过期 ⇒ 未改文件**：计划称 `:21` 让读者「rebuild as schema v3」，而现文写的是「重建为 schema **≥v3**（v3 是运行时可读的下限，不是笔误）… 本仓现产 v4 —— 生产侧 `mcp-server/scripts/_lib/build-yarn-sqlite.mjs:27` 钉 `SCHEMA_VERSION = "4"`」。三件实况互证：① 生产侧常量逐字在 `:27`，**计划点名的路径少了 `_lib/`** ⇒ 写手按 `scripts/build-yarn-sqlite.mjs` 与 `mcp-server/scripts/build-yarn-sqlite.mjs` 两路径 grep `SCHEMA_VERSION` 均 0 命中，那 0 命中是**引用行号/路径漂移**，不是「常量不存在」；② 运行时 `mcp-server/src/mappings/yarn-sqlite.ts` **没有任何数值比较** —— `lookupField` 判的是结构式 `dbHasFieldsTable`（`:337-344`，`SELECT 1 FROM fields LIMIT 1`）与 `dbHasSeargeFields`（`:346-353`），`schemaVersion` 只在 `:386` 读出、`:404` 印进提示（提示 `:405` 原文 = 「请运行 npm run build:yarn-sqlite 重建为 schema v4」），全仓唯一的数值比较在门侧 `mcp-server/scripts/assert-index-consistency.mjs:425`（`Number(meta.schemaVersion ?? 0) >= 3`，配套注释 `:33`「schemaVersion>=3 还必须有 idx_*_official 索引」）⇒「v3 起才有 `fields`」在运行时与门两侧都成立，`≥v3` 不是笔误；③ `data/fabric_1.21.1/mappings/yarn-mappings.sqlite` meta **只读**实测 `schemaVersion = 4`、表含 `fields` + `searge_fields` ⇒ 三者对齐后判 **`t1` 已成、正文一词未改**。**文件名 / id `mapping-fields-v3` 未改名**，引用者已 grep 全（3 处：`community_knowledge/README.md:89`、`community_knowledge/indexes/index-l0.json:1524` 与 `:1526`（生成物）、`authored/mapping-fields-v3.md:2` 自身；`mcp-server/scripts/assert-*` 全树对该名 0 命中 ⇒ **没有门按名引用**）。**`t3`**（saveddata URL）**已成**，但**计划 `t3` 自己的假设也只对一半**：计划说「URL 含 `/docs/` ⇒ 审计的「缺前导 `/docs/`」机制推理错」—— 机制推理确实错，可那个 URL **真的坏了**（`…/1.20.4/docs/…` = 404、`…/docs/1.20.4/…` = 200，2026-09-24 `curl.exe` 实测），现文 `authored/saveddata-world-persistence.md:113` 已是 200 那条并带实测注记、`L5` 闭环 ⇒ 本轮 0 写。**`t2` 挂起（本行是它的登记处）**：两件事分开 —— (a) **误读已纠正**：「`ATTRIBUTION.md` 的 3836 行仍缺」是反向引用（计划原话 `ATTRIBUTION.md has no 3836 entry at all`），已改写 `L31`；(b) **真欠项仍在**：`t2` 禁止的是「照审计的数批量改」，要求的是**换成逐行取证 pass**（逐条记录每个「显式」从哪读到），那件至今没做，且只能靠逐帖打开（mcmod 对脚本客户端 403/429）⇒ 与 `L35`（用户 2026-09-24 按反爬关闭 `C-1`）**显式挂钩、随之挂起**。推进只有两条路：用户授权一次浏览器逐帖，或永久维持 `:36` / `L35` 那类自我更正。**禁止**把「计划说不要批量改」读成「`t2` 已成」，也禁止据此写「`S21` 成立」。判据复核命令：`node -e "…stories[20]…"` 直读计划 JSON + `grep -n SCHEMA_VERSION mcp-server/scripts/_lib/build-yarn-sqlite.mjs` + 只读 sqlite meta（`temp/ralph-20260922/_r38-probe.mjs`）。**判据⑤ 的 INFO「已定案」通道**（`mcp-server/scripts/assert-lib-session-resolve-parity.mjs` 新增 `INFO_DEBT_DECIDED` + ⑤C 三把尺：无 `basis` ⇒ 红 / 指向不存在的源稿 ⇒ 红 / 与现盘 INFO **双向失步** ⇒ 红；命中 ⇒ 措辞改「已定案（见 basis）」并把**未定案 / 已定案分开计数**，INFO 逐条键由「按 skill 合并一行」改为按 `(skill,loader)` 一条）：现盘 `INFO 总面 2 条 = 未定案 0 + 已定案 2`（`mc-compat-jei/quilt`、`mc-curios/fabric` = `L45` 定案「不补」那两条），**INFO 恒不进退出码**未破（main rc=0）。三记反证：① `basis` 留空 ⇒ 红；② 表里记一条实际不存在的欠账（夹具 `mc-libgui/fabric`）⇒ 红、指向不存在的源稿（`mc-gone-away`）⇒ 红；③ `S22_INFO_DECIDED=off` 让现盘按空表读 ⇒ 那 2 条**逐名回落到未定案**（`temp/ralph-20260922/logs/r38-gate-off.log`）⇒ 抑制不是吞掉，且该开关**只会更吵、不会更静**，故不是消音通道。另钉两条：「分桶守恒」（`未定案 + 已定案 ≠ INFO 面` 即红）与「现盘接线对照」（驱动漏传定案表 ⇒ selftest 红）；纯函数 `skillOverclaimCheck` 的第 5 参 `decided` **默认 `[]`** 而非真表 —— selftest 的小夹具声明面里没有 `mc-curios` / `mc-compat-jei`，拿真表进夹具会被「指向不存在的源稿」腿误咬（本轮第一版就这么红过一次）。`--selftest` 65 → **73 例（41 投毒必红 + 32 正对照）** rc=0。⚠️ 点名一条**二手转述活了 11 轮**：「3836 行仍缺」自第 27 轮起被逐轮抄递（`_audit-S14-S27.md` → 各轮写单 → `L31` → 第 36/37 轮报告），本轮第一次直读计划 JSON 才发现方向反了。 | **`t2` 要推进 = 用户**（授权一次浏览器逐帖取证；或明确「永久维持自我更正」，届时把 `L31`/`L46` 改写成「不做 + 复核命令」）；`t0`/`t1`/`t3` 无需拍板（本轮正文一字未改）；⑤C 的打印策略本轮按「**仍逐名打印、只换措辞 + 分开计数**」落，若要改成「已定案就不打印」= 另裁（那会牺牲「清单被人读到」这一条，与 `L13` 的教训相冲）。 |
| `L47` 第 39 轮只读审计发现：**两份 `temp/` 审计文件的结论已被后续轮次超越或本身为假** ⇒ 不改 temp 历史行，现行读法只在本表 | **第 40 轮逐处实测**（as-of 2026-09-25）。① `temp/ralph-20260922/_audit-S1-S13.md` 给 S12 判「部分 · `assertDataUsable` 在任何测试文件 0 引用、`path.ts:429` 自述是假话」——**已被第 28 轮超越**：`mcp-server/test-core.mjs:75` 本轮读到 `import { resolveDataDir, diagnoseDataPaths, assertDataUsable } from "./dist/utils/path.js";`，`:8383` = `const red = assertDataUsable(redRoot);`、`:8397` = `const green = assertDataUsable(greenRoot);`（双向假树夹具整节在 `:8352` 之后，写单给的区间 `:8352-8401` 与实测一致），`:8354` 注释已把旧的「0 引用」写成**过去时**；`mcp-server/src/utils/path.ts:430` 自述「2026-09-24 起真存在」、`:433` 保留旧误述为历史 ⇒ 该档**已闭环**，审计表那句是过期快照。② `temp/ralph-20260922/_audit-S14-S27.md` 称「`.done` 待裁项盘上未见裁定文本」**为假**：裁定就在 `mcp-server/scripts/_lib/bedrock-corpus.mjs:84`（本轮逐字读到该行正文 = 「S14-T5 裁定（2026-09-22 第 8 轮）：**`.done` 不该出现在 processed/ 里**，所以这里不再制造它」，三条凭据在 `:85-88`），代码已按裁定改（`:81` 注释自述旧现象「实测一轮 scriptapi 留下 3 个 .tmp + 3 个 .done」），且 `find data -name '*.done'` 本轮实扫 = **0** 件 ⇒ 无悬空待裁项。两份审计文件本体在 gitignored 的 `temp/`、不入库 ⇒ **不改它们的历史行**，本行只登记「该结论已被超越 / 为假 + 现行读法在哪」。 | 无人欠做（现行读法已落本表）。排期：谁要把 temp 审计面归档（`openspec/` 整树 0 文件受纳管，见本节开头第三条），得先解决纳管 ⇒ 属 `L10` 的用户侧动作 |
| `L48` **全局表病（不点名谁错）**：本仓 task 编号有**三套并存约定** ⇒ 裸字母号跨表对齐**必错位一位** | **本轮实测三处口径互不相同**（as-of 2026-09-25）：`temp/ralph-20260922/_audit-S14-S27.md` 通篇 **0-based** `T0…Tn`（编号即其 `forEach` 下标）、本表 `L31`/`L46` 用 `t0…t3`（0-based）、而真相源 `temp/plan-20260921-fix.json` 的人读引用习惯 **1-based** `t1…tn` ⇒ **同一件事在不同表里差一位**。代价本轮就踩过：第 40 轮写单标题把 neoforge/1.21.11「point at the … factory forms」那条残腿叫 `t1`、第 39 轮审计一号叫它 `t2`，主代理据前者指控后者「记错编号」后自纠 —— 那句打印用的正是 0-based 下标 ⇒ **指控本身不成立**，双方都可能没错，只是基数不同。**规矩（长期，不是本轮注脚）**：登记、派单、报告一律引**「计划原文逐字片段 + 该 task 的一句逐字」**，禁止只写字母号；非要引用他人表里的编号时，必须同时点名**该表文件**与其**基数（0-based / 1-based）**。⇒ 现状 = 规矩只在本表，`AGENTS.md` / `CONTRIBUTING.md` 正文尚无该条。 | 维护者拍板是否把该规矩提进 `CONTRIBUTING.md` §验证纪律或根 `AGENTS.md`（本轮刻意只落表，不动那两处）；机器判据属**排期** —— 没有任何门判「引用的 task 编号对不对」，连规则里引用的 `文件:行` 行号也无门判（`assert-workflow-hitl` 那条路径腿只 `existsSync`） |
| `L49` **§未排期清单整节从未进过任何提交**（受纳管文件的**已提交内容**停在 HEAD 之前，工作树 ` M`）⇒ 本表前言自称的「状态的唯一现行读法」对干净 clone 与任何一次 restore 都不成立 —— 与 `L10` 的「文件未纳管」是**两类不同机制的暴露，不许合并**（第 41 轮登记） | **本轮实测**（as-of 2026-09-25。复跑写法全部**不含裸竖线**：竖线一写进单元就被本表自己的形门按竖线拆格判 `BAD_CELL_COUNT` ⇒ 本行刻意不用「grep 数行首竖线 + 反引号 + L」那种内联写法，改用下面四条等价命令）。① **整节未进历史（决定性一条）**：`git log -S'未排期清单' --oneline -- CONTRIBUTING.md` = **0 个提交**；而 `git log --oneline -- CONTRIBUTING.md` = **14** 个提交 ⇒ 该文件的提交史里从未有过该节，最后一次动过它的是 `98f05674`（sweep110），当前 HEAD = `9141eed5`（sweep112）。② **行数差 = 整节**：`git show HEAD:CONTRIBUTING.md > temp/ralph-20260922/logs/r41-head-contrib.md` 后 `wc -l` = **375**，`wc -l < CONTRIBUTING.md` = **438** ⇒ 差 **63 行**；`diff` 该落盘件与工作树 = **单一 hunk**（HEAD 第 372 行之后整段多出工作树的 373–435 行）⇒ 没提交的**就是整节**，不是散点改动。（**375 / 438 / 63** 是登记时点即插入本行**之前**的数；插入本行后同两条命令 = **439 / 差 64** —— 本行自己也是未提交内容的一部分。）③ **HEAD 里连标题都查无**：对 ② 的落盘件数「未排期清单」命中 = **0**（工作树 = **2**：节标题 + 表内互引）；按本表形门的同口径数表行（行首 = 竖线 + 反引号 + `L` 号），HEAD 件 = **0** 行，工作树插入本行**前**现扫 = **48** 行、首号 1 末号 48，插入本行后 = 49 行、末号 49。这组数由本轮探针一次打印：`node temp/ralph-20260922/_r41-probe-uncommitted.mjs`（rc=0，日志 `temp/ralph-20260922/logs/r41-probe-uncommitted.log` 即「前」那一组：375 / 438 / 0 / 48 / 0 / 2）——探针在 `temp/`（gitignored、不入库）⇒ 复跑入口以 ①②④ 三条 git 命令为准。④ **状态字**：`git status --porcelain CONTRIBUTING.md` = **` M CONTRIBUTING.md`**。**后果**：任何一次 `git restore CONTRIBUTING.md` / `git checkout -- .` / sweep 前的 reset 都会**静默抹掉整张表**。该节第 25 轮建（台账 R125）、到第 40 轮逐轮登记，表行自述共引用 **20** 个不同轮次号（8、11、22、24–40，本轮现扫）；一起没掉的不是排版，是每条「为什么不修」的账与 `L10` / `L47` / `L48` 三张危险登记。**门链对此不报警**：`mcp-server/scripts/assert-backlog-table-shape.mjs` 读**磁盘**不读 HEAD（其自述锚「## 未排期清单」+ 现扫路径）⇒ 「48 行 × 3 列、号序连续」绿 **不等于** 该内容存在于版本库；且这道形门自身此刻也还没 `git add`（见 `L10`），两层暴露叠在一起。**与 `L10` 的分工**：`L10` = 文件**根本没被纳管**（状态 `??`，第 40 轮现扫 10 件、其中 8 件 `mcp-server/scripts/*.mjs`、7 件被受纳管 runner 引用）⇒ 症状是 clone 后**文件不存在**、runner 在引用位点直接断链；本行 = 受纳管文件的**已提交内容落后于工作树**（状态 ` M`，差 63 行且差的正是整节）⇒ 症状是 clone 后**文件在、内容少一节**，而每天看工作树的人完全看不出异常。两者处置动作也不同（`git add` 对 `git commit`），合成一条会让两条复跑路径互相顶掉 ⇒ 分行登记。 | 用户：**提交由用户执行**（agent 禁 `git add` / `commit` / `push`，见根 `AGENTS.md` 与维护侧铁律；本轮产出的正是「该提交」的凭据，不是提交本身）。维护侧无可代劳，且本轮**不动 `.gitignore`**、不建议把 `temp/` 入库（派单已禁）。要提交的只是 `CONTRIBUTING.md` 一个已纳管文件的整节内容；提交后按本节末「新增条目的规矩」（闭环不删行）把本行改写成「已闭环 + 复核命令 ①」，别删行。 |
| `L50` 计划 `S17`/`S26` 的 `tasks[4]` 把 **D-5** 写成 **D-4**（字母错位，正是 `L48` 点名的病）⇒「转引量级更正 + S26 复验结论」至今 **0 个入库落点**，本节是第一处（第 42 轮登记） | **本轮实测**（as-of 2026-09-25，全部数字由本轮三个探针现跑，未抄派单）。① **错位坐实**：`temp/audit-20260921/REPORT.md:357` 逐字 = 「D-4（P2，元数据脱钩）`data/neoforge_primers/_fetch-report.json` 过期」，`:358` 逐字 = 「D-5（P2）fabric 转引目标 6 处洞 / 5 档」；而派单原件 `temp/plan-20260921-fix.json` 的 `S17.tasks[4]` 逐字写 "D-4 is 2 sites / 2 packages, not 6 / 5"、`S26.tasks[4]` 写 "D-4's magnitude" ⇒ 计划引的是 **D-5** 的量级却标成 D-4。**本轮未改计划 JSON**（它是用户派单原件），错位只在本行点名。② **量级四口径复算**（分母 = `data/` 现扫 15 个 `fabric_*` 目录，逐文件按字符串计数）：正口径「`data/fabric_*/fabric-docs/*/processed/*.md` 里逐字带未展开 `@[code …](@/.github/workflows/build.yml)` 标记的行」= **2 处 / 2 档**（`develop_automatic-testing.md`，1.21.8 的 `:140`、1.21.10 的 `:144`）；同标记跨所有文件 = **12 处 / 4 档**（1.21.8 · 1.21.10 · 1.21.11 · 26.1.2）；松散子串 `build.yml` 跨所有文件 = **36 处 / 6 档**（分面：processed-md 6 · raw 6 · `semantic/db.sqlite` 12 · `reference.provenance.json` 12）；松散子串只在 processed .md = **6 处 / 4 档**（含散文句 "Add this to your `.github/workflows/build.yml` file"）。⇒ 审计的 **6 处 / 5 档在任何单一干净口径都不复现**：6 只能从后两口径凑出，**5 没有任何面给得出**（可选值只有 2 / 4 / 6）。③ **现状措辞（不写「洞」）**：那 2 处标记仍在盘上未展开，但**取件别名已落**——`data/fabric_{1.21.8,1.21.10,1.21.11,26.1.2}/.github/workflows/build.yaml` 四档全在（各 2,908 B，同处 `build.yml` **不存在**），provenance 里该标记条目的 `realPath` 指向 `build.yaml`（1.21.8 `:696`、1.21.10 `:901`、1.21.11 `:1170`、26.1.2 `:1513`）。⇒ 文件解析不再是缺口，真症状是**区间取空**：标记要 `transcludeWith=:::automatic-testing:game-test:3`，而 `build.yaml` 里 `:::` 计数 **0**、两个区间名 **0 命中** ⇒ 展开成**空气泡**。旧措辞「6 处洞」双重失真（数错 + 症状错）。④ **落点核查**（`git grep -n -e <词> -- CONTRIBUTING.md README.md AGENTS.md mcp-server/README.md` 六条各跑）："2 sites" rc=1 命中 0 · "6 sites" rc=1 命中 0 · "5 packages" rc=1 命中 0 · "转引目标" rc=1 命中 0 · "6 处" rc=1 命中 0 · "5 档" rc=0 命中 1（= `AGENTS.md:59` 讲 quilt 语料档数，与本项无关）⇒ 更正从未进过任何入库文档。⑤ **「无家可归」到底几条：本轮（第 45 轮）按标识符口径现扫重算 = 14，`G-1` 归入无家；旧默认值 13 作废**。分母 = `temp/ralph-20260922/_scout-r42-A.md` §4 落点表（现扫 `:59..:76` = **18 行**；桶分布 already-fixed **7** + wrong-mechanism **7** + corrected-number **4**）；本轮复跑探针 `node temp/ralph-20260922/_v45-homeless.mjs` → `temp/ralph-20260922/logs/r45-homeless.log`（打印基准目录，逐行按第三列字面判，四桶相加 = 表行数才落笔）。**14 行无家可归**（已修桶 7 = W-1 · P0-4 · **G-1（本轮改判）** · T-2 HITL 半 · W-3 · W-9 · D-1 · 错机制桶 7 = P0-6 · P0-11 · P0-12 · P0-1@1.21.1 · C-8's line · D-4/D-5 magnitude · §9.1 C-1）· **2 行 HOME**（988=classCount→`L20` · README:43/:11 已 RESOLVED）· **1 行 partial**（indexEntries 258）· **1 行 weak/none**（13→19 canonical ENTITYTYPES）⇒ 14+2+1+1 = **18** 与表行数自洽。**13 为什么错（一句话）**：它按第三列把 `G-1` 那行算成有家，而那个 HOME 的引证 `CONTRIBUTING.md:236-237` 是**同形误引**——`:236` 逐字是「第 8 步是门禁总入口（… 串链 + `G1`…`G4` 全量 + 假根投毒）」，讲的是**门禁编号 `G1`/`G2`/`G3`/`G4`**，与审计项编号 `G-1` 同形不同物；本轮复跑三机制：HEAD blob 取出后逐字数，连字符 `-`、U+2011 不换行连字符、U+2013 短横**三种写法各 0 次**（`git show HEAD:CONTRIBUTING.md` 落盘 `temp/ralph-20260922/logs/r45-head-contrib.md`，HEAD 换行数 375）· 工作树 `G-1` 全部命中都落在本行 `:433`（grep 按行计数 = **1 行**，即它自己）。⚠️ 该文件 Tally 自述「~19 items → 3 HOME + 2 partial + 14 无家可归」两头都对不上现扫：表是 **18** 行不是 19，且它把 `G-1` **同时**列进 3 HOME 又报 14 无家（3+2+14 = 19 ≠ 18）⇒ 那个 14 **不是**「把自家标 HOME 的 G-1 从已修桶挪进无家凑出来的」——**这句是第 44 轮对本行的重述，本轮证伪**：侦察并没有挪，它只是自己相加不自洽；而它报的 14 **与本轮按判据重算出的 14 同值纯属两条路走到同一个数**，正当理由是「`G-1` 的 HOME 系同形误引」，不是「另一次数出来的」，也不是「口径漂移」。**G-1 这笔为什么两套口径会分岔**（第 44 轮三机制现跑：`node temp/ralph-20260922/_v44-headhome.mjs` → `temp/ralph-20260922/logs/r44-headhome.log`，HEAD blob 落盘 `temp/ralph-20260922/logs/r44-head-contrib.md`）：① 取 HEAD blob 后逐字数 **`G-1` = 0 次**，把连字符换成 U+2011（`G‑1`）或 en dash（`G–1`）**也各 0 次**；② `git grep -c -e G-1 HEAD -- CONTRIBUTING.md` **rc=1 且无输出**；③ 而 HEAD 里 **`G1`**（无短横）= **3 次 = `:224` `:225` `:236`**，三行原文讲的是 **G1–G4 门名**与「第 8 步是门禁总入口」（`:224` 「G1/G2 的 LEDGER 常量取门成功行打印的实测值」· `:236` 「G1–G4 全量」）⇒ **同形不同物**：审计项编号 `G-1` 至今**没有**入库落点。工作树侧 `G-1` 出现 **12 次、全部落在本行 `:433`**（本行的叙述自身 ⇒ 本轮改本行会让该数再变，禁止续用旧值），`G1` 另有 `:224` `:225` `:236` `:425` `:428` `:433`。⚠️ 但**内容**在 HEAD：`道数不写死` 在 HEAD **1 次 = `:237`**（逐字「道数不写死（会随迭代增长），以 `ls mcp-server/scripts/assert-*.mjs` 为准。」），`mcp-server/scripts/assert-` 在 HEAD **3 次 = `:236` `:237` `:248`**，工作树同三处行号逐字相同 ⇒ 侦察给的「落点 `:236-237`」**指得对，只是那个落点没顶着 `G-1` 这个名字**。⇒ **本行取 14（标识符口径 = 本表其余 17 行的判法：第三列给的是 file:line 落点，落点必须真的是这一项）**；**内容口径 13**（承认 `:236-237` 那句「道数不写死」在 HEAD 里逐字存在、且语义上确是 `G-1` 结论的载体）只作为**历史备选值**保留，不得再当默认值抄走。要写 15 得声明「partial 与 weak/none 两行都判无落点」；三个数 13 / 14 / 15 不得混用。⑥ **这 14 条（连 D-5 量级本身）此前唯一的载体是 `temp/`（gitignored）**：`_scout-r42-A.md` 与其探针 `_v42a-d4count.mjs`；⇒ 按 `S26.tasks[4]` 的「落到会生效的地方」口径 = **未落**，本行是第一处入库落点。⚠️ 但本行落在 §未排期清单，而该节整节未提交（见 `L49`）⇒ **写盘 ≠ 生效**。**复跑**：`node temp/ralph-20260922/_v42a-d4count.mjs`（四口径一次打印，须在仓库根跑——它以 `process.cwd()` 为基准）· `node temp/ralph-20260922/_v42a-verify.mjs`（逐站点行号 + `build.yaml` 与 `:::` 计数 + REPORT 与计划原文）· `node temp/ralph-20260922/_v42a-14count.mjs`（§4 表逐行清点 + G-1 在 HEAD 的证据 + 六条 `git grep`）· **第 45 轮起以 `node temp/ralph-20260922/_v45-homeless.mjs` 为准**（§4 表逐行四桶清点 + 三种短横写法在 HEAD blob 与工作树的计数 + `:235..238` 原文，打印基准目录）。本轮日志：`temp/ralph-20260922/_v42a-d4count-r42.log`、`_v42a-verify.log`、`_v42a-14count.log`、`temp/ralph-20260922/logs/r45-homeless.log`、`logs/r45-head-contrib.md`（件在 `temp/` ⇒ 复跑入口以上述命令为准，日志只作本轮凭据）。 | 用户，三笔：① `L49`（整节未提交——第 42 轮复算 `git show HEAD:CONTRIBUTING.md` = **375** 行 vs 工作树 **439** 行、差 **64**，并称与 `L49` 的预测逐字相等；**第 43 轮同口径复测 = HEAD 375 / 工作树 440 / 差 65**，且 `git diff --numstat` = **+65 / −0** 与 375+65=440 自洽 ⇒ 第 42 轮那组 439 与 64 同它自己台账 #16 记的 65/0 **互相矛盾、off by one**，`L49` 的「439 / 差 64」预测本身也偏 1；口径 = `wc -l` 换行计数，用 `split(/\r?\n/)` 会各多 1，本轮第一版探针就踩了这个）与 `L10`（未纳管 assert 件）都要用户 `git add` 才生效，agent 禁 `git add`/`commit`/`push`，维护侧无可代劳。② 是否授权改 `temp/plan-20260921-fix.json` 的 t5 字母（本轮按派单**未改**，只在此点名）。③ 「13 vs 14」的争论**第 45 轮已按标识符口径定为 14**（`G-1` 的 `:236-237` 落点经三机制复跑系同形误引 ⇒ 改判无家），默认值已改写，无需再拍板；**仍待拍板的是 14 的边界**：把 weak/none 那行（13→19 canonical ENTITYTYPES，门 `assert-forge-1182-registry-consts.mjs:52` 有半个落点）与 partial 那行（indexEntries 258，`L29` 只记事实不记判定）也判无落点，就得到 15——本轮**没有**这么判，两行按原判留在表内。`S26.tasks[4]` 其余 13 条（14 减 D-5 量级本行）是否逐条建表行 = **排期**（本轮只登记，未逐条改盘）。 |
| `L51` 库构件面生产者 `scripts/build-lib-manifest.mjs` **按构造被静默截断**（每 slug 只发一次 `api.modrinth.com/v2/project/<slug>/version`、不带 limit 与 offset，而该端点默认页 = **100 个版本**）⇒ 第 43 轮已改翻页（`offset += 100` 直到某页 < 100；`MAX_PAGES=30` 触顶即打日志并判该 slug 失败；429 与 5xx 与链路抖动退避 1s/3s/6s；另加 `--only=` 点名与 `--out=` 候选件，候选只许落 `temp/` 或仓库外，由 `scratchWriteText` 兜）并全量重抓出**候选**，**但真面 `mcp-server/data/lib-manifests/all.json` 本轮未覆盖（车道忙，见 §3）**；连带后果写死在这里：**本轮之前所有按该快照做的库窗口裁定一律待复核** | **截断普查**（分母 = 快照 48 slug · as-of 2026-09-25 · 两条腿各跑一次、结论逐字相同：`node temp/ralph-20260922/_v43-truncation-census.mjs` 与 `node temp/ralph-20260922/_v43-census-rerun.mjs`，后者另打印 HTTP 状态码；**两条都要梯子**，curl 侧 `--proxy http://127.0.0.1:7897`，而 Node fetch 腿实测 Modrinth 直连也 200）。结果：**31/48 被截断**（page0 满 100 且 page1 非空，且这 31 个 slug 全部在快照里 ⇒ 快照确实只收了第一页）· 其中 **21 个 page1 也满 100 ⇒ ≥200 版本** · 再读 page2：那 21 个里 **17 个仍满 100 ⇒ ≥300 版本**，4 个在 page2 收口（forge-config-api-port 229 · pehkui 225 · polymer 273 · resourceful-lib 237）· **14 个不足一页** · **3 个 page0 HTTP 404**（`libgui` · `server-translations` · `spruceui-obsidianui`；状态码 404 不是 429 限流，且这三个已不在今日 slug 全集里 ⇒ 定性为 slug 取不到，**不得**读成「上游没有该库」）。被截断的 31 个 slug 占快照 **2052 / 2870** 行（71.5%，分母 = 快照 Σentries）。**翻页后的实况**（候选 `temp/ralph-20260922/logs/r43-candidate-all.json`，复跑 `node scripts/build-lib-manifest.mjs --out=temp/ralph-20260922/logs/r43-candidate-all.json --force`；比对 `node temp/ralph-20260922/_v43-compare.mjs <候选件>` 与 `node temp/ralph-20260922/_v43-universe-drift.mjs`）：今日 slug 全集 **46** 个（快照 48 少 3 个 404、多 1 个 `rei`）· 候选 **45 slug / 2,927 行**（`jei` 撞 `MAX_PAGES` 帽判失败）· 上游版本对象 **13,608**（vs 快照口径的「每 slug 最多 100」）· HTTP 请求 **165**。⇒ **行数几乎没变**（两侧都在的 44 个 slug：**2,794 → 2,843，+49**，逐 slug 最多 +4；23 涨 / 20 平 / **1 跌** = `yacl` −2，快照那 4 行是 `26.3-snapshot-4…7` 快照版游戏号、被 2 行 release 取代）· 但**同一组合里胜出的构件换了 185 行**（`fileName` 或 `versionNumber` 变），因为 `entries` 是 gameVersion × loader **去重后**的组合数、不是版本数。六个曾被按快照裁定过的库，逐 loader release 上界 前 → 后：`cloth-config` fabric 26.2→**26.3** · neoforge 26.2→**26.3** · forge 1.21.3→1.21.3 不变；`owo-lib` fabric 26.1.2→**26.2** · quilt 26.1.2→**26.2** · neoforge 1.21.10 不变（⇒ 09-25 的 `mc-owo` 反转由新面坐实，26.2 的 release 行确实存在）；`pehkui` 四 loader 全不变（fabric·neoforge·quilt 1.21.1 / forge **1.20.1**）⇒ 第 42 轮收件 #3 的 forge 差额**在完整面上仍成立**；`terrablender` 三 loader release 上界 1.20.4 不变（只有 anyMax 涨到 26.3 的 beta）· quilt 0 release 不变；`playeranimator` 全不变（fabric·neoforge 1.21.4 / forge·quilt 1.19.4）；`patchouli` 全不变（26.x 有 9 行但**全是 beta、0 release**，前后一样）。**第二成因（每版本只留 primary 一个构件）已量化后决定不动**（`node temp/ralph-20260922/_v43-fileleg.mjs` 与 `_v43-fileleg2-filenames.mjs`，分母 = 48 slug 去 jei 的**全部** 13,608 个版本 / 15,578 个 file）：带 >1 file 的版本 **1,722 个（12.7%）**，但 Modrinth 给这些 file 对象的 `file_extensions.loaders` **0/13,608 有值**；改按 filename 加载器词比 page0 采样的 **510** 个多构件版本，指向不同加载器的 **0 个**（多出来的是 `-sources.jar` / `-api.jar` / `_SOURCE_` 变体，同加载器），并按同一 matcher 投毒三条对照（synthetic forge+fabric 对必判红 / jar+sources 必判绿 / 无词必判绿，全 OK，见 `node temp/ralph-20260922/_v43-fileleg-control.mjs`）。⇒ 按 (gameVersion × loader × filename) 展开会把 2,927 行放大到 **27,853（×9.52）** 而**不增任何加载器覆盖**，且会同时改 `assert-lib-census-counts` 的 rows 与 artifacts 口径与全部 SKILL「N 条实测」正文——**本轮不动**（派单 §2 允许并要求的保守收口）。**能红的判法**（本轮实测两条都真红）：`node scripts/build-lib-manifest.mjs --only=jei --out=temp/x.json` ⇒ rc=**2**、不落笔、日志含「翻页触顶…拒绝静默截断」；`--only=__no_such_slug__` ⇒ rc=**2**、含「拒绝产出空清单」（本轮新加的地板：`--only` 全拼错时旧逻辑会产出空清单并以 failed=0 过闸门写进真面）。反证：`--force` 时才写候选。**已知未解**：`jei` 上游 ≥3,000 版本，帽 30 页不够 ⇒ 要么抬 `MAX_PAGES`，要么接受 `jei` 不入面。**复跑要梯子**这条只针对普查探针，生产者的 Node fetch 腿直连即可（本轮实测直连 200 / 梯子 200 一致）。**第 44 轮（2026-09-25）把「跳过并打印警告」换成「失败继承」**——要治的病不是截断本身，而是**修截断那一轮把静默截断换成了静默删库**：`capped` 与 404 都让整条 slug 从输出里消失，重跑一次就把 `jei` 的 73 行与三个已 404 库的行抹掉，而那 3 行**不是本生产者能再生成的**（Modrinth 404；历史上由人工按非 Modrinth 源补抓，见 `mcp-server/scripts/assert-lib-ownership.mjs:99` 的 2026-09-16 第七次签字「all.json 补 3 条目（45 → 48 slug）」）。新机制：逐 slug 结果分**四类**并按类打印计数 `ok` / `capped`（翻页触顶）/ `notfound`（上游 404，**或本仓 slug 源不再点名该旧面 slug**）/ `error`（退避后仍失败，含「200 但展开 0 行」）；非 ok **一律继承旧面该 slug 的全部行**，标记走 slug 级**新增可选键** `partial` / `captureState` / `capturedPages` / `inheritedFrom` / `failureReason`（**entry 的既有 8 键一字未动**，下游 `assert-lib-census-counts` 与 catalog 解析按 8 键读）；同时**整次运行退出码非 0** 并逐名打印「继承自哪一版」（`inheritedFrom` = 旧面路径 + mtime + slug/行数）；旧面也没有该 slug 时**单列「无处可继承」清单** loudly 报出（不静默）；新增 **`--require-complete`** ⇒ 任何未抓全直接判失败且**不落盘**（保守档，默认关）。**判据能红**（不是装饰）：`node temp/ralph-20260922/_v44-poison.mjs` 把继承腿中性化成恒假后**三条失败腿全红**（旧行整条消失 = 病被复现）而**对照腿仍绿**，真生产者 0 红（rc=0 判定 POISON_OK）；`node temp/ralph-20260922/_v44-unit.mjs` = **36 项内存断言全绿**（含「无处可继承必须进 dropped」「全 ok 不得打标」「局部跑不许把整面拖进来」「继承链的上一版 as-of 必须可追」）。真网络腿：`--only=jei --out=temp 候选 --force` ⇒ rc=**2** 且候选里 **jei 的 73 行逐字仍在** + `capturedPages=30`；不带 `--force` ⇒ rc=**2** 且不落笔；`--require-complete` ⇒ rc=**2** 且不落笔；对照 `--only=caelus --require-complete` ⇒ rc=**0**（证明没把门做成恒红）。**第 44 轮已覆盖真面**（车道静：基线 06:15:24Z，06:19/06:23 两次复查 90 s 内别道零写入、`_debug_gate_selftest/**` 自 06:00:03Z 起未再动；备份先落 `temp/ralph-20260922/logs/all.json.bak-2026-09-25`，sha256 `d955f0ad…` 与旧面逐字节相同，再 `--write --force`）：**真面 48 slug / 2,870 行 → 49 slug / 3,003 行**（新面 sha256 `72a8bd57…` = 候选逐字节相同），四类计数 **ok 45 / capped 1（jei）/ notfound 3 / error 0**，继承 **4 个 slug / 76 行**；逐 slug 前→后：**没有任何 slug 或行被删**（旧面 48 个全在），共享 48 个 **2,870→2,919（+49）**（23 涨 · 24 平 · 1 跌 = yacl −2），新增 **`rei` 84 行**；`mc-lib-catalog` 那座桥的现值不受影响（声明面 **36** 份 · 桥通 **33** · 未通桥 **3**（mc-lib-catalog · mc-script-server · mc-script-ui），与覆盖前逐字相同 ⇒ **死 slug 没有新增盲区**，盲区 0 增量）。⚠️ **覆盖当场让 S22 判据④b 判红 3 条**（预演在先：`node temp/ralph-20260922/_v44-parity-dryrun.mjs` 把门的 MANIFEST 常量指到候选件再跑，红点与覆盖后完全一致）：`mc-terrablender` 的 fabric/forge/neoforge 正文披露句写「beta 构件至 26.2」，而新面越界 beta 已到 **26.3**（三端 release 上界仍 **1.20.4** 未变）⇒ 按「先对齐生产侧、再改 harness」改了该库 `SKILL.md:35-38` 的**披露句**（26.2→26.3 + 点名构件面 as-of 2026-09-25），**未动** `mcVersions` / `mcVersionsByPlatform` 任何数值（那是第 45 轮从完整面重导的那一项）⇒ 门复跑 rc=**0**。六个点名库逐 loader release 上界 前 → 后（`node temp/ralph-20260922/_v44-sixlibs.mjs`，口径 = 该 (slug,loader) 里 `versionType=release` 行的 gameVersion 最大值）：`cloth-config` fabric 26.2→**26.3** · neoforge 26.2→**26.3**（forge 1.21.3 不变）· `owo-lib` fabric 26.1.2→**26.2** · quilt 26.1.2→**26.2**（neoforge 1.21.10 不变）· `geckolib` fabric/forge/neoforge 26.2→**26.3**（quilt 1.20.1 不变）· `pehkui` 四 loader 全不变（forge 仍 **1.20.1**）· `terrablender` 三端 release 上界仍 1.20.4（只有 beta anyMax 26.2→**26.3**，但**行数 78→81 变了**，「不变」只对 release 上界成立）· `playeranimator` / `patchouli` / `caelus` 全不变（caelus 31 行逐字不动 ⇒ W0-1 等式侧天然不红，实测 `assert-lib-census-counts` rc=0 · 85 个锚点全过）。**`rei` 的账算清了，不是脚本漏收**：`library-catalog.ts` 在 **2026-09-14 19:36（commit `71027844` sweep69）**把该条目 slug 串从 `jei, emi, roughly-enough-items` 改成 `emi,jei,rei`；`roughly-enough-items` 今天实测 **HTTP 404**（Node fetch 腿与 `curl.exe --proxy http://127.0.0.1:7897` 腿同判），而 HEAD 版生产者本来就会 `split(",")` 展开多值串（快照里 emi 与 jei 两家都在 ⇒ 展开当时生效）。⇒ 旧快照少那一家 = **「生成时读到的 catalog（优先 `mcp-server/dist/diagnostics/library-catalog.js` 编译产物，可能比 src 旧）里第三个 slug 是死名」+「旧策略把 404 静默跳过」两因叠加**，不是「脚本没解析多值串」；今日全集实扫 = catalog **50** 条条目 / 非空 slug 串 **44** 条 / 展开 **46** 个 slug + authored `lib-*`·`library-integration*` **50** 份里的 **46** 个 slug（authored-only 增量 **0**）⇒ 生产者今日点名 **46** 个 slug。**三个 404 slug 的上游真相（只出数、不改面）**：`libgui` 404，搜索候选 `libguifoxified`（11,179 下载）· `libgui-forge`（1,346 下载），**两者都不是 Cotton 官方发布**（官方走 GitHub Releases + maven）· `server-translations` 404 且 `"Server Translations"` 搜索 **total_hits=0**（Modrinth 无该项目）· `spruceui-obsidianui` 404 而 **`obsidianui` 200 / 2,566,326 下载**（合并 slug 已拆成单家）。探针 `node temp/ralph-20260922/_v44-upstream.mjs` → `temp/ralph-20260922/logs/r44-upstream.log`。「**截断从被截断的件里数不出来**」那句方法学的唯一现行读法在 `mcp-server/README.md:367`（本行按派单**不重抄**，避免两处漂移）。 | 用户与第 44 轮，四笔：① **覆盖真面**：第 44 轮**已执行**（先备份 `temp/ralph-20260922/logs/all.json.bak-2026-09-25` 并逐字节核对旧面 sha256 `d955f0ad…`，再 `--write --force`；新面 sha256 `72a8bd57…` = 候选件逐字节相同；车道静判据与复查次数见 `temp/ralph-20260922/round-44-failure-inheritance.md` §0）。生产者自带的 `all.json.bak` 也落在真面同目录（**受纳管目录里的未跟踪新文件**，1,450,852 B = 旧面；删除与否由用户定，agent 不 `git add`/`commit`/`push`）。② `knowledge/libs/**` 的**窗口值与 `mcVersionsByPlatform` 一个没动**（第 45 轮的②，必须从**现已完整**的面重导）；本轮只按门的判据改了 `mc-terrablender` 的**越界 beta 披露句**（26.2→26.3，`SKILL.md:35-38`）。③ **`jei` 仍触顶**（`MAX_PAGES=30` ⇒ ≥3,000 版本）：抬帽到多少页、还是接受「`jei` 永远靠继承」= **需拍板**（本轮面靠继承保住 73 行，但那 73 行不是今天抓的）。④ **三个 404 slug 的改名候选表已备好但没有采纳**：`spruceui-obsidianui` → `obsidianui`（200 / 2,566,326 下载）最像真名，`libgui` 的两个候选都不是 Cotton 官方发布，`server-translations` 在 Modrinth 完全搜不到 ⇒ 要不要在 `library-catalog.ts` / authored frontmatter 点名改名 = 内容判断，**需拍板**。⑤ **第 45 轮的输入**：三端 release 上界与窗口终点要不要跟到 26.3（cloth/geckolib）、`owo` 是否把 fabric/quilt 收到 26.2、`mcVersions` union 上界 26.2 低于现面 beta 26.3 这件事要不要写进正文。 |
| `L52` 库构件面生产者的 `--write` 每次自动在**受纳管目录**里留一份 1.4 MB 备份 `mcp-server/data/lib-manifests/all.json.bak` ⇒ 用户一次 `git add .` 就会把二进制体积的旧面提交进仓库 | **本轮实测**（as-of 2026-09-25，第 45 轮，仓库根 = `C:/Users/zzrCN/OneDrive/桌面/MC_skill`）：`git status --porcelain mcp-server/data/lib-manifests/` = ` M all.json` + `?? all.json.bak` ⇒ 备份是 **git 可见的未跟踪件**；`git check-ignore mcp-server/data/lib-manifests/all.json.bak` **rc=1**（= 没被忽略，同目录的 `mcp-server/dist/` 由 `.gitignore:27` 忽略、它不在任何忽略规则内）。大小 **1,450,852 B**（= 覆盖前的旧面逐字节，第 44 轮 §1 记的 sha256 `d955f0ad…`），真面现为 1,519,506 B。生产者 `scripts/build-lib-manifest.mjs` 的 `--write` 无条件先写该 `.bak`（**没有开关关掉它**），故**每次重抓必然再产生**，删了也会在下一次 `--write` 回来。三条出路（互斥，任一条都要动不同面 ⇒ 逐条给代价）：**① 产物改落 `temp/` 或缓存**（最干净，但要改生产者的备份路径常量，且失去「就地回滚」便利；改动面 = 脚本 1 处 + 文档口径同步）；**② `.gitignore` 加一行**（改动最小，但配置面禁令 —— 记忆「不要动我的配置面」+ 本仓 agent 禁改 `.gitignore`，**只能用户自己加**）；**③ 每次重抓后手删**（零代码改动，但把负担永久压在人手上，且删掉的是唯一的就地回滚依据）。**本轮三条都没做**：未改 `.gitignore`（禁令）、未删该件（可能是回滚依据）、未改生产者（派单 §禁列了 `data/**` 与本轮「只读 `all.json`」，改脚本产物流向属越界）。⇒ 本行只做登记 + 三选一。 | **用户（三选一由用户拍板）**：① 改生产者的备份落点 = 授权后动 `scripts/build-lib-manifest.mjs`；② `.gitignore` 由用户亲手加（agent 不改配置面）；③ 立「每次重抓后手删」为规程，则本行改写为已裁定项。`all.json.bak` 本体本轮**保留未动**。 |
| `L53` `assert-workflow-hitl.mjs` 的块正则与路径腿**只读 src**（`TEMPLATES_TS` 指 `src/prompts/templates.ts`）⇒ src 与 dist 正文分叉时该门看不见 | **本轮实测**（as-of 2026-09-25，进程内 import 该门导出的 `parseBlocks` / `countTitleLines` / `validateTemplates`，探针 `temp/ralph-20260922/_v46-hitl-dist.mjs`，日志 `temp/ralph-20260922/logs/r46-B-hitl-dist.log`）：`BLOCK_RE` 要求恰两空格缩进的 `"key": {`，src 侧块起始行缩进分布 `{2:48}` ⇒ 解析 **48** 块 / 48 行 `title:` / 判 0 项；dist 侧（`dist/prompts/templates.js`，49,364 字符）缩进分布 `{4:48}` ⇒ `parseBlocks` 解析 **0** 块、而 `countTitleLines` 仍读到 **48** 行 title，`validateTemplates(distText)` 返回 1 项 `COLLECTOR_RETURNED_ZERO`。**缺口性质要说准**：不是「换到 dist 就恒绿」（采集面塌了会被那条地板抓住），而是**该门从不读 dist** ⇒ 编出去的正文与 src 不同（漏 `npm run build`、tsc 折叠模板字符串、常量替换差异）时 HITL 腿与路径腿双双失明；现成的旁证只有 `get_server_status` 的 `buildStatus.buildRequired`（按 mtime 比 src 与 dist，不比内容）。同轮另测得路径腿的采集面比想象中的窄：`CITED_PATH_RE` 只认 `<平台>/<数字开头版本>/.cursor/{rules,skills}/…`，因此 `knowledge/**`、`data/**`、目录形态引用（`forge/1.21.1`）与 `data/quilt_*` 通配**都不在它判面内**（实跑打印「扫 48 块 / 其中 3 块带具象路径引用 / 判 34 条 / 拒 0 条」，as-of 2026-09-25）。要把这条腿铺到 `knowledge/…` 的人注意两件事（本轮量过）：① 正则必须带左边界，否则 `community_knowledge/…` 会被 `knowledge/…` 段吃掉（本轮 `(?<![\w-])` 口径实扫：派单三模板 `mc-events-forge` / `mc-events-neoforge` / `mc-events-fabric` 去重路径 **47** 条，其中根锚在盘 36、占位可展开 9、通配 1、档内简写 1、断链 **0**）；② 引号内的**档内简写**必须放行 —— 但简写不在派单点名的那两条里：`mc-events-forge` 与 `mc-events-neoforge` 实扫简写 **0** 条，唯一的 1 条在 `mc-events-fabric`（`knowledge/common/verified-api-1.21.4.md`，真身 `fabric/1.21.4/knowledge/common/verified-api-1.21.4.md` 在盘）。探针 `temp/ralph-20260922/_v46-events.mjs`（进程内取 `dist/prompts/index.js` 的 `getWorkflowTemplate`，零 spawn、零写盘）。 | 排期（本轮只量清、不修）。修法二选一由维护侧定：把腿改成 src 与 dist **各判一遍**（dist 侧需另配 4 空格缩进的块正则，不能复用 `BLOCK_RE`），或改成进程内 import `dist/prompts/index.js` 取正文再判（那样判的是服务链真读的文本，但会丢掉模板 key 与块结构）。要用户点头的只有「判 src 还是判 dist」这一条口径选择。 |
| `L54` `S17/t1` 的两半：前半句已成但未提交、后半句的「manifest writer」不在仓内（⇒ 该子句不可盘上证） | **本轮实测**（as-of 2026-09-25，第 47 轮，仓库根 = `C:/Users/zzrCN/OneDrive/桌面/MC_skill`；只读，`data/**` 零写入）：计划 `temp/plan-20260921-fix.json` 的 `S17/t1` 两句**判词不同**，逐句落盘证：① 前半句「remove the second copy of recipes/ingredients (:298/:490) and recipes/incode (:306/:498)」**已成、但只在工作树** ——`git status --porcelain data/forge_1.20.1/forge-docs/_manifest.json` = ` M`、`git diff --numstat` = 删 **16** 行（第 489 行起那两个副本块）、文件 mtime `2026-09-24T08:50+08:00`；重复键现扫 **0**（口径 = 2 空格缩进顶层键，**71** 键，`JSON.parse` 后同为 71），两处串各剩 2 次命中 = 一个键 + 一个 `url` 字符串。台账里做这次删除的是**第 11 轮**（`PLAN-STATE-r2.md` 的 `incode 同样 2→1，两份 size 6035/6035 与 3734/3734 逐字相同`），而它**至今未进任何提交** ⇒ 与 `L49` 同族的生效性尾巴，处置动作 = 用户 `git add` 与 `git commit`（agent 不代做）。② 后半句「and make the manifest writer dedupe keys」**不可盘上证：写手不在仓内** ——全仓提 `_manifest.json` 的只有 `mcp-server/scripts/audit-data-consistency.mjs`（`scripts/` 与 `mcp-server/src/` 各 **0** 命中），而它是**只读消费者**（`:653` 拼路径读、`:689` 出判据），仓内没有任何脚本写这份 `_manifest.json` ⇒ 该子句没有可改对象，永久留在读侧。 这面**并非无人看**：重复键由 `audit-data-consistency.mjs` 的 `G-manifest-dup-key`（`:689`）常驻判，其采集面对**非 2 空格缩进**的塌法另有自证（`:559` 记录「把 `{2}` 改成 `{9}` ⇒ 该腿采集 0 条即红」），该门的退出码定性已在 `L41`（第 33 轮）落账；表形侧另有同族机制可参照（`assert-backlog-table-shape.mjs` 的跳号/重号/采 0 行三态）。 本轮**没动手也没动数据**（`data/**` 是每轮禁写面，且按「语料缺陷只排除 + 留痕、永不打补丁」的裁定，改 `data/**` 需用户点头）。 | 用户（两件事都归他）：① 把 `data/forge_1.20.1/forge-docs/_manifest.json` 的既成删除**提交**进 HEAD；② 若仍要「写盘器去重」这条能力，得先回答**写手是谁**——本仓没有产 `_manifest.json` 的脚本（要么承认该子句作废、要么由用户指定该文档面生产者的归属），agent 侧不新建写手也不改 `data/**` |
| `L55` `A-27` 的「处」等式腿**今天真的红了**（红在 `npm test` 链第 10 腿，不在 `test-scripts.mjs` 里） | **本轮实测**（as-of 2026-09-25，第 47 轮收口链的读数，主循环复跑；仓库根 = `C:/Users/zzrCN/OneDrive/桌面/MC_skill`）：`node temp/ralph-20260922/_r31-run-all-legs.mjs` 全 62 腿跑完 = **绿 61 / 红 1**、`ALL_LEGS_OVERALL=1`（日志 `temp/ralph-20260922/logs/r47-E-alllegs.log`）。唯一红腿 = **LEG 10 `node test-wave-bcd.mjs`**（10.7 s），抛点 `mcp-server/test-wave-bcd.mjs:901`（`w4A8Gates` 内），消息逐字：`A-27：散文出现次数口径须写实测值 **330** 处 / **52** 个文件`。 判据形状（读源码得，不是猜）：该腿递归数 `mcp-server/src/**/*.ts` 里匹配 `ok:` 冒号后接 `false` 的**字面量位点**（排除合同散文自身 `src/utils/actionable.ts`），再要求 `actionable.ts` 的口径行**逐字**含「实测 occ 处 / occFiles 个文件」——即「处」那一组是**等式钉**，同行的「行 / 个文件」那一组只给 ±5 容差（`:14` 的注里写明「A-27 只等式钉『处』那一组」）。 盘的上的声明值 = `mcp-server/src/utils/actionable.ts:14` 写的 **328 处 / 51 个文件**（该行自述 as-of 2026-09-24 Ralph 第 21 轮复算）。 **归因（这条红不是第 47 轮踩的）**：第 47 轮 `data/**` 与 `src/**` 零写入、也没跑 `npm run build`，我另测本轮唯一动过的 `src` 件 `src/diagnostics/library-catalog.ts` 该口径命中 **0** 处 ⇒ 它改不到这条腿。红来自**同一台机器上另一个会话今天在 `src/**` 里新增带内失败返回**（该会话 16:35–16:49 写 `src/mappings/index.ts`、`src/tool-registry.ts`、`src/cli.ts` 并新建两份 `src/mappings/*.ts`，17:13 前后又跑了 `npm run build`）。 ⇒ 关键读数：**这个数还在动**。同一条腿我在 17:2x（链内）读到 330，17:27 单腿复跑（`node test-wave-bcd.mjs`，rc=1）读到 **331** 处 / **52** 个文件，四分钟内 +1 ⇒ 现在把 `:14` 改成任何一个数都会在下一次新增时再红。 **消费面后果要说准**：`test-wave-bcd.mjs` 是 `mcp-server/package.json` 的 `scripts.test` 第 **10** 项（腿表由 `_r31-run-all-legs.mjs` 按 `" && "` 切该串生成），`&&` 短路 ⇒ **`npm test` 现在就在第 10 腿停下，第 11–62 腿在 `npm test` 下一腿都不跑**；而 `node mcp-server/test-scripts.mjs` 单跑仍 rc=0（它是第 7 腿的内容、不含 wave-bcd），两个读数不矛盾。这正是本仓坚持用无短路 runner 收口的理由。 | 归**并发会话**（谁的 `ok: false` 谁负责）：在**同一次改动里**把 `src/utils/actionable.ts:14` 的「处 / 个文件」两个数改成当时实测值，改完立刻单腿复跑 `node test-wave-bcd.mjs` 判绿——事后单独补数会永远慢一拍。用户侧只剩一条口径选择：**要不要**继续保留「处」这一组的等式钉（保留 = 每次加带内失败都必须同步散文，这是本仓刻意要的成本；改成容差 = 少一次红但失去「散文与代码脱节」的检测）。本轮**只登记不修**（该件正被另一会话写，动它是抢笔）。 |
| `L56` `limit` 窗口判据残面：GENRE 门 ⑥ 现管 **5** 面（bedrock + forge / neoforge / fabric / search_docs(neoforge)，2026-09-25 再加 search_docs(quilt) 只收窄腿），仍欠 **8 面**；且该残面此前只活在门头与 selftest 文案里（grep「残面」于本文件与 `mcp-server/docs/*` = 0 命中）⇒ 本行是它的唯一登记面 | **两桶点名（2026-09-25 分桶；分母 = 14 个带 limit 工具面 − 已管 5 面）**。**甲桶（同形状可挂现有 `checkFaceLimitSchema` / `checkFaceLimitWindow`，挂前须先拍板常量提炼）**：`search_community_docs`（`src/docs-platform/community/index.ts:23`，limit int(1..50) 默认 20，`.max(50)` 内联、无常量导出）与 `search_loader_api`（`src/wave/register.ts:51` 与 `:402` 两处 limit，20/50 同为内联）——门不抄数字是既定规矩 ⇒ 前置 = 把内联上界提成 `*_DEFAULT_LIMIT` / `*_LIMIT_MAX` 导出常量，再实测其默认/上限/池三数定归「放宽面」还是「只收窄面」。**乙桶（不该硬挂「超池必披露」）**：5 × `get_*_doc_related`（`forge/index.ts:1140`、`neoforge/index.ts:466`，default 5 固定小窗）与 `query_registry`、`query_upstream_releases`（`src/tool-registry.ts:267`，默认 12，**total 恒全量**）——limit 语义是相关度截断/分页，不是候选池窗口，照搬「超池必披露」会造出无意义判据 ⇒ 须另立判据族（如「limit 只截断不重排 + total 与截断数一致」）并经用户拍板。⚠️ `src/tool-registry.ts` 正被并行会话改写（17:1x 加 `refresh`，见 `L55`）⇒ 乙桶动手前先与并行车道对齐。as-of 2026-09-25 | 排期（用户拍板两件：甲桶常量提炼与否、乙桶判据族设计；拍板后挂腿是可代劳的机械活） |
| `L57` leg2（键值 ↔ 正文实名，冲突基线 = **0 零容忍棘轮**）的**依赖方向此前没有一行 tracked 台账文字**：门只读对照产物，产物根 / 生产者 / 联网义务都不在任何 triage 入口 ⇒ 新克隆 / 新终端的人看不出「零冲突」什么时候是**空转**（skipped 仍 rc=0） | **本轮实测**（as-of 2026-09-25，全部现扫门与生产者源码 + git 状态，非抄写）：① 依赖方向 = `assert-skill-mappings-key.mjs` 的 leg2 只读对照产物，落点三层（`:211-216`：显式 `--pairs-dir` 优先 → `$MC_SKILL_CACHE/yarn-mojmap-pairs` → `os.tmpdir()/mc-skill-yarn-mojmap-pairs`）；产物不在 ⇒ 打印 `leg2 skipped（缺对照产物：…index.json）` 且**保持 leg1 的退出码**（`:677-684`；门头 `:12-13` 与「skipped ≠ 判过」自述方向没错，但那是**脚本内文字**，triage 的人得先打开门源码才看得到）⇒ **未设 `MC_SKILL_CACHE` 的新终端等价于 skipped**（tmpdir 是一次性目录，跨会话不保真）而 rc 仍 0。② 生产者 = `node scripts/build-yarn-mojmap-pairs.mjs`（全部 13 档；缺的 mojmap 侧 `client.txt` 经 `ensureMojangClientMappings` **逐档现拉、要联网**，现拉件落产物目录下不写 `data/**`；`--offline` 只用本地已有；产物 = `<out>/yarn-mojmap-<v>.json` + `index.json`，**从不写仓库**）。③ **最狠一条：生产者本体仍未纳管**（`git status --porcelain` = `??`、`git ls-files --error-unmatch` = pathspec 不匹配）⇒ 干净 clone 上**连重跑入口都不存在**，与本节 `L10` 清单互引。④ 产物在盘时的判据力（口径 = 门头注记 ⑦）：扩族后 leg2 判 **437** 件 / 冲突基线 **0**——但这一切只在产物存在时成立。**复核命令**：`node scripts/build-yarn-mojmap-pairs.mjs`（联网）→ `node scripts/assert-skill-mappings-key.mjs` ⇒ 汇总行应打「leg2 判 437 件、冲突 0」；清 tmp 且不设变量复跑 ⇒ 应打 `leg2 skipped` 且 rc 不变（门自证 `:651-667` 已带该形态的端到端腿）。**同日 17:5x 活体演示（危害升级）**：tmpdir pairs 被清后跑 `assert-cross-layer-names` ⇒ **A/B 130→0（假绿）+ C 33→58（+25 假红**，C 腿排除集经由 pairs，缺席时把「该档 mojmap 也存在的合法名」误判为跨层名；红点文件全部 mtime ≤ 建门时点 ⇒ 零新增，证据 = `git diff --name-only` 按 mtime 过滤 13:35 后 = 0 + `temp/_ab-attrib.cjs` 复算）；重跑生产者（13/13 档 96,457 对，落 `D:\mc-skill-temp\cache`）后 C=33 恢复、A/B=135 重钉（门注释带依据）。⇒ 跨层门的 A/B/C 三腿都必须在 pairs 在场时读数；**运行环境请设 `MC_SKILL_CACHE=D:\mc-skill-temp\cache`**。as-of 2026-09-25 | 用户：`git add mcp-server/scripts/build-yarn-mojmap-pairs.mjs`（重跑入口入库，同 `L10`；agent 不代做）。依赖方向的 tracked 文字由本行补齐，不再欠 |
| `L58` 第 48 轮：mcmod 许可列按帖内取证批量改口（**闭环 `L31` 的逐行取证 pass**）+ 两条新欠账（禁区侧矛盾、判面覆盖为零）。⚠️ 本行的号本来是 `L56`：写入后 `assert-backlog-table-shape` 报 `BACKLOG NUM_ORDER 断点 57->56@441` —— 并发会话在我落笔期间新增了 `L56`（`limit` 窗口）与 `L57`（leg2），**表形门当场抓到撞号**，遂改为本号。⇒ 未提交的共享登记表上「先数末号再插行」不足以保证不撞（读与写之间会被插队），要稳只能靠这道门复跑。 | **本轮实测**（as-of 2026-09-25，取证 = 用户以内置浏览器逐帖直访 `www.mcmod.cn/post/<id>.html`，判词落 `temp/audit-20260921/mcmod-license-20260925.md`（75 行 · 65 个帖号格）；被改面 = `community_knowledge/links/mcmod-dev-tutorials.md`）。 **动值清单（分母说清）**：虚标 → 第三态 **17 格**、类型错 → 帖页形态 **8 格**、方向反 **2 格**（3836 禁转载实为 BY-NC-SA、3142 未声明实为禁转载）、待核升级/定稿 **2 格**（3793 升显式、4315+6258 定稿未声明）、附加限制补记 **1 格**（3414 的「MCMOD 为唯一发布站」）；另把 **8 个「一格多帖」**拆成逐帖一行（3853/3921/3864、3211/系列02、3978/5838、3814/4163、4473/3419、5487/4403、4315/6258、4392/1458）⇒ 文件 192 → **236 行**、逐行证据位表 **67 行**（有声明 21 + 无声明 46）。第三态措辞与门内 `halfA` / `halfB` **逐字同形**，为的是将来扩判面不必再改正文。 机制与反证：改值走 `temp/ralph-20260922/_v48-license.mjs`（**断言旧值 + 单命中才写**，21 条 SET + 8 条 SPLIT 全命中才落盘；它当场拦下我一次「3414 抄两遍」——第二遍旧值命中 0 列 ⇒ ABORT 未写，随后加了「重复 id 预检」并投毒验证会炸），证据位重建走 `_v48-fixrefs.mjs`（带「每行竖线数必须为 6」的表形硬闸）。修的两处自造缺陷：① 行号解析命中 frontmatter 的 `url:` 行（3282 报成 `:4`，真行 `:19`）⇒ 改成只在以竖线开头的表行里找；② 我在说明句里照抄了门内 needle 的字面串 ⇒ **门把那句说明当成 2571 行来要两半措辞，自造一根假红**（`assert-community-attribution` rc=1），改写成不照抄的表述后 rc=0。 台账：`node mcp-server/scripts/assert-community-attribution.mjs` rc=**0**（`logs/r48-attr2.log`）、`--selftest` rc=**0**（7 例 = 5 投毒必红 + 基线绿 + 不判对照绿）；主表 22 个受影响行按 `post/<id>.html` 加「显式 BY-NC-SA」串复扫 = **残留 0**；`community_knowledge/indexes/index-l0.json` 的该条 `summary` 与 frontmatter **逐字仍相等**（本轮未动 frontmatter ⇒ 无需重跑生产者）；2571 那一行与改前 **逐字节相同**（diff 验）。 **欠账一（要用户动，禁区）**：`community_knowledge/ATTRIBUTION.md:50` 把 2306 / 3603 / 3887 与 3340 / 5442 并列成「显式 BY-NC-SA」，而本轮逐帖直访这三帖**均无声明行** ⇒ 该文件与本表自 2026-09-25 起互相矛盾；`ATTRIBUTION.md` 是禁区（本仓规则 + 该门 F51 豁免的 needle 都在里面），**只能由用户直接编辑**，agent 侧只登记不动。3340 / 5442 不在主表内，无对应行可改。 **欠账二（判据为零覆盖，别把跑绿当验过）**：`assert-community-attribution.mjs` 的裁定面 `THIRD_STATE.key` 只咬「2571 那篇的链接串」所在的行 ⇒ 本轮改的 **67 行取值与措辞全在任何门判面之外**，门 rc=0 对这批改值**不构成任何反证**；要把这批纳入判面，得新增一条「协议列取值必须与逐行证据位表的证据级一致」的门（需另配投毒），属改判据、另轮。 另记一条取证局限：取证只认「本篇教程/本教程/本文」开头的模板声明行，作者换句式就会被判「无声明」⇒「A（无声明）」只说明**没有模板声明行**，不说明作者没有许可意图。 | 用户：① 改 `ATTRIBUTION.md:50` 那 3 条（禁区，agent 不动）；② 拍板「是否把 67 行的协议列纳入判面」（要新门 + 投毒）。排期：`S21/t3` 的 BY-NC-ND 那两行已随本轮逐行化，若还欠「每行显式 row」请按现表复核后再开口。复核命令：`node mcp-server/scripts/assert-community-attribution.mjs`（0）· `node temp/ralph-20260922/_v48-license.mjs`（干跑，报 21/8 命中）· `grep -c '帖内无逐帖显式声明，站级默认 BY-NC-SA 3.0 适用' community_knowledge/links/mcmod-dev-tutorials.md`（24） |
| `L59` 用户 2026-09-25 对 `L58` 所列 12 条的逐项裁定（本行 = 裁定台账，含 A 的推荐与 D 的贴文） | **裁定与落点**：**A**（`S17/t1` 后半句「manifest 写手去重」）= 本轮不执行，要我给推荐 ⇒ **推荐 ③ + 补出处**：本轮把「无写手」这条前提**用三种口径重测过**（精确串 `_manifest.json` 只命中 `audit-data-consistency.mjs` 与本文件；`_manifest` 松散匹配 14 件逐件看全是 `validate_addon_manifest` / `generate_addon_manifest` 等异名 token；补测 `.js` 抓取器 —— `fetch-forge-docs.js:25` 读的是 `data/forge-versions-manifest.json` **另一份件**，其 `:400` 只写页面正文），全仓带写盘语境的 `_manifest` 命中 **0** ⇒ 前提成立；件内 `label` / `priority`（🟡⭐）+ 逐条 `fetchedAt` / `size` 的形态判为**一次性人工或代理产物**（全仓此类件 **6** 份）。⇒ 该子句改判为「**去重判据常驻读侧**」（`G-manifest-dup-key` @ `audit-data-consistency.mjs:689`，其塌法自证在 `:559`），后半句按 ③ 记；出处说明只落本行（`data/**` 不许动）。 **B** = ① **授权限定重跑**（转换器涉及面，先给清单 + dryRun）⇒ 排 R50。**C** = ③ 要正文 ⇒ 放开口子的代价由维护侧承担，但**必须窄化为具名豁免**（点名那 2 个 marker，不得放开成通用旁路，否则 `assert-corpus-faithfulness` 失去「其余件没被改」的证明力）⇒ 排 R50。**D** = ① 用户直接改 `ATTRIBUTION.md:50`，贴文见下 ⇒ 本轮只登记、不动禁区。**E** = ③ 折中：把「第三态措辞须含 `halfA` + `halfB` + as-of」从单行**扩到全表**并配投毒反证 ⇒ 排 R49。**F** = ③ 给 46 行「无声明」统一加更硬的免责措辞 ⇒ 同排 R49（与 E 同面，一轮做完）。**G** = ① **维持关闭**（mcmod 反爬类不重开；今日「梯子路 104/104 = 200」的新反证已在 `community_knowledge/AGENT_USAGE.md`，裁定不变）。**H** = ④ **`all.json.bak` 留在原地**当「旧面可比对物」⇒ 它长期占 `??` 名额，属裁定不是欠账，今后不再报它。**I** = ① **立项**全量重算 8 份带 `mcVersionsByPlatform` 的库上界 ⇒ 排 R51。**J** = **可以**（36 份 SKILL.md 加显式 `modrinthSlug` + 7 宿主镜像 + 重跑 `sync-skills.ps1`）⇒ 排 R52，且必须在 I 之后（同面）。**K** = ① **授权复核**并据结果改点名（`L42` / `L45` 那 4 条 loader 债务，走梯子 + `query_upstream_releases`）⇒ 排 R51。**L** = **现状即规矩**：历史 `L` 行永不改，闭环只写在**新行** ⇒ 已就地写进本节末「新增条目的规矩」那条。 **D 的贴文**（供替换 `ATTRIBUTION.md:50` 的「除 …」从句）：「除 3340 / 5442 为显式 BY-NC-SA（2026-08-23 核实、2026-09-25 未复测，两帖不在本表）、**2306 / 3603 / 3887 经 2026-09-25 内置浏览器逐帖直访确认帖内无模板声明行 ⇒ 只有站级默认 BY-NC-SA 3.0 适用**、4987 为未声明、4880 / 2500 按站点默认 BY-NC-SA 外无特殊声明」。 **本轮实跑**：三口径 grep + `ls data/*/*/_manifest.json` = **6** 份 + 写盘语境 grep = **0** 命中。⚠️ 另记一条方法教训（差点把结论建立错）：本条前两轮用的是**按扩展名筛的 grep**，而生产者完全可能用变量拼文件名 —— 本轮补测 `.js` 与「拼接式」两口径才敢确认；同族 = `L58` 的撞号、`L55` 的等式钉。 | 用户：D 那一行（禁区，只能你改）。我：按 R49（E+F）→ R50（B+C）→ R51（I+K）→ R52（J）排队执行，每轮一个 writer、收口跑门。 |
| `L60` 第 49 轮：闭环 `L58` 欠账二（判面零覆盖）——`assert-community-attribution.mjs` 长出 R49 全表腿 + `L59`-F 免责加硬落地。⚠️ 登记两条「按派单没动」的账（见末段）。 | **实测落点**（as-of 2026-09-25，第 49 轮；派单 = `temp/ralph-20260922/_brief-r49.md`，报告 = 同目录 `round-49-license-gate-widening.md`）。 **E 腿**：门新增纯判据 `thirdStateFace`（双采集器交叉、禁自指）= 证据位表 证据级 逐字 `**A（无声明）**` 的 id 清单 × 主表协议格（split 第 3 格）带 ⚠️ 的行；交集逐格判 `halfA` + `halfB` + `as-of 20xx-xx-xx` 三件套，缺任一判红点名；⚠️ 格 id 不在 A 清单也判红（交叉）。三条地板全 `<`：采集 / 实判 / 含 as-of 现值 **46 / 25 / 25**（分母 = 本门采集器对盘实读，非 grep），地板 **40 / 20 / 20**，注「只许 <、语料扩了要跟着抬」；分母塌 0 ⇒ `COLLECTOR_RETURNED_ZERO` rc=1，证据位小节标题消失 ⇒ `ANCHOR_MISSING` rc=1。反证：门 `--selftest` **15 例全中**（12 投毒必红含 R49 七例 + 基线/对照 + 现盘全表面）；`temp/ralph-20260922/_r49-poison-proofs.mjs` 对**真文本** temp 副本六例全中（撕 halfA / 撕 halfB / 删 as-of / 喂空 / 锚失 / 不判对照 rc=0），三句自证（生产 SHA 前后 `c28d01229813a2b7` 相等 / 无 POISON 残留 / 副本已删）。折中边界写死在门注与正文「判据的实话」段：连 ⚠️ 带措辞**整格改判**仍逃逸 ⇒ 那才是 `L58` 欠账二全量一致新门的活。 **F 腿**：`temp/ralph-20260922/_v48-fixrefs.mjs` 的 `NONE_TXT`（46 格唯一真源）加硬为「①没有模板声明行的事实 + ②不构成对『可否商用 / 可否演绎 / 可否转载』任何方向的判断、点名要引用须先向作者确认 + ③halfA/halfB 逐字保留」，方法边界段同步；两次 `--write`（F 一次、E 改口一次）干跑读数恒 `rows=67 nA=21 nNone=46`；主表 25 个 ⚠️ 格按 `post/` 链接逐格复扫 = as-of 与 `外部-only` 缺项 **0** ⇒ 派单 F.4 的「补缺」本轮无事可补、零格改动。 **两条新登记（本轮按派单不做，别再当「已覆盖」读）**：① **19 个 A（无声明）id 的主表格仍是旧措辞**（「默认 BY-NC-SA」「默认（未声明）」，取证件 `temp/audit-20260921/mcmod-license-20260925.md` 的 `:24/:26-28/:31/:39/:41-42/:44-47/:54/:57-59/:62-63/:65` 共 19 行判 ✅/口径对）⇒ 不在 R49 交集、不受判；要升统一措辞 = 主表 19 格改词（composite 24→43），属新派单。② **3978 / 4403 两格证据行是生成器糊判**：`_v48-fixrefs.mjs` 对「一格两帖」复合行按 `/无声明/.test(page)` 整格采 ⇒ 帖页实测写「3978 有声明行…；5838 无声明」的复合行把 3978 也标成 `**A（无声明）**`（4403 同型，audit `:18/:56` 逐帖坐实两帖**有**声明行、主表 `:32/:101` 已正确引声明行）⇒ 派单 F.3 明令计数 67/21/46 不得变，本轮未修；修 = 动判定逻辑，读数会变 67 / **23** / **44**。 另记本轮一次车道外干扰归因：第 8 步 `test-scripts.mjs` 唯一红 = §S4 门链四数普查「`assert-lib-coord-snapshot.mjs` 无调用方（可达 P=62 ≠ 盘上 K=63）」，该件 mtime = 2026-09-25 22:24 本地（= 开工车道 13:56Z 静读**之后**）、git 状态 `??`、全仓无引用 ⇒ 并发会话在写面，非本轮踩的（同族 = `L55`「谁的 ok:false 谁负责」）。**复核命令**：`node mcp-server/scripts/assert-community-attribution.mjs`（rc=0，末段读 46 / 25 / 25）· `node mcp-server/scripts/assert-community-attribution.mjs --selftest`（rc=0，15 例）· `node temp/ralph-20260922/_r49-poison-proofs.mjs`（rc=0，6/6；件在 temp 不入库，复跑入口以命令为准）。 | 用户：① 要不要把 19 格旧措辞升统一第三态（主表改词派单，composite 24→43，地板须跟着抬）；② 生成器「一格两帖」糊判修复（动 `_v48-fixrefs.mjs` 判定逻辑 ⇒ 计数 67/21/46 → 67/23/44，须重跑 --write + 门复扫，另轮派单）；③ `ATTRIBUTION.md:50` 禁区那条仍欠（`L59`-D 贴文待用户手改，`L58` 欠账一未闭环）。 |
| `L61` 第 48 轮生成器的**按格判类缺陷**被第 49 轮揭出并修复（3978 / 4403 曾被判成「A（无声明）」，与主表自相矛盾）| **归因与复现**：缺陷在 `temp/ralph-20260922/_v48-fixrefs.mjs`，旧写法 `none = /无声明/.test(page)` 拿**整格**文本判类，而取证件 `:18` / `:56` 两格是「一格两帖」形：`3978 「…CC BY-NC 协议。」（无 SA）；5838 无声明` 与 `5487 无声明；4403 「…未经允许禁止转载。」` ⇒ 「无声明」三字属于**另一个帖**，两 id 被连带误判。**主表当时是对的**（那两格由 `_v48-license.mjs` 显式写值），错的只有生成的证据表层 ⇒ 两层面自此互相打脸。**这是第 49 轮 F 件加硬 `NONE_TXT` 时顺带暴露的**：E 件的新采集器 1 就以「A（无声明）」id 清单为真源，错判会直接进判面。 **修法**：改为**按 id 取自己那段**（以 `；` / `;` 切段、段内含该 id 才用；单 id 行仍按整格；切不到段则保守按整格判并留「该格没点名这个 id」的退路），`quoteFor` 同步按段取原文 ⇒ 不再把邻帖的话当本帖的话。 **改后台账（as-of 2026-09-25 实测）**：证据表 **67** 行不变，有声明 **21 → 23**、无声明 **46 → 44**；四格现值 = `:32` 3978 **A**（声明行 = 3978 「…CC BY-NC 协议。」（无 SA））、`:33` 5838 A（无声明）、`:100` 5487 A（无声明）、`:101` 4403 **A**（声明行 = 4403 「…未经允许禁止转载。」）；主表第三态格仍 **24**（本轮零改），`halfA` / `halfB` 全文件各 **70** 处（25 → 70 系第 49 轮把两半逐字嵌进 46 个免责格，口径已分面写清）。 **复跑**：`node mcp-server/scripts/assert-community-attribution.mjs` rc=**0**、`--selftest` rc=**0**（**15 例 = 12 投毒必红 + 2 基线/对照绿 + 1 现盘全表面**）；`--write` 前后各跑一次干跑取计数阶梯（67 恒定、21/46 → 23/44 是唯一被允许的位移）。`data/**` 与 `mcp-server/data/**` 零写入。 ⚠️ **方法教训（记给后人）**：一格多值行**不得整格判类** —— 与 `L47`「表病不点名」、`L58`「撞号」、`L55`「等式钉」同族，全是**采集面形状与判据假设不符**。凡按文本内容分类，先问「这句话在格内属于哪一个 id」。 | **等一句话（属许可面、归 2026-09-01 裁定）**：主表另有 **19** 格「默认族」旧措辞（`默认 BY-NC-SA` / `默认（未声明）`），其证据行本轮已是 A（无声明）⇒ 新判面只覆盖 **25 / 44** 个第三态格。要不要把这 19 格也统一成第三态三件套（覆盖变 44 / 44，代价 = 动 19 个许可格的措辞、需按 F 件同法投毒复跑）—— **一句话即可，不拍板就维持现状**。 |
| `L62`（2026-09-25 23:5x 旁路实测，**新立案、无人动过**）Learn 页脚 chrome 从未被 `NOISE_LINES` 咬住 ⇒ **254 个页脚 chunk 随 20:07 的重建进了语义库**，词法命中页脚措辞时直接吐给用户 | **分母** = `data/bedrock_stable/bedrock-docs/stable/processed/` 递归 **258 篇 .md**，按形态分面：顶层 **20** 篇 = 09-24 跑过 `--reprocess` 的 curated，子目录 **238** 篇 = toc 抓的。逐字串按文件计（`grep -l` 数行数，非数命中行）：`Want to try using Ask Learn` curated **20/20**、toc **234/238**；`## Feedback` 同样 20/20 与 234/238；S14 三条里的 `Was this page helpful?` curated **0/20**、toc **234/238** ⇒ curated 那 20 篇的三条旧噪音确实已消失，**本行不推翻那条旧读法**，残留的是另外两个形状。**索引侧**（同一棵树 `semantic/db.sqlite`，20:07 重建、5312 chunks 总分母）：含 Ask-Learn 的 chunk **254**（distinct doc 254）、含 `## Feedback` 的 **254**、含 `Was this page helpful?` 的 **234**；这 254 块长度 **85–172 字符、254/254 全在 `minContentChars=200` 以下** ⇒ 只进 `chunks` + `chunks_fts`，**0 条进 `chunk_embeddings`**（同批正控：`chunk_embeddings ⋈ chunks` 全量 = 3374 = meta `embedded`，所以那个 0 是「向量的腿没被污染」，不是 join 塌了）。**用户可见面已实测**：`node mcp-server/dist/cli.js search_bedrock_docs` 三发（`dist/` 23:41 构建）——`--query="Ask Learn"` 时 20 条命中里 **10 条 `matches[].snippet` 就是页脚本身**（首条 `stable/documents/learningjourneyguide`，snippet 起 `## Feedback Was this page helpful? Yes No No …`）；`block components list` 与 `custom block geometry` 两发 chrome snippet = **0**（同一个解析器，故 0 是真负例、不是探针失效）。**机制**：`mcp-server/scripts/_lib/bedrock-corpus.mjs` 的 `NOISE_LINES`（按符号引用，该件现 ` M`）里 `/^Ask Learn/i` 要求行首，而盘上是「前导空格 + `Want to try …`」；`/^Feedback$/i` 要求整行恰为 `Feedback`，而盘上是 `## Feedback` 标题行 ⇒ 两条式子永不咬合。238 那批的新鲜度 `未复核`（`--reprocess` 只覆盖 20 个 curated id 属该脚本自述设计，见 `L33`）。 | **用户两问**：① 是否扩 `NOISE_LINES` 那两条式子（改转换器侧，**禁**手工回改 `data/**`，见「语料不回改」口径）；② 若扩，**过滤与重建必须同批** —— 本行就是「重建单独跑、过滤没跟上」的产物。代价 = 一次 `--reprocess`（20 篇）+ 一次 bedrock 索引重建 + G4 重签，且须与 bedrock 车道的写窗口错门。**不做也辩护得住**：chrome 只在查询词法命中页脚措辞时上位，内容型查询实测 0/20。维护侧在点头前不动 `_lib/bedrock-corpus.mjs`、不重建。 |
| `L63`（2026-09-26 00:2x，side chat 旁路）**审计项 K42 的定性被推翻，规则已就地改文（8 宿主）**：`05-blocks-items.mdc` 那句 `must include both` **不是伪造引文**，真错在**出处层级** | **立案方的判据本轮复跑，数字对、结论错**：`grep -rlF "must include both" data/bedrock_stable/` = **0 个文件**（该轮记 888 文件全扫，我这轮同一条命令复测仍 0）⇒ 但**「本仓语料 0 命中」推不出「上游没这句」**。本轮自己抓 Learn 两张组件参考页 `minecraftblock_geometry` 与 `minecraftblock_material_instances`（页内 `ms.date 2025-02-11`、`updated_at 2026-08-25`），**两页各带一条同文 Note**，逐字即该句 ⇒ 成对约束为**真**，`format_version` 1.21.80+ 只给 `geometry` 不给 `material_instances` 确实不合官方口径；同页另给两条规则原先没写的下限：组件本身要 **≥ 1.19.40**，`isotropic` / `tint_method` 才要 1.21.80+。**真缺陷 = 出处错标 + 示例名无据**：旧文把参考页的 Note 挂在教程页 `stable/custom-block` 名下、只写「Learn 原文」不给页名，而该句不在本仓语料；同件示例里的 `demo_die` 语料 **0 命中**。**改文**：`bedrock/.cursor/rules/05-blocks-items.mdc:34` 拆成两层出处（**外部-only** = 该 Note，点名「本仓 0 命中、引用须现场 WebFetch、不得称语料逐字」；**本仓语料逐字** = `processed/custom-block.md:309`、`:311`、`:335-336` 的 die 示例），示例纹理名改 `die_red` + `"render_method": "opaque"`（组件级 `"*"` 写法由该参考页 Samples 支撑；本仓 `:359-362` 是 `item_visual` 内的同形写法）；再 `powershell -File scripts/sync-skills.ps1 -TargetDir <repo>\bedrock` 同步。**复验**：`demo_die` 全仓 **0** 残留、`die_red` 在 **8/8** 宿主、`assert-skill-mirrors` rc=0（扫档 57 · 镜像比对目标 9758 · AGENTS 漂移 0）。 | **用户两笔**：① 提交（`git status --porcelain bedrock/` 现 **32** = 01/05/07/09 四条规则 × 8 宿主，其中 07/09 是 09-23 的旧改、不属本轮）。② 若仍要立「规则正文自称『××原文』须逐字命中」那道门，**先量爆炸半径**：本条就是「语料 0 命中但上游真有」的实例 ⇒ 只按本仓语料判红会误伤，判据必须留「外部-only + 给页名」这一档。维护侧**未建门**。 |
| `L64`（同轮）**审计项 K47：缺陷成立，但它开的修法也成立不了**（规则已按实现改文，8 宿主） | **缺陷侧（本轮读实现坐实）**：旧句「`validate_addon_manifest` / `generate_addon_manifest` 只收整数形与 beta 串」——`mcp-server/src/bedrock/index.ts` 的 manifest 校验分支是「`typeof` 非 `number`、或 `!Number.isInteger(...)`、或值 `< 1`，三条任一命中」⇒ 报「**format_version 必须是正整数**」，任何字符串（含 `"beta"`）都拒；`generate_addon_manifest` 恒写 `format_version: 2` ⇒ 「beta 串」确为误记。**修法侧（本轮推翻立案方给的替换文）**：其建议「`--beta` 改的是 `min_engine_version`」**不成立**——同件里 `const min = args.minEngineVersion ?? [1, 26, 44]` 与 beta 无关；`args.beta` 实际只做两件事：① 追加「依赖实验性玩法 / Beta APIs」警示，② script 模块的 `dependencies[].version` 由 `stableVer` 换成字面量 `"beta"`。⇒ 旧文案那个「beta 串」是 **`dependencies.version` 的形态被错搬进 `format_version`**，改文按这三件事写，并**保留** semver 形被有意拒绝那半句与其 W5-2 出处。**复验**：旧句 `只收整数形与 beta 串` 现全仓 **0** 命中；`bedrock/` 脏件数见 `L63` 第三列（同一批 32）。 | **用户**：与 `L63` 同批提交（agent 禁 add/commit）。维护侧无可代劳。⚠️ 另记一笔归属：`temp/ralph-audit-20260925/HANDOFF-FIX.md` 与 `STATE.md:215` 里 K42 仍写作「伪造引文 / 亲验」、K47 的替换文仍是「改 `min_engine_version`」——那是**兄弟会话正在跑的活物**，本轮不改它，由该车道按本两行自行更正。 |
| `L65` 用户裁定「也统一成三件套」⇒ 主表 19 个「默认族」协议格并口，`thirdStateFace` 判面覆盖 **25 → 44 / 44**；顺带挖出一条 **3.0 vs 4.0** 的新口径冲突（⚠️ 本行原编号 `L62`，被并发会话抢先占用后改号 —— 同族 = `L58` 的撞号，两次都被 `assert-backlog-table-shape` 抓到） | **做了哪些**：19 格（`2338 4987 2012 1347 4679 2955 2049 1863 3846 549 1560 3160 4880 3012 2194 4930 3881 6659 2523`，as-of 本轮现扫）从 `默认 BY-NC-SA` / `默认（未声明）` / `默认（未声明，译作）` 改为第三态三件套（`halfA` + `halfB` + `as-of 2026-09-25` + `内置浏览器直访` + `外部-only`）。真源仍是脚本 = `temp/ralph-20260922/_v50-defaults-to-thirdstate.mjs`（**断言旧值属于三个枚举之一 + 单命中才写**；19/19 命中；收尾自检 = 「第三态格数增 19」且「旧枚举格残留 0」，任一不符即 ABORT 不写盘）。`3881` 的「**译作**」与 `3160` 的口径冲突以**追加子句**保留，没被模板冲掉。 **判面确实变宽（不是只跑绿）**：门自己播报 **A（无声明）44 / 实判主表行 44 / 含 as-of 44（地板 40/20/20 全 `<`）**，改前该读数是 46/25/25 ⇒ 覆盖 25→44 有据；`--selftest` 15 例（12 投毒 + 2 对照 + 1 现盘全表面）rc=**0**。 **⚠️ 新发现的口径冲突（等用户裁，别沉在表里）**：`3160`（KubeJS6 面向新手的配方魔改教程）现按本页 halfB 写「站级默认 BY-NC-SA **3.0**」，而 `ATTRIBUTION.md:49` 逐字记它「页面未见显式声明，按站点默认 BY-NC-SA **4.0** 处理」。两个数不可同真：站页脚（`L59` D 项取证）说的是 3.0，`:49` 说的 4.0 要么是当时抓的**该帖自身**声明、要么是当时写错版本号。已在该格内点名，**未据此改判、也未动禁区**。 **未做**：未复测帖页（本轮无联网）；`ATTRIBUTION.md` 一个字没动。复核：`node mcp-server/scripts/assert-community-attribution.mjs`（0）· `node temp/ralph-20260922/_v50-defaults-to-thirdstate.mjs`（干跑，报 19/19 与两侧计数）· `grep -c '帖内无逐帖显式声明，站级默认 BY-NC-SA 3.0 适用' community_knowledge/links/mcmod-dev-tutorials.md`（**43**，另 1 格是 2571 的复合句式 ⇒ 判面 44） | 用户一句话：`3160` 的站点默认到底取 **3.0** 还是 **4.0**（取 4.0 就得同时给 `ATTRIBUTION.md:49` 与本表两句都对得上的写法）。 |
| `L66` 第 50 轮：闭环 `L59`-B（转换器动作面留痕改用读侧办成）+ `L59`-C 的**前提被实测推翻**，落成「默认必红 + 具名才免」两道新地板而不是豁免条目。 | **B（真身 = bedrock 语料转换器，不是派单猜的映射脚本）**：转换器 = `mcp-server/scripts/fetch-bedrock-docs.js` + `_lib/bedrock-corpus.mjs`（账在 `PLAN-STATE-r2.md:51` 与 `_final-census-131.md:139` 的 S14/t6）。派单 §1 猜的 `build-yarn-*` / `convert*` / `*-mappings*` / `extract*` 全是**映射表件**，且 `build-*-pairs.mjs` 经 `write-guard` 的 `scratchWriteText` 只写 `$MC_SKILL_CACHE`、目标落进仓库即 throw，压根写不到 `data/**` —— 照猜测跑会量错一整轮。dryRun 两腿真跑 rc=0：`--reprocess`（不给 `--write`；脚本在 418 行 return，**根本不调 `learnToMarkdown`**）与 `--retag`（离线判据重放，index-l0 258 条 · release-notes 31 · 需改动 0）。字节证明：`temp/ralph-20260922/logs/r50-B-snap-{before,after-legs}.json` 前后 889 件 / 26,793,508 B / 聚合内容哈希 `6284fddcc2ad1deb` **相等**，内容变化 0 · mtime 变化 0。`--write` 腿按 §1.4 **未跑**（它必写 `data/bedrock_stable/bedrock-docs/stable/processed/*.md` + `index-l0.json` + `fingerprints.json`）。 **动作面留痕改用读侧办成**（census 那句「补它需写 `data/**`」不成立）：`--reprocess` 要的 raw HTML 不在仓内（`find data/bedrock_stable -name '*.html'` = 0），于是用授权 curl 形态取回 4 页（HTTP 200 · 51,606–108,323 B）落 temp，`learnToMarkdown` **内存重跑** —— 盘上 vs 工作树版 宽行 **291 / 291** · 表张 **12 / 12**（逐页 entity-components 209/2、biomes 18/1、pack-manifest 64/9、getting-started 0/0 逐项等），同输入 HEAD 版（`de5e8a7c`）= **0 表**。⇒ `_audit2-S1-S14.md:237` 悬着的归因今天定了：10/882 的暴涨来自 `tableToMd` 死路径修复，不是 S15 重处理；基线本轮复扫 = **10 文件 / 882 .md**（宽口径与严口径同值，命中行 570），不是 R83 警告的 2。预期差量化：4 页若真 `--write` = **+4/-4 行**，每页恰 1 行且唯一内容是 `> 抓取时间：` 时间戳，行数一页不差（225/29/194/170）⇒ 内容零漂移。⚠️ 顺带挖出一条归属风险：**盘上语料是未提交版转换器产出的**（`_lib/bedrock-corpus.mjs` 工作树 mtime 09-24 02:43Z 晚于语料 02:07Z，那 67 行未提交改动正含 `tableToMd` 处）⇒「先 `git checkout` 该文件、再跑 `--reprocess --write`」会把 10 个文件 291 行表格全删成 0。 **C（前提推翻）**：派单 §2 说那 2 个 marker（`data/fabric_26.1.2/fabric-docs/26.1.2/{processed,raw}/develop_automatic-testing.md` 的 `@/.github/workflows/build.yaml#automatic_testing_game_test_3`）的 reference 件不在仓内 —— 实测**在**：26.1.2 / 1.21.11 / 1.21.10 / 1.21.8 四档各有 2,908 B 的 `build.yaml`，`git hash-object` = `1d55d026c886a0f2f828029f906dc028b51249a0` 与该档 `reference.provenance.json` 的 blobSha 逐字相等；区段标记在 `:60/:70/:72/:77` 成对；读者展开该页 `angleSites=11 · angleMissing=[] · angleRegionMiss=0` 且块内是真 YAML；全档 2,305 处标记 **NotFound 0**；取件端 `--plan`（离线）报 2,282/2,282 目标能在 main 树解析。梯子两通道各 HTTP 200 / 2,908 B / sha256 `38e241fd…4261`，与盘上件逐字节相同（`logs/r50-C-ladder-fetch.log`）。⇒ **没有件需要豁免**，照派单塞两条进 allowlist 反而是给忠实件挂一张「允许它不等于上游」的空白票 —— 那正是 `L59`-C 自己禁止的「整体放松」。改落两道地板：**A10** `<<<` 的 blob 缺失 ⇒ **默认必红**，只有 `MISSING_BLOB_ALLOWLIST` 五件齐（pack / target / reason / asOf / evidence，asOf 还必须 `YYYY-MM-DD`）才放行，条目本轮没被任何站点用上即判**僵尸豁免**红（本主题下「僵尸」的正确形状与派单字面相反：条目豁免的是「缺失」，所以缺口一补回条目就失配）；**A11** 采集面塌 0 ⇒ `COLLECTOR_RETURNED_ZERO` + rc=1（旧盘实测：指空目录报「0 棵…树」还 rc=0，那是判据没吃到输入）。真根现值 `directiveSites 633 = directiveOnDisk 633` ⇒ 缺口 0 ⇒ A10 是**零字节变更的收紧**；缺口数由既有两锚相减隐含，**未新增台账字段、未跑 `MC_SKILL_CORPUS_RELEDGER=1`**（无需重算）。反证：`temp/ralph-20260922/_r50-C-poison-proofs.mjs` **19 断言全中 rc=0**（投毒全在 temp 假根与 temp 门副本；生产门 sha 前后 `12007013984d2083` 相等、副本零残留）；§S4-G3 另补 2 夹具把两条新腿钉进默认门链（投毒 10 → **12 记**，本轮该块实跑绿）。派单 §2.4⑤ 要的 `--selftest` **本门没有**（全仓该字样只在别的门里），等价面 = §S4-G3 + 上述 temp 夹具。 **本轮三次自造假红（记给后人）**：① 注入用 `replace` 后比 `patched===src` 判「找不到字面量」⇒ 空表注空表被误杀，改成 `includes` 预检；② 夹具 `w()` 用 `lastIndexOf("/")` 取父目录，本机 `join` 给反斜杠 ⇒ 把 `page.md` 砍成空目录 `page.m`，反手触发 A9 空壳判据，一次造出 12 条假失败；③ 门副本一度写进 `mcp-server/temp/r50-c-poison/`，深一层使 `../dist` 解析失败 ⇒ 红腿全靠「缺 dist」蒙过，现 `run()` 见「缺 ../dist / npm run build」直接抛为夹具事故。三条同族形状 = **红的来源必须先证明是判据给的**（与 `L58` 撞号、`L55` 等式钉一家）。 | ① **B 的 `--write` 真跑要不要做**：清单与预期差已给（20 页各 1 行时间戳 + `index-l0.json` / `fingerprints.json` 抖动，内容 0 漂移），代价是动 `data/**`，且 `L62` 那条「过滤与重建必须同批」还欠着 ⇒ 单跑重建会把页脚 chrome 再灌一遍。② bedrock 转换器 67 行未提交改动 + 「**语料由未提交代码产出**」这笔账归谁：要么提交 `_lib/bedrock-corpus.mjs`，要么明确提交前禁跑 `--reprocess --write`。③ 门链第 8 步现红**不是本轮的**：`assert-index-consistency(G4)` 报 5 个未登记 yarn 库 `data/forge_{1.17.1,1.18.2,1.19.4,1.20.1,1.20.4}/mappings/yarn-mappings.sqlite`（实测全部 `??` 未跟踪、mtime 09-26 00:43–00:45、单件 42.8–56.6 MB，正是开工车道 16:42:06Z 那批写入），修法在兄弟车道跑 `MC_SKILL_INDEX_RELEDGER=1`，本轮**不代跑不代登**。另因 G4 在 `mcp-server/test-scripts.mjs:1921` 断言处短路，§S4「门链四数」普查（该文件 2160 行之后）本轮**没跑到** ⇒ 派单 §4.2 要的「跑前跑后各记一次 P/K」拿不到，不是漏记。 |
| `L67` 未做③（Forge 1.17+ SRG 成员层）落地后的**唯一残账**：15 件产物在盘但全部未跟踪，而真库可用性腿是**硬腿** ⇒ 干净 clone 必红（第 51 轮登记） | **本轮实测**（as-of 2026-09-26，仓库根）。未跟踪三簇 = `data/forge_<v>/mappings/` 下 6 份削减件 `srg_to_official-<v>.tsrg`（合计 32,273,362 B）+ 6 份 `mcp_config-<v>-<ts>.provenance.json`（14,007 B）+ 6 份 `yarn-mappings.sqlite`（现扫 33.6 / 40.8 / 42.8 / 50.2 / 51.0 / 54.0 MB = **272.4 MB**）⇒ 共 18 件 / 303.2 MB。同目录另有 4 件 `??`（`methods.csv` / `fields.csv` / `params.csv` / `mcp_snapshot-20210309.provenance.json`）**不是本轮产物**，别并进来记账。分母与先例：同路径下**已跟踪**的映射库 22 份合计 **540.2 MB**（fabric 13 · forge 9），且 `data/forge_1.16.5/mappings/obf_to_srg.tsrg` 早就是 MCPConfig 派生件且 tracked ⇒ 入库 = +44% 且有同类先例；原始 `client.txt` 继续按 .gitignore 排除，provenance 里逐档写了许可姿态。**硬腿三条**（库不在盘 ⇒ `mcp-server/test-core.mjs` 判红，**不会**静默 skip）：`s2Srg1201`（等值断言 1.20.1 的 AT 行含 `m_137477_`）、`srgLeak201` 与 `resolveCsvMappingDbPath("1.20.1", null) === null` / `("1.20.1","forge")` 为字符串那对、`s2NoDb`（forge 1.21.1 无库档必须仍留 `<TODO:SRG名>`）。**别把夹具门当兜底**：`mcp-server/scripts/assert-forge-srg-ingest.mjs` 自建 OS tmpdir 夹具、不读这 15 件 ⇒ 它绿**不代表**真库在盘（其在盘对账腿会明印 `在盘对账：0 档 … 该腿本轮未判`）。复算入口：`git status --porcelain data` 看那 15 行 `??` · `du -b data/forge_*/mappings/yarn-mappings.sqlite` · `node mcp-server/scripts/assert-forge-srg-ingest.mjs`（对账腿印逐档 sha256 ↔ provenance ↔ 库 meta 是否同源） | **用户，两选一**（agent 禁 `git add` / `commit` / `push`）：① 提交那 15 件 ⇒ 三条硬腿按现状生效；② 不提交 ⇒ 把三条腿从 `test-core` 挪进上面那道夹具门（维护侧下轮可代劳，但要先定，否则第 8 步链在干净 clone 上是红的）。本轮**已按 ① 的前提写腿**，选 ② 只需挪位置、判定内容不变 |
| `L68` 第 50–51 轮收口（B 读侧办成 · **C 的前提被证伪 ⇒ `L59`-C 裁定落空** · I/K 重算完但**未落库面** · **我两处错登记：`rei` 在面 + 把话安在第 47 轮头上**） | **一、C 件（用户裁 ③「要正文、放窄豁免」）的对象不存在**——我那句「2 个 marker 解析通了、取回区间为空」是错的。两机制实测：① 4 档 `data/fabric_*/.github/workflows/build.yaml` **都在盘**（各 2 908 B、`git hash-object` 同 `1d55d026…`、与 `reference.provenance.json` 的 `blobSha`/`sha256`/`bytes` 三项相符）；② 我自己跑**真 reader**（`node mcp-server/dist/cli.js get_fabric_doc_full --id=develop_automatic-testing --version=26.1.2`）⇒ 15 524 B、含 `runs-on: ubuntu`、**裸标记 0**。错因 = **拿原文文件当渲染面读**：语料按设计保留 `<<<`，展开发生在 reader（根 `AGENTS.md` 明写），我先前在 `processed/*.md` 里 grep `runs-on` 得 0 就判「区间空」。⇒ `data/**` 零写入、豁免未建（建了就是给忠实件挂空白票，正是 `L59`-C 自己禁止的）。它换成两道真地板：**A10**（blob 缺失由无条件 `continue` 改成默认 `fail()` + 具名 `MISSING_BLOB_ALLOWLIST`，五字段齐才放行、缺口补回即判僵尸红）+ **A11**（`findTrees()` 实扫 0 棵 ⇒ `COLLECTOR_RETURNED_ZERO`）。我独立复跑：生产 rc=0（49 树 · `<<<` 633 处 · **633 处字节均在盘 ⇒ 缺口 0**）、自建空根投毒 `MC_SKILL_CORPUS_TEST_ROOT=<空目录>` ⇒ rc=**1** ✓。⚠️ 另记一条伪守卫：这道门**没有 `--selftest`**，传该 flag 会照常跑生产判据并 rc=0 —— 我第一次就把它当「自检全中」读了。 **二、B 件（用户裁 ①）办成且未越面**：4 页 × 三面对照（disk / HEAD / worktree）⇒ worktree `{宽行 291, 表 12}` 与盘上**逐面相等**、head `{0,0}` ⇒ 坐实「2→10 归因 = `tableToMd` 死路径修复」，`_audit2-S1-S14.md:237` 悬案结；基线分母 `find data/bedrock_stable -name '*.md'` = **882**，宽口径 10/882。`--write` 按停手条件未跑（该腿必改 `processed/*.md` + `index-l0.json` + 指纹）。**新暴露风险**：盘上 bedrock 语料由**未提交版**转换器产出 ⇒ 提交若漏那支 `_lib/bedrock-corp*` 脚本，日后重跑会把已修的表格洗空。 **三、I+K 重算（只测不写，车道两段均 BUSY：并发会话整轮在写 `mcp-server/**`）**：带窗口的库文件 = **11 份**（我派单写 8 ⇒ 错）；38 组窗口 = **低报 1 / 越界 0 / 相符 34 / 形状不可判 3**（geckolib 两开放形 + caelus/fabric「点分 release 零行」，与门内「快照零构件跳过」同族）。唯一该改 = `knowledge/libs/all-platforms/mc-architectury/SKILL.md:6` `neoforge=1.20.2-**26.2** → **26.3**`（两腿同证：构件行 `architectury-neoforge-22.0.2.jar` + 上游 `[NeoForge 26.3] v22.0.2` 2026-09-18）。K 侧：派单点名的「四条 `TRIAD_OVERCLAIM_DEBT`」**已在第 34 轮清掉**（我拿陈旧表派单），现表 **6 条、名字不同**：`mc-spruceui/forge+neoforge` **撤销（构件面错）**—— 该库改名 `obsidianui`，按 id `E0L8mfJZ` 读到 101 build（forge 44 / neoforge 22）；`mc-spruceui/quilt`、`mc-libgui/quilt`、`mc-server-translations/forge+neoforge` **维持**（Modrinth 404 + search 0，其构件行走 GitHub/Cotton-maven/Nucleoid-maven 三条非 Modrinth 渠道，本轮未读）；INFO 现面 = **3** 条非 5。 **四、我的两处错（如实留）**：① 「`rei` 至今不在构件面」**假** —— 现测 `rei` 有 **84** 行；② 我把它写成「第 47 轮已证」，而第 47 轮报告第 **148** 行逐字是「`rei` **在面**、49 个 slug 名单已列」⇒ 属**引用他件时没打开原文**，同族 = `feedback-subagent-claim-adjudication`。连带 `INFO_DEBT_DECIDED` 里据它写的定案句已失实（登记待改，本轮没动 `mcp-server/scripts/**`）。 **落地态**：改动清单在 `temp/ralph-20260922/_r51-changes.json`（**8 条**，锚点校验我复跑 `ANCHORS_ALL_OK entries=8` rc=0）⇒ 下一条**车道 IDLE 的轮**照它写盘。 | 用户：`3160` 站点默认取 3.0 还是 4.0（`L65`）、`ATTRIBUTION.md:50` 那一行（`L59`-D）仍在等。主循环：待 `mcp-server/**` 车道静下来后按 `temp/ralph-20260922/_r51-changes.json` 落 8 条 + 改 `INFO_DEBT_DECIDED` 失实句 + 给 `assert-corpus-faithfulness` 补 `--selftest`（补了才算真有两档）。复核：`node mcp-server/scripts/assert-corpus-faithfulness.mjs`（0）· `MC_SKILL_CORPUS_TEST_ROOT=<空目录> 同命令`（1）· `node temp/ralph-20260922/_r51-verify-anchors.mjs`（0）· `node mcp-server/dist/cli.js get_fabric_doc_full --id=develop_automatic-testing --version=26.1.2`（含 `runs-on`、裸标记 0） |
| `L69` 第 51 轮落库的**连带发现**：判据⑤ 的 INFO 面 2 → **3 条**，新那条至今**未定案** | 门现盘自述（as-of 2026-09-26 02:2x，逐字）「§S22 判据⑤ INFO（不判、不计入退出码）: 总面 3 条 = 未定案 1 + 已定案 2（定案表 2 条 / 2 条正对应现盘 INFO）」⇒ 定案表没有僵尸条目，新增的 1 条**不在表里**。**新 INFO = `mc-compat-jei:rift`**：合并串 `emi,jei,rei` 里有 `rift` 行而 `catalog.loaders=[fabric,forge,neoforge]` 未点名 = 「少推荐」，按 2026-09-22 裁定④ **不判红、不进退出码**（门 main 仍 rc=0、`--selftest` 73 例 rc=0）。**成因不是本轮的写回**：构件面 mtime `2026-09-25T06:21:20Z`（09-25 那次重抓）把 `rei` 带进面 ⇒ 现测 **84 行**，其中 `rift` 2 行 / **2 release**（上界 `1.13.2`，`RoughlyEnoughItems` 的 Rift 移植件），而 `jei`、`emi` 各自 **rift 0 行**（探针 `temp/ralph-20260922/_r51-rei-probe.mjs`，只读）。**同一次测量顺手把 `INFO_DEBT_DECIDED[mc-compat-jei/quilt].basis` 里那句错话改了**：原文写「`rei` 该 slug 根本不在构件面」，实测 rei 在面、只是 **quilt 0 行**（三 slug 的 quilt 分解现测 = `emi` 13 行 / 13 release / 上界 1.21.1、`jei` 0 行、`rei` 0 行）⇒ **判定不变（不补）、依据由「不在面」升为「在面无该行」**，已按此重写该串并注「第 51 轮据构件面复核改点名」。另一条 `mc-curios/fabric` 的 basis 本轮复核**仍真**（`curios` fabric 1 行、`versionType=beta`、release 0）⇒ 未改。**为何不自行定案**：给 rift 定「不补」= 往 `INFO_DEBT_DECIDED` 加第 3 条（须带 basis），反过来给 `mc-compat-jei` 点名 rift = 动 `authored/library-integration-jei-emi.md` 源稿并重跑 `build-library-catalog-from-authored.mjs` —— 两条都是内容判断，归用户。**同轮还查出循环 lint 自己的两个缺陷**（`temp/ralph-20260922/_anchor-lint.mjs`，非入库门、门链里没有它）：① `rNN-` 分支的捕获组不含前导下划线 ⇒ 任何 `_rNN-*.json` 引用都被按去掉下划线的名字去找 ⇒ 恒假红（本轮 `_r51-changes.json` 就这样被报 MISSING，我先误判成「引用没写全路径」并据此改了一处形式，改完仍红才发现是 lint 的错）；② 裸名分支只在 `temp/ralph-20260922/` 与 `logs/` 两处找 ⇒ 引用 `mcp-server/docs/evidence-*.md` 必然假红。两处已修（捕获组补上前导下划线并加 `.mjs` 后缀、候选根加 `mcp-server/docs` 与仓库根），**修后投毒仍全红**：另写夹具引用三发编号 99 的假产物名（`.json` / `.md` 两形状 + 一个不带前缀的 `verify-` 形状），rc=1 逐名点出，而两条真引用不再报 ⇒ 放宽没把它改成摆设（夹具与假名按「描述 needle 不得逐字含 needle」的规矩只留在 `temp/`，不写进本表，否则每轮都要为它们加豁免）。**残留 1 记真红**（不是 lint 的问题）：`L51` 时代起的引用 `temp/ralph-20260922/round-44-failure-inheritance.md` **在盘上从来没有这个文件**（同轮真实产物叫 `round-44-ASSESSED-BY-MAIN.md`），bare 与显式路径两种写法各报一次 ⇒ 悬空指针。按「历史行永不改」规矩**本行只登记不代改**；要清有两条路：把那处引用改成真实件名，或补写那份从未产出的报告（后者等于事后发明证据，不推荐）。 | **待用户一句话**（二选一，或明确「维持永久噪音」）。选「不补」则贴进 `mcp-server/scripts/assert-lib-session-resolve-parity.mjs` 的 `INFO_DEBT_DECIDED` 数组末尾：`{ skill: "mc-compat-jei", loader: "rift", verdict: "not-add", basis: "第 51 轮实测：合并串里 rift 行全部来自 rei 一家（rei 2 行 / 2 release，上界 1.13.2；jei 与 emi 各 rift 0 行）⇒ 点名 rift 就是让 REI 替 JEI 背书，与同表 quilt 条同形。分母 = mcp-server/data/lib-manifests/all.json（as-of mtime 2026-09-25T06:21Z）；探针 temp/ralph-20260922/_r51-rei-probe.mjs。as-of 2026-09-26" }`，贴后跑 `node mcp-server/scripts/assert-lib-session-resolve-parity.mjs`（应 rc=0 且播报 3 = 未定案 0 + 已定案 3）。选「点名」则改源稿后须重跑生产者并复跑该门 + `assert-lib-census-counts`（85 锚点腿）。 |
| `L70` 第 52 轮 J 件落库后的两笔残账：库面 36 份里 **29 份**已带显式 Modrinth slug 键、**7 份**三条腿全无未加，**且「刷 7 个宿主镜像」这个前提被实测证伪** | **本轮实测**（as-of 2026-09-26 02:4x；`node temp/ralph-20260922/_v52-snapshot.mjs before/after` + `_v52-diffcheck.mjs`，后者 rc=0）：分母 = `find knowledge/libs -name SKILL.md` 现扫 **36** 份（改前带该键 0 份）⇒ 写 29 份、每份恰 +1 行、净 +738B，其余 7 份按三条腿口径无证据（`mc-libgui` · `mc-server-translations` · `mc-spruceui` 三份 authored 短文自述 Modrinth 已下架且该键显式空串，其 all.json 行是 `captureState` 为 notfound 的继承占位；`mc-lib-catalog` · `mc-author-shared-libs` 是总目录与全家桶、自身源稿 0 处 modrinth 字样；两份基岩 Script API 非 Modrinth 项目）。本轮写入的 28 枚去重 slug **全部**经上游 `/v2/project/<slug>` 现读回显同名（`temp/ralph-20260922/_v52-api-check.json`：http200 共 29 发含一枚项目 ID 探针、回显同名 28/28、失败 0）。**镜像面**：`scripts/sync-skills.ps1` 的读取根只有 `<档>/.cursor/skills`（全文 0 处引用 `knowledge/libs`）⇒ 库面源稿改动**产不出任何镜像差**：镜像门 `assert-skill-mirrors` 改前改后同一行读数（扫档 57 · 镜像比对目标 9758 · 技能正文 1257），宿主树（`.claude` `.continue` `.trae` `.opencode` `.agents` `.zcode` `.pi`）里带该键的文件 **0 份**；「7 宿主」真正对应的是 **47 项档内同形手稿**（`mc-compat-jei` 33 项 + `mc-cloth-config` 14 项，各 × 7 = 329 个镜像文件，实测缺镜像 0 项），而它们是 per-version 文档、正文不由中心稿生成（`scripts/project-cloth-skill.mjs` 只回填注入标记行） | **待裁两问**：① 键要不要传导进那 47 项档内手稿（要 ⇒ 先定「谁生成手稿正文」，因为 `sync-skills.ps1` 不读库面、投影脚本不再生正文；不要 ⇒ J 件本轮即闭环）。② `mc-config`（配置选型总纲）本轮按「只写自己源稿逐字支持的 slug」落单值，而 catalog 里 `skillId=mc-config` 有 5 行、其中 4 行各带一枚 slug（另 3 枚 = `forge-config-api-port` · `fzzy-config` · `midnightlib`，在该稿正文 **0 命中**）⇒ 合并成多值还是维持单值属口径判断；`mc-author-shared-libs` 同题（家族 15 枚 slug 逐行清单见本轮探针 `temp/ralph-20260922/_v52-evidence.json` 与 catalog 现扫输出）。**同轮另登记一记别人的红**（不修）：全量 runner `temp/ralph-20260922/_r31-run-all-legs.mjs` 62 腿里 leg 14 `node test-w3-low.mjs` rc=1，红在 F131 块从 `mcp-server/src/mappings/convert.ts` 抽条件时只替换单参形 `resolveCsvMappingDbPath(version)`，而该文件 :485 已是两参 `resolveCsvMappingDbPath(version, prefer)`（mtime 2026-09-26 01:05，早于本轮开工 02:2x）⇒ eval 出未定义函数；该腿 0 处引用 `knowledge/libs`，与本表两笔残账无关 |
**新增条目的规矩**：沿用 `L<n>` 递增编号，一条一行，`现状` 必须带分母 + 口径 + as-of 或明写 `未复核`；**禁止**把上一轮报告的状态原样抄交（那是「被框架错的前提能活很多轮」的成因，见 `temp/ralph-20260922/round-22-final-sweep.md` §6 第 10 条）。闭环的条目**不删行**。**2026-09-25 用户裁定（`L59` 的 L 项）：规矩就此定为「历史行永不改，闭环只写在新增行里」**——原句写的是「改写为『已闭环 + 复核命令』」，与实际做法（第 47 轮 §5.2 起：往历史格插指针会破坏「一行登记当轮实况」的归因结构）相反，故按现状收敛此句。此后闭环一条 `L##` 的正确做法 = 在**新行**里写「闭环 `L##`」+ 复核命令，被闭环的那一行一字不改地留着当实况。

---

## 问题与讨论

如有疑问或想法，欢迎提交 Issue。配置 MCP 时请先阅读 [`AUTO_SETUP.md`](./AUTO_SETUP.md)。
