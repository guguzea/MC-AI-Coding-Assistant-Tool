# 版本变更记录：1.20.x

## 1.19.4 → 1.20.x 主要差异

> 以下为 Forge 1.19.4 与 1.20.x 的主要差异说明：

### 已知差异

| 项目 | 1.19.4 | 1.20.x |
|------|---------|---------|
| Minecraft 版本 | 1.19.4 | 1.20+ |
| Forge 版本 | 43.x | 47.x+ |
| pack_format | 9 | 15（1.20.1） |
| Mappings | Parchment 1.19.4-2023.04.04 | Parchment 1.20.1-2023.09.03 |
| MobCategory | 使用 | 使用（1.20.7+ 重命名为 SpawnGroup） |

### 1.20.x 通用重要变更

#### DeferredRegister 成为唯一推荐

1.20.x 继续推荐使用 DeferredRegister：

```java
// 推荐方式
public static final DeferredRegister<Block> BLOCKS =
    DeferredRegister.create(ForgeRegistries.BLOCKS, MOD_ID);
BLOCKS.register(modEventBus);

// ❌ 旧方式（1.18 之前）
@SubscribeEvent
public static void register(RegisterEvent event) {
    event.register(ForgeRegistries.BLOCKS).register("my_block", MyBlock::new);
}
```

#### BlockBehaviour.Properties 变化

1.17+ 移除了部分旧方法，使用 `of()` 静态工厂：

```java
// ✅ 推荐（链式调用）
BlockBehaviour.Properties.of(Material.WOOD)
    .mapColor(MapColor.WOOD)
    .strength(2.0f)
    .noOcclusion();

// ❌ 已移除
BlockBehaviour.Properties.create(Material.WOOD)  // create 不存在
```

#### EntityType 注册方式

1.18+ 使用 `EntityType.Builder.of()` 注册实体，移除了旧的 `EntityRegistry` 方法。

---

## 1.20.1 Forge 特有注意事项

### Forge 47.x 推荐配置

```properties
minecraft_version=1.20.1
forge_version=47.4.18
forge_version_range=[47,)
loader_version_range=[47,)
mapping_channel=official  # 或 parchment
mapping_version=20231030.163645  # official
# parchment: mapping_version=2023.09.03-1.20.1
```

### Parchment 映射配置

```properties
mapping_channel=parchment
mapping_version=2023.09.03-1.20.1
```

注册 Parchment Gradle 插件（在 build.gradle）：
```groovy
plugins {
    id 'org.parchmentmc.librarian.forgegradle' version '1.+'
}
```
