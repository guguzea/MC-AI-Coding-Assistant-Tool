# 版本变更记录：1.18.x

## 1.18.2 Forge（40.1.x）概述

> Forge 1.18.2 是 Minecraft Caves & Cliffs 更新的一部分，引入了大量世界生成和 API 变更。

## 核心变更

### 世界高度变化

| 版本 | 世界高度 |
|------|----------|
| 1.17.x | 0 ~ 256 |
| **1.18.x** | **-64 ~ 320** |

```java
// ✅ 1.18.2 世界高度
LevelHeightAccessor world = level;
int minBuildHeight = world.getMinBuildHeight();  // -64
int maxBuildHeight = world.getMaxBuildHeight();  // 320
```

### 注册表字段名变更

| 注册内容 | 旧名称（1.17.x） | 新名称（1.18.x） |
|----------|-----------------|-----------------|
| 方块实体 | `BLOCK_TILES` | `BLOCKENTITIES` |
| 实体类型 | `ENTITIES` | `ENTITYTYPES` |
| 菜单类型 | — | `CONTAINERS` |
| 流体类型 | — | `FLUIDTYPES` |

### DeferredRegister 成熟

1.18+ 是 DeferredRegister 完全成熟的版本：

```java
// ✅ 推荐方式
public static final DeferredRegister<Block> BLOCKS =
    DeferredRegister.create(ForgeRegistries.BLOCKS, MOD_ID);
BLOCKS.register(modEventBus);
```

### ForgeGradle 版本

| Minecraft 版本 | ForgeGradle 版本 |
|---------------|------------------|
| 1.16.x | FG3 |
| 1.17.x | FG4 |
| **1.18.x** | **FG5** |
| 1.20.x | FG6 |

```groovy
// build.gradle
id 'net.minecraftforge.gradle' version '[5.1.2,5.2)'
```

### pack_format 变化

| 版本 | pack_format |
|------|------------|
| 1.17.x | 7 |
| **1.18.x** | **8** |
| 1.19.x | 9 |
| 1.20.x | 15 |

### Parchment 映射

```properties
# gradle.properties
mapping_channel=parchment
mapping_version=1.18.2-2022.07.31
```

---

## 1.18.x 与其他版本对比

| 功能 | 1.18.2 | 1.19.x | 1.20.x |
|------|--------|--------|--------|
| Java | 17 | 17+ | 17+ |
| ForgeGradle | 5.x | 5.x / 6.x | 6.x |
| pack_format | **8** | 9 | 15 |
| 世界高度 | **-64~320** | -64~320 | 0~320 |
| DeferredRegister | ✅ | ✅ | ✅ |

---

## 迁移注意事项

### 从 1.17.x 迁移到 1.18.x

1. **世界高度**：检查所有 Y 坐标相关代码
2. **ForgeRegistries 字段**：更新字段名
3. **ForgeGradle**：从 FG4 升级到 FG5
4. **Java**：确保使用 Java 17

### Java 版本

```groovy
// ✅ 必须配置 Java 17
java.toolchain.languageVersion = JavaLanguageVersion.of(17)
```
