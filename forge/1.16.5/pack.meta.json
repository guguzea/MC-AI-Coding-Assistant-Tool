{
  "status": "ready",
  "pack-status": "ready",
  "platform": "forge",
  "minecraftVersion": "1.16.5",
  "scaffold": {
    "mode": "gradle",
    "gradle": "7.6",
    "provenance": {
      "text": "house",
      "wrapper": "gradle-wrapper-task"
    },
    "buildVerified": false,
    "gaps": [
      "按声明的 wrapper 钉值仍未跑通（`buildVerified` 只能为 false）：FG `[4.1,4.2)` 配置期硬拒 Gradle 7+（2026-09-10 实测 `_g_forge1.16.5_v5.log`：`Found Gradle version Gradle 7.6. Versions Gradle 7.0 and newer are not supported yet. Note: Support for Gradle 7 will be added in ForgeGradle 5.`），而 `gradle-wrapper.properties:3` 钉的正是 7.6。根 memory 裁定「Forge 各档与官方 MDK 的代差不追平，不擅自改钉值」⇒ wrapper 钉值保持不变，此条不是模板代码问题",
      "钉值之外、本档可自行修复的两处 Gradle-7-only 构造已于 2026-09-11 改到 FG4 可用形态（不是改钉值）：`build.gradle` 原 `base { archivesName = mod_id }` 在 Gradle 6.9.4 上不存在（实测 `Could not find method base() for arguments …`，`_g_fg4exp_1.16.5.log`；7.6 上存在）→ 已换回顶层 `archivesBaseName = mod_id`，与同 FG4 档 forge/1.15.2 一致；`settings.gradle` 原 `org.gradle.toolchains.foojay-resolver-convention 0.4.0` 在 7.3.3 上即报 `Could not generate a decorated class for type FoojayToolchainsPlugin. > org/gradle/jvm/toolchain/JavaToolchainResolverRegistry`（7.6 可用，但 FG4 不收 7+）→ 已删除，改为注释要求「用 JDK 8 运行 Gradle」",
      "ForgeGradle 4 缓存目录缺陷（本档构建成功的前置条件，必须披露）：FG4 用 `Files.newOutputStream` 写 tsrg 时不 `mkdirs`，首次构建必失败 —— `NoSuchFileException: …minecraft_user_repo\\de\\oceanlabs\\mcp\\mcp_config\\1.16.5-…\\srg_to_*.tsrg`（`MinecraftUserRepo.findSrgToMcp`）/ `…mcp_repo\\…\\obf_to_srg.tsrg`（`MCPRepo.findRenames`）。本机取证脚本按报错路径预建目录后重跑（最多 3 次）才通过；用户侧等价操作 = 重跑 `gradlew build` 或在报错路径手动建目录，与模板代码无关",
      "mappings 通道已于 2026-09-11 由 `parchment` 改为 `official`（`gradle.properties:15-16` = `official` / `1.16.5`）：本档 FG4 无 parchment provider —— 裸声明报 `Unknown mapping provider: parchment_2021.07.27-1.16.5`，补 `org.parchmentmc.librarian.forgegradle 1.+` 后插件自身在 FG4 不可应用（`Could not generate a decorated class for type LibrarianForgeGradlePlugin` → 缺 `net/minecraftforge/gradle/mcp/ChannelProvider`，FG5+ 才有；`_g_fg4exp2/exp3_1.16.5.log`）。Parchment 坐标本身存在（curl `maven.parchmentmc.org/org/parchmentmc/data/parchment-1.16.5/maven-metadata.xml` 含 `2021.07.27`），要用它必须先把 FG 升到 5+；`settings.gradle` / `build.gradle` 的 ParchmentMC 仓库保留给这类用户。改 official 同时是本包既有示例命名的归属：official = 官方 1.16.5-36.2.34 MDK 默认（`AGENTS.md` Mappings 行已记），且本包 rules/skills/code-patterns 全部 113 处 `.tab(ItemGroup.TAB_…)` / `stacksTo(...)` 只在该通道成立",
      "scaffold Java 代码现已在声明通道编译通过（2026-09-11，Gradle 6.9.4 + JDK 8 + 上述缓存补救，副本 `diff -r` 为 0、构建期零改动）：`_g_fg4exp8_1.16.5.log` BUILD SUCCESSFUL in 17s，产物 `build/libs/examplemod-1.0.0.jar`。达成前修掉的通道混用缺陷（javap `forge-1.16.5-36.2.34_mapped_official_1.16.5.jar` 逐名核实）：`ExampleMod.java` 的 `Item.Properties.maxStackSize(64)` → `stacksTo(64)`（MCP 名与同行 `tab(TAB_MISC)` 互斥，原文件在任一通道都编译不过）、`MinecraftServer.getWorld(World.OVERWORLD).getWorldInfo().getWorldName()` → `getServerDirectory()`（official 侧为 `getLevel(RegistryKey)`，且 `IWorldInfo` 无可读名 world-name getter）、`net.minecraftforge.event.server.ServerStartingEvent` → `net.minecraftforge.fml.event.server.FMLServerStartingEvent`（1.16.5 尚无前者）、删除两条不存在的 `net.minecraft.world.storage.loot.*` 导入（1.16 真路径为 `net.minecraft.loot.*`）",
      "scaffold 不含 assets（模型/纹理/lang）→ 复制后物品为 missingtexture、显示名为未翻译占位键；本模板只演示注册代码，Forge 全部 10 档 scaffold 同状（实测各档 src/main/resources 仅 mods.toml/mcmod.info + pack.mcmeta，assets 文件数 0）"
    ]
  }
}
