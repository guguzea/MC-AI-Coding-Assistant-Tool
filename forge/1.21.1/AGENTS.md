# Forge 1.21.1 — 无教程树

本目录 **不是** 完整 00–10 模组教程。Forge MkDocs 数据目前停在 1.20.4（`KNOWN_VERSIONS.forge` 无 1.21）。

改口：`search_forge_docs` / `search_docs({platform:"forge"})` 与 javadoc。不要把 1.20.4 的 00–10 改版本号冒充本档。
`detect_mod_project` 对本版规则树返回 `PACK_NOT_FOUND`。
不要把 NeoForge 1.21.1 的 DeferredHolder / PayloadHandlers 当 Forge 1.21 教程。
