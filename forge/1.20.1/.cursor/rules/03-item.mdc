# 03 — 物品开发

> 适用版本：Forge 1.20.1

---

## 约束

### Item 子类规范

- 基础物品类继承 `Item`（`net.minecraft.world.item.Item`）
- 禁止重写已废弃的 `onCreated()` 等方法
- 物品必须通过 `DeferredRegister<Item>` 注册

### Item.Properties 配置

```java
new Item.Properties()
    .stacksTo(int maxStackSize)                 // 默认 64
    .durability(int maxDamage)                  // 有耐久物品
    .craftRemainder(Item copyFrom)              // 消耗后保留物品
    .fireResistant()                            // 防火（鞘翅等）
    .rarity(Rarity.RARE)                        // 稀有度（COMMON, UNCOMMON, RARE, EPIC）
    .requiredFeatures(FeatureFlags...)           // 特性门控
```

> **注意**：`Item.Properties` 在 Forge 1.20.1 中**没有 `tab()` 方法**。物品的创造模式标签通过 `BuildCreativeModeTabContentsEvent` 单独注册，不要写在 Properties 里。

### ItemTier（工具材料）规范

```java
public enum MyItemTier implements Tier {
    MY_MATERIAL(4, 2000, 10.0f, 4.0f, 20, () -> Ingredient.of(Items.DIAMOND));

    // getLevel(), getUses(), getSpeed(), getAttackDamageBonus(), getEnchantmentValue(), getRepairIngredient()
}
```

- `getLevel()`：挖掘等级（木=0，石=1，铁=2，金=3，钻=4，奈=5）
- `getUses()`：耐久值
- `getSpeed()`：挖掘速度
- `getAttackDamageBonus()`：附加攻击伤害
- `getEnchantmentValue()`：附魔能力值

### 食物属性（FoodProperties）

```java
new Item.Properties()
    .food(new FoodProperties.Builder()
        .nutrition(int saturation)        // 饱食度（0-20）
        .saturationMod(float modifier)    // 饱和度修正
        .meat()                           // 狗可食用
        .alwaysEat()                      // 总是可食用（即使饱食）
        .effect(() -> new MobEffectInstance(MobEffects.MOVEMENT_SPEED, 200), 1.0f)
        .build()
    )
```

### 注册约束

- ItemBlock 关联方块时，registry name 必须与方块完全相同
- 工具/盔甲使用 `Tier` 时，需要在构造中传入 `.defaultDurability(durability)`
- **物品的创造模式标签通过 `BuildCreativeModeTabContentsEvent` 注册**（在 Mod 构造函数的 modEventBus 上），不写在 `Item.Properties` 里

---

## Decision Flow

### Decision: 需要什么类型的物品？

```
IF 需要可放置的基础方块物品
  → 注册 BlockItem（关联已有的方块）
  → 不需要新建 Item 子类

IF 需要普通物品（无特殊功能）
  → extends Item，使用默认属性
  → 最简单的物品

IF 需要工具（剑、镐、斧、铲）
  → extends DiggerItem
  → 需要定义 ItemTier
  → 需要在 constructor 传入：speed, damage, durability, tier, attackSpeed属性

IF 需要盔甲
  → extends ArmorItem
  → 需要定义 ArmorMaterial
  → 需要在 constructor 传入：material, slot, properties

IF 需要食物（可食用物品）
  → extends Item + 设置 .food()
  → 或 extends Item 覆盖 use() 方法实现自定义食用逻辑

IF 需要可耐久物品
  → 设置 .durability(maxDamage)
  → 覆盖 hurtEnemy() 处理使用逻辑
  → 覆盖 useOn() 处理方块交互（右键放置等）

IF 需要附魔书以外的附魔物品
  → 不支持，Forge 1.20.1 附魔只能通过附魔台/附魔桌

IF 需要鞘翅、火焰弹等特殊物品
  → 参考 Forge 源码或示例模组
  → 通常需要自定义渲染器
```

### Decision: ItemTier 定义

```
IF 自定义工具材料
  → 定义枚举实现 Tier 接口
  → 5 个必需方法全部实现
  → getRepairIngredient() 必须返回有效的 Ingredient
  → 在物品类中引用：super(speed, damage, durability, tier)

IF 复用现有材料
  → Tier.STONE / Tier.IRON / Tier.DIAMOND / Tier.GOLD（静态字段）
```

### Decision: 物品放置在哪个 Creative Tab

> **重要**：物品不写在 `Item.Properties` 中，而是通过 `BuildCreativeModeTabContentsEvent` 注册。

```
IF 建筑方块 → CreativeModeTabs.BUILDING_BLOCKS
IF 自然方块 → CreativeModeTabs.NATURAL_BLOCKS
IF 工具 → CreativeModeTabs.TOOLS
IF 武器 → CreativeModeTabs.COMBAT
IF 食物 → CreativeModeTabs.FOOD_AND_DRINK
IF 材料 → CreativeModeTabs.MATERIALS
IF 红石 → CreativeModeTabs.REDSTONE
IF 交通 → CreativeModeTabs.TRANSPORTATION
IF 杂项 → CreativeModeTabs.MISC
IF 搜索 → CreativeModeTabs.SEARCH（不推荐）
```

注册方式（示例）：

```java
// 在 Mod 构造函数中：
FMLJavaModLoadingContext.get().getModEventBus().addListener(MyMod::onBuildContents);

// 事件处理：
public static void onBuildContents(BuildCreativeModeTabContentsEvent event) {
    if (event.getTabKey() == CreativeModeTabs.BUILDING_BLOCKS) {
        event.accept(MY_BLOCK_ITEM);        // BlockItem 接受 ItemLike
        event.accept(MY_ITEM);             // 普通物品
    }
}
```

---

## 示例：基础物品

```java
// items/MyItem.java
public class MyItem extends Item {
    public MyItem() {
        super(new Item.Properties()
            .stacksTo(64)
        );
    }

    @Override
    public InteractionResult useOn(UseOnContext context) {
        // 右键点击方块时的逻辑
        return InteractionResult.SUCCESS;
    }
}
```

## 示例：工具（剑）

```java
// items/MyTier.java
public enum MyTier implements Tier {
    MY_MATERIAL(3, 1561, 8.0f, 3.0f, 15, () -> Ingredient.of(Items.DIAMOND));

    private final int level;
    private final int uses;
    private final float speed;
    private final float damage;         // 工具的基础伤害加成（不含类型加成）
    private final int enchantmentValue;
    private final Ingredient repairIngredient;

    MyTier(int level, int uses, float speed, float damage, int enchantmentValue, Supplier<Ingredient> repairIngredient) {
        this.level = level;
        this.uses = uses;
        this.speed = speed;
        this.damage = damage;
        this.enchantmentValue = enchantmentValue;
        this.repairIngredient = repairIngredient;
    }

    @Override public int getLevel() { return level; }
    @Override public int getUses() { return uses; }
    @Override public float getSpeed() { return speed; }
    @Override public float getAttackDamageBonus() { return damage; }
    @Override public int getEnchantmentValue() { return enchantmentValue; }
    @Override public Ingredient getRepairIngredient() { return repairIngredient.get(); }
}
```

```java
// 注册剑：4 参数构造函数
// Tier.getAttackDamageBonus() + 3.0f（剑的类型加成）= 总攻击伤害
// 例如：damage=3.0f → 总攻击伤害 = 3.0f + 3.0f = 6.0f
// 注意：attackDamageModifier 参数类型是 int，不是 float
public static final RegistryObject<Item> MY_SWORD = ITEMS.register("my_sword",
    () -> new SwordItem(MyTier.MY_MATERIAL, 3, 1.6f, new Item.Properties())
);
//                           ^^^ int 参数：基础攻击加成
//                                    ^^^^ float 参数：攻击速度
// 创造模式标签通过 BuildCreativeModeTabContentsEvent 注册到 CreativeModeTabs.COMBAT
```

## 示例：盔甲

```java
// items/MyArmorMaterial.java
public enum MyArmorMaterial implements ArmorMaterial {
    MY_MATERIAL("my_material", 40, new int[]{4, 7, 9, 4}, 20,
        SoundEvents.ARMOR_EQUIP_DIAMOND, 3.0f, 0.1f, () -> Ingredient.of(Items.DIAMOND));

    // getDurability(SlotType), getDefenseForType(SlotType), getEnchantmentValue(),
    // getEquipSound(), getToughness(), getKnockbackResistance(), getRepairIngredient()
}
```

```java
// items/MyArmorItem.java
public class MyArmorItem extends ArmorItem {
    public MyArmorItem(MyArmorMaterial material, Type type, Properties properties) {
        super(material, type, properties);
    }
}
```

## 示例：食物

```java
// items/MyFoodItem.java
public class MyFoodItem extends Item {
    public MyFoodItem() {
        super(new Item.Properties()
            .food(new FoodProperties.Builder()
                .nutrition(4)
                .saturationMod(0.3f)
                .meat()
                .effect(() -> new MobEffectInstance(MobEffects.JUMP, 200, 1), 1.0f)
                .build())
        );
    }

    @Override
    public ItemStack finishUsingItem(ItemStack stack, Level level, LivingEntity entity) {
        super.finishUsingItem(stack, level, entity);
        return stack;
    }
}
// 创造模式标签通过 BuildCreativeModeTabContentsEvent 注册到 CreativeModeTabs.FOOD_AND_DRINK
```

> 注：`effect()` 中的 `MobEffectInstance` 第一个参数是 `MobEffect`（如 `MobEffects.JUMP`），不是 `int` ID。
