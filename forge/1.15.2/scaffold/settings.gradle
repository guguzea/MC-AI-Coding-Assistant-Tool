pluginManagement {
    repositories {
        gradlePluginPortal()
        maven {
            name = 'MinecraftForge'
            url = 'https://maven.minecraftforge.net/'
        }
        maven {
            name = 'ParchmentMC'
            url = 'https://maven.parchmentmc.org'
        }
    }
}

// 不加载 foojay-resolver-convention：0.4.0 在本档声明的 Gradle 7.3.3 上实测报
// Could not generate a decorated class for type FoojayToolchainsPlugin，而 FG4 又硬拒 Gradle ≥7
// ⇒ 请用 JDK 8 运行 Gradle（JAVA_HOME 或 org.gradle.java.home）
