# 版本变更记录：1.12.x

## 1.12.2 Forge 重要变更

### RegistryEvent 替代旧方式

1.12.2 使用 `RegistryEvent.Register<T>` 进行注册：

```java
@Mod.EventBusSubscriber(modid = MOD_ID)
public class ModRegistry {
    @SubscribeEvent
    public static void register(RegistryEvent.Register<Block> event) {
        event.getRegistry().register(
            new Block(Material.ROCK)
                .setRegistryName(MOD_ID, "my_block")
        );
    }
}
```

### pack_format 变更

| MC 版本 | pack_format |
|--------|-------------|
| 1.12.2 | **4** |
| 1.13.x | 6 |
| 1.14.x | 7 |
| 1.16.x | 8 |
| 1.18.x | 9 |
| 1.20.x | 15 |

### Java 版本

- 1.12.2 要求 **Java 8**
- 1.18+ 要求 Java 17+
- 1.20.6+ 推荐 Java 21

### 注册表命名

| 版本 | 注册表类 |
|------|---------|
| 1.12.2 | `IForgeRegistry<Block>` |
| 1.18+ | `ForgeRegistries.BLOCKS` |

### NBT 类名

| 版本 | NBT 类 |
|------|---------|
| 1.12.2 | `NBTTagCompound` |
| 1.18+ | `CompoundTag` |

### 方块实体

| 版本 | 类名 |
|------|------|
| 1.12.2 | `TileEntity` |
| 1.18+ | `BlockEntity` |

### 代理模式

| 版本 | 方式 |
|------|------|
| 1.12.2 | `@SidedProxy` + CommonProxy/ClientProxy |
| 1.18+ | `DistExecutor` + modEventBus |
