# 07 — 数据包

> 适用版本：Forge 1.12.2

---

## 约束

### 重要：1.12.2 无 DataGen API

**Forge 1.12.2 没有 DataGenerator API**。所有数据包（recipes、loot_tables、advancements 等）必须**手动编写 JSON 文件**，放在 `src/main/resources/assets/{modid}/data/{modid}/` 目录下。

### 目录结构

```
src/main/resources/
├── mcmod.info
├── pack.mcmeta
└── assets/{modid}/
    ├── lang/
    │   └── en_us.lang
    ├── models/
    │   ├── block/
    │   └── item/
    ├── blockstates/
    ├── textures/
    └── recipes/            # 配方 JSON（手动编写）
    └── loot_tables/        # 战利品表 JSON（手动编写）
```

> 注意：1.12.2 的资源文件和数据包都在 `assets/{modid}/` 下，不像 1.13+ 那样分离为 `data/` 和 `assets/` 目录。

### pack.mcmeta

```json
{
  "pack": {
    "pack_description": "${mod_name}",
    "pack_format": 4
  }
}
```

> `pack_format` 在 1.12.2 = **4**（不是 1.13+ 的更高值）

---

## Decision Flow

### Decision: 生成什么类型的数据

```
IF 生成合成配方
  → 手动编写 JSON 文件
  → 放到 assets/{modid}/recipes/

IF 生成战利品表
  → 手动编写 JSON 文件
  → 放到 assets/{modid}/loot_tables/

IF 生成语言文件
  → 使用 .lang 格式（不是 .json）
  → 放到 assets/{modid}/lang/

IF 生成方块状态（BlockState）
  → 手动编写 JSON
  → 放到 assets/{modid}/blockstates/

IF 生成模型
  → 手动编写 JSON
  → 放到 assets/{modid}/models/
```

---

## 示例：合成配方 JSON

```json
{
  "type": "crafting_shaped",
  "pattern": [
    " X ",
    " X ",
    " Y "
  ],
  "key": {
    "X": { "item": "minecraft:diamond" },
    "Y": { "item": "minecraft:stick" }
  },
  "result": { "item": "examplemod:my_item", "count": 1 }
}
```

```json
{
  "type": "crafting_shapeless",
  "ingredients": [
    { "item": "minecraft:diamond" },
    { "item": "minecraft:diamond" }
  ],
  "result": { "item": "minecraft:diamond_sword", "count": 1 }
}
```

---

## 示例：熔炉配方

```json
{
  "type": "smelting",
  "ingredient": { "item": "minecraft:iron_ore" },
  "result": "minecraft:iron_ingot",
  "experience": 0.7,
  "cookingtime": 200
}
```

---

## 示例：战利品表

```json
{
  "pools": [
    {
      "rolls": 1,
      "entries": [
        {
          "type": "item",
          "name": "examplemod:my_item"
        }
      ],
      "conditions": [
        {
          "condition": "minecraft:survives_explosion"
        }
      ]
    }
  ]
}
```

---

## 示例：语言文件（.lang 格式）

```
# 物品
item.examplemod.my_item.name=My Item
item.examplemod.my_block.name=My Block

# 方块
tile.examplemod.my_block.name=My Block

# 实体
entity.examplemod.my_entity.name=My Entity
```

---

## 常用语言键格式

```
# 物品
item.{modid}.{item_name}.name={显示名称}

# 方块
tile.{modid}.{block_name}.name={显示名称}

# 实体
entity.{modid}.{entity_name}.name={显示名称}

# 工具提示
tooltip.{modid}.{item_name}.{line}={提示文本}
```

---

## 常见错误

- ❌ 配方 JSON 放在错误路径（1.12.2 应在 `assets/{modid}/recipes/`）
- ❌ 语言文件使用 .json 格式（1.12.2 使用 .lang 格式）
- ❌ pack_format 版本错误（1.12.2 用 4）
- ❌ 配方中 namespace 包含大写字母
