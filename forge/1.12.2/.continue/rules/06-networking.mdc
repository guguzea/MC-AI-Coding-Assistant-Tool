# 06 — 网络通信

> 适用版本：Forge 1.12.2

---

## 约束

### 物理端约束（最重要）

- **所有网络包只能在物理端分隔的类中使用**
- 服务端 mod 的网络包发送给客户端时，客户端收到的是**反序列化后的数据对象**
- 禁止在客户端包类中直接访问服务端独有类（如 `WorldServer`）
- 使用 `DistExecutor` 或 `@SideOnly` 判断当前物理端
- 使用 `world.isRemote` 判断当前逻辑端（1.12.2 用 `world.isRemote`，不是 `level.isClientSide`）

### SimpleNetworkWrapper 配置

```java
public static final SimpleNetworkWrapper INSTANCE = NetworkRegistry.INSTANCE.newSimpleChannel(CHANNEL_ID);

// 注册消息
INSTANCE.registerMessage(id, MyMessageHandler.class, MyMessage::encode, MyMessage::decode, MyMessage::handle);
```

### 消息类规范

- 实现 `IMessage`（含 `writeBytes`/`readBytes`）
- 无需实现 `IMessageHandler`，处理器通过 `registerMessage` 的 BiConsumer 注入
- 消息类必须有无参构造函数（用于反序列化）
- **禁止**在消息类中存储对世界或实体的直接引用（序列化后引用会断开）
- 使用 `PacketBuffer`（继承自 `ByteBuf`）手动读写基础类型

### 注册时机

- `SimpleNetworkWrapper` 实例创建在 `FMLPreInitializationEvent` 或 `FMLInitializationEvent` 中
- 消息注册在 `FMLInitializationEvent` 中

---

## Decision Flow

### Decision: 选择同步需求类型

```
IF 需要服务端主动推送给客户端（服务器通知客户端）
  → 使用 ServerToClientPacket（服务端发送，客户端处理）

IF 需要客户端主动请求服务端（客户端触发服务端逻辑）
  → 使用 ClientToServerPacket（客户端发送，服务端处理）
  → 验证客户端发送的数据（永远不要信任客户端！）

IF 需要双向同步
  → 使用双向消息：客户端发送请求 → 服务端回复

IF 同步大量数据
  → ❌ 不要逐字段发送（高网络开销）
  → 使用 `NBTTagCompound` 批量序列化
```

### Decision: 消息方向

```
服务端 Mod：
  INSTANCE.sendTo(msg, playerMP)   → 发送给指定玩家
  INSTANCE.sendToAll(msg)         → 发送给所有玩家

客户端 Mod：
  INSTANCE.sendToServer(msg)        → 发送给服务端
```

---

## 示例：消息类定义

```java
// messages/MyMessage.java
public class MyMessage implements IMessage {
    private int value;
    private ResourceLocation targetId;

    public MyMessage() {}  // 必须有无参构造函数

    public MyMessage(int value, ResourceLocation targetId) {
        this.value = value;
        this.targetId = targetId;
    }

    @Override
    public void toBytes(PacketBuffer buf) {
        buf.writeInt(value);
        buf.writeResourceLocation(targetId);
    }

    @Override
    public void fromBytes(PacketBuffer buf) {
        this.value = buf.readInt();
        this.targetId = buf.readResourceLocation();
    }
}
```

## 示例：消息处理器

```java
// messages/MyMessageHandler.java
public class MyMessageHandler implements IMessageHandler<MyMessage, IMessage> {
    @Override
    public IMessage onMessage(MyMessage msg, MessageContext ctx) {
        // ctx.getServerHandler().player.getEntityWorld() 获取世界
        if (ctx.side == EnumFacing.EnumFacingSide.SERVER) {
            // 服务端收到消息
            EntityPlayerMP player = ctx.getServerHandler().player;
            // 处理逻辑...
        }
        return null;  // 用于 Reply 模式
    }
}
```

## 示例：注册和发送

```java
// network/NetworkHandler.java
public class NetworkHandler {
    public static final SimpleNetworkWrapper INSTANCE =
        NetworkRegistry.INSTANCE.newSimpleChannel(MOD_ID + ":main");

    public static void registerMessages() {
        INSTANCE.registerMessage(
            0,                          // ID
            MyMessage.class,             // 消息类
            MyMessage::toBytes,          // 编码
            MyMessage::new,              // 解码（构造函数引用）
            MyMessageHandler::onMessage // 处理器
        );
    }

    // 服务端发送给指定玩家
    public static void sendTo(EntityPlayerMP player, MyMessage message) {
        INSTANCE.sendTo(message, player);
    }

    // 服务端广播
    public static void sendToAll(MyMessage message) {
        INSTANCE.sendToAll(message);
    }

    // 客户端发送给服务端
    public static void sendToServer(MyMessage message) {
        INSTANCE.sendToServer(message);
    }
}
```

## 示例：客户端发送消息

```java
// 在客户端代码中（玩家输入等触发）
NetworkHandler.sendToServer(new MyMessage(value, targetId));
```

## 示例：服务端广播消息

```java
// 在服务端逻辑中广播给所有玩家
NetworkHandler.INSTANCE.sendToAll(new MyBroadcastMessage(data));
```

> 重要：收到消息后**永远不要**直接修改世界。Forge 1.12.2 的消息处理在网络线程，必须将操作发送到主线程执行。
> 注意：`DistExecutor` 用于服务端发往客户端的单向消息。
